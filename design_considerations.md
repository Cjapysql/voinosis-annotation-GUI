# 설계 시 고려했던 사항들

DMS 라벨링 툴을 만들면서 실측 데이터를 검증해가며 확인/결정한 사항들을 정리.
(CLAUDE.md의 "Domain rules"를 기반으로, 이번 세션에서 새로 확인된 내용까지 반영)

## 1. 시간 정렬 (Synchronization)

- **모든 센서는 t_sec(유닉스 epoch 초) 하나의 공통 축 위에서 정렬한다.** 리샘플링해서
  공통 그리드를 만드는 게 아니라, 센서마다 독립적인 "절대시각 → 로컬 인덱스" 변환
  함수를 갖고, 라벨 구간(start_ts, end_ts) 하나를 그대로 각 센서에 넘겨서 각자
  알아서 자기 좌표로 바꾸게 했다 (`back/timestamp_index.py`, `back/radar_index.py`).
- **비디오를 시간 정렬의 기준(reference)으로 삼는다.** 카메라는 프레임마다 촘촘하게
  실측 timestamp가 남지만, 오디오는 청크 단위라 sparse해서 세그먼트 경계 판단을
  오디오 자체 데이터로 하면 엉뚱한 세그먼트를 짚는 사고가 실제로 발생했다. 그래서
  `AudioTimestampIndex`는 가능하면 비디오가 계산한 `segment_starts`를 그대로 받아쓴다.
- **fps/샘플레이트를 메타데이터로 신뢰하지 않는다.** cv2가 보고하는 fps는 컨테이너에
  박힌 명목값(예: 정확히 30.0)이라 실측과 다르다 - 실측 timestamp의 (첫~끝 실제
  경과시간) ÷ (진짜 프레임 수)로 역산한 fps를 쓴다. 이걸 무시하면 세그먼트 끝쪽에서
  최대 4~5초까지 어긋난다.
- **정렬 기준 시각으로는 "저장 완료 시각"이 아니라 "센서가 실제 캡처한 시각"을
  쓴다.** 신규 timestamp csv 스키마엔 두 가지 시각이 다 있는데(동기화기준시각_sec
  vs 저장완료시각_sec), 후자는 디스크 저장 지연만큼 밀려 있어 다른 센서와 같은
  축이 아니다.
- **float64 정밀도가 정렬에 충분한지 실측으로 검증했다.** 이 프로젝트의 시각 값
  크기대(2026년, 10자리 정수부)에서 float64 최소 표현 간격은 약 0.24마이크로초로,
  필요한 최소 단위(오디오 샘플 간격 62.5μs, 비디오 프레임 간격 33ms)보다 훨씬
  촘촘해서 문제없다.

## 2. 세그먼트 파일(녹화 중단/재개) 처리

- **녹화가 중간에 멈췄다 재개되면 기존 파일에 이어쓰지 않고 새 파일이 생긴다.**
  그래서 카메라/오디오 모두 하나의 논리적 스트림이 물리적으로 여러 파일(seg 번호로
  구분)로 쪼개져 있을 수 있고, 번호가 연속이 아닐 수도 있다(001, 003, 006 등) -
  정렬 순서만 신뢰하고 번호 값 자체는 신뢰하지 않는다.
- **재생용으로는 여러 오디오 세그먼트를 미리 하나의 연속 wav로 이어붙인다**
  (`back/audio_stitcher.py`). `PlaybackController`가 파일 하나를 마스터 시계로
  쓰는 구조라 "경과시간 = 파일 내 위치"라는 단순 선형식이 끝까지 유지돼야 하기
  때문. 세그먼트 사이 무음 구간은 실제로 무음을 채워 넣고, 세그먼트 실제 길이가
  비디오가 선언한 길이보다 길면(실측상 항상 그랬음, 세그먼트당 최대 1.5초) 초과분을
  잘라낸다.
- **최종 export(자르기)는 재생용 이어붙이기와 별개로, 원본 세그먼트 파일에서
  직접 잘라낸다** (`back/segment_exporter.py`) - 재인코딩 방식(ffmpeg -ss 키프레임
  탐색이 아니라 cv2로 프레임 단위 순차 디코딩)이라 정확도가 더 중요한 곳.

## 3. 원본 데이터 수집 파이프라인이 계속 바뀐다는 전제

- 수집 파이프라인이 개발 도중 최소 두 번 바뀌었다: (1) 카메라/오디오/IMU/watch
  폴더 구조가 "seg 번호가 파일명에 붙는 flat 구조"에서 "seg 번호가 폴더명인 중첩
  구조"로, (2) 카메라/오디오 timestamp csv의 컬럼 스키마가 영문 `frame_idx,t_sec,...`
  에서 한글 `저장영상프레임번호,동기화기준시각_sec,...`로.
- `back/session_loader.py`(파일 탐색)는 신/구 폴더 구조를 자동 판별해서 둘 다
  지원하도록 만들었다 - 이미 수집된 과거 트라이얼 데이터를 계속 쓸 수 있어야 하기
  때문. 반면 `back/timestamp_index.py`(csv 컬럼 파싱)는 신규 스키마만 지원하도록
  단순화했다 - 앞으로의 모든 신규 레코딩이 이 스키마로 고정되는 게 확정됐기 때문
  (오래된 트라이얼의 재처리 필요성보다 코드 단순성을 우선).
- IMU/watch csv는 이 스키마 변경의 영향을 받지 않았다(`frame_idx,t_sec,...` 그대로) -
  변경이 파이프라인 전체가 아니라 카메라/오디오 로거에만 있었던 것으로 보인다.
- **신규 스키마엔 "복제 프레임" 개념이 추가됐다** (`복제프레임여부`, `프레임종류`) -
  카메라 드라이버가 다음 프레임을 제때 못 받으면(USB 대역폭 경합, CPU 스케줄링
  지연, 디스크 I/O 백프레셔 등으로 추정) 직전 프레임을 복사해서 넣어 mp4의 fps를
  일정하게 유지한다. 현재는 이 플래그를 무시하고 진행하기로 함 - 복제 행은 그냥
  직전 행과 동일한 시각이 한 번 더 찍히는 것뿐이라 gap 감지나 시간→인덱스 계산에
  영향이 없기 때문.

## 4. 초안(Draft)과 최종 확정(Export)의 명확한 분리

- 라벨링 도중의 모든 상태(타임스탬프+라벨 쌍)는 `back/draft_store.py`의
  `LabelDraft`에만 존재하고, 이 시점까진 아무 것도 실제로 잘리지 않는다. 자유롭게
  수정/삭제 가능.
- "최종 저장" 버튼을 눌러야만 `SegmentExporter`가 호출되어 실제로 모든 센서 파일을
  프레임/샘플 정확도로 잘라 `session_XXX_id_XXX/{scenario}/{segment}/` 구조로
  저장한다 - 이 동작은 되돌릴 수 없는 것으로 취급한다.

## 5. 시나리오별로 다른 세그멘테이션 자유도

- `distraction`은 라벨러가 완전히 자유롭게 구간을 나눌 수 있다(하나의 task
  window를 여러 서브세그먼트로 쪼갤 수 있음) - segment 번호도 시나리오별 독립
  카운터.
- `drowsiness`/`cognitive`는 survey json에서 자동 계산된 구간이 기본값(가이드라인)
  이지만, 라벨러가 프레임 단위로 정밀 조정할 수 있게 했다 - 자동 계산이 항상
  프레임 단위로 정확하다고 보장할 수 없기 때문.
- `drowsiness` 구간은 `driving_task.start_time - 60초 ~ start_time`으로 고정
  계산된다 - 설문 응답(음성)이 그 앞뒤 오디오에 섞여 들어가지 않게 하려는 의도된
  규칙.
- `cognitive`는 시나리오 전체에 걸친 카운터 없이, `pre_nback1~3, pre_cbt1~3,
  post_nback1~3, post_cbt1~3` 같은 고정된 이름을 쓴다 - task 종류/순서가 이미
  설문 구조로 정해져 있기 때문.

## 6. 계층 분리 (back / front)

- `back/`은 Qt를 전혀 참조하지 않는 순수 데이터 계층 - 다른 프론트엔드나 배치
  처리 스크립트에서도 그대로 재사용 가능하게 하려는 의도. `front/`는 `back`을
  가져다 쓰지만 반대 방향(back → front)은 금지.
- 진단/검증용 스크립트(`check_av_frames.py` 등)도 `back`만 임포트해서 Qt 없이
  headless로 동작한다.

## 7. 비개발자 배포

- 라벨러가 파이썬 환경을 직접 구성하지 않아도 되도록 PyInstaller로 단일 실행
  파일을 빌드한다(`dms_labeling.spec`, `build.sh`). 배포 대상과 같은 OS/아키텍처
  에서 빌드해야 하는 제약이 있음.
- `DMS_Actions.xlsx`는 실행 파일 번들에 넣거나, 없으면 실행 파일 옆에 두면
  자동으로 찾도록 해서 배포 유연성을 확보했다. xlsx가 아예 없어도 "기타"(자유
  텍스트) 라벨링은 가능하게 해서 완전히 막히지 않게 함.

## 8. 실측 데이터에 대한 방어적 처리 (확인 안 된 가정에 대해)

- watch CSV 컬럼명(`timestamp` vs `t_sec`)이 실제 하드웨어로 확정 검증되지 않아서,
  `_filter_csv_by_time`이 두 후보를 다 시도한다 - 못 찾으면 예외를 던지는 대신
  빈 파일로 조용히 넘어가게 했다(하드웨어 미확정 상태에서 전체 export가 죽는 것보다
  나은 선택).
- IMU 폴더는 대소문자 표기가 실측 데이터마다 다를 수 있어서(`IMU` vs `imu`) 후보
  이름을 순서대로 시도하는 `_resolve_dir` 헬퍼를 둠.

## 9. 오프라인/헤드리스 환경에서의 검증

- 개발 환경에 디스플레이/오디오 장치가 없어서, `QT_QPA_PLATFORM=offscreen` +
  `widget.grab()`으로 실제 렌더링 결과를 PNG로 저장해 확인하고, PulseAudio
  `module-null-sink`로 가상 오디오 장치를 만들어 `QMediaPlayer`가 실제 이벤트
  루프를 돌며 `positionChanged`가 진행되는지까지 검증하는 방식을 썼다.

## 10. 재생 중 마스터 시계 설계

- 여러 카메라 패널을 동시에 갱신해야 하는데, `QMediaPlayer.positionChanged`
  신호만 믿고 화면을 갱신하면 실측 약 15Hz(64ms 간격)로만 신호가 와서 영상이
  뚝뚝 끊긴다. 그래서 오디오 위치 변경 시점을 벽시계(`time.monotonic()`)에
  앵커링해두고, 별도의 33ms 타이머가 그 사이를 외삽해서 부드럽게 갱신하도록
  분리했다.

## 11. 세그먼트별 timestamp csv (스트림 전체 공용 csv 모델 폐기)

- 신 파이프라인에서는 세그먼트 폴더(`seg{NNN}/`)마다 자기 timestamp csv가 있고,
  그 csv는 그 세그먼트만의 정확한 데이터라는 게 실측으로 확인됐다(수집 장비를
  정지했다가 재시작하면 세그먼트가 나뉘는데, 재시작 이후에도 각 구간의 타임스탬프는
  정확히 찍힌다는 게 확정 사실). 과거(구 파이프라인)엔 세그먼트 여러 개가 csv
  하나를 공유하는 flat 구조뿐이었다.
- 그래서 `CameraStreamFiles`/`AudioStreamFiles`의 `segment_files`를 `(seg_num,
  media_path)` 2튜플에서 `(seg_num, media_path, timestamp_csv_path)` 3튜플로
  바꿔서, 세그먼트 하나하나가 자기 csv를 직접 들고 있게 했다. `back/timestamp_index.py`의
  `_load_segment_t_secs()`가 "같은 csv 경로를 공유하는 세그먼트가 몇 개인지"로
  구/신 구조를 자동 판별한다 - 혼자 쓰는 csv면 통째로 신뢰하고(신 구조), 여럿이
  공유하면 기존 gap 감지로 나눈다(구 구조, 하위호환).
- 오디오도 카메라와 동일하게 자기 데이터로 먼저 스스로 `segment_starts`를
  계산하도록 바꿨다(예전엔 오디오 자체 csv가 sparse해서 세그먼트를 잘못 짚는
  사고 전례 때문에 무조건 비디오 뒤에 숨었음). `reference_segment_starts`는
  이제 "자기 데이터로 도저히 못 채운 세그먼트가 있을 때만" 쓰는 보조 수단으로
  축소됐고, 그마저도 즉시 계산되는 값이 아니라 **필요할 때만 평가되는 콜러블**로
  넘긴다 - 보통의 경우(자기 데이터로 다 채워짐)엔 이 무거운 계산(카메라 인덱스를
  새로 만드는 것) 자체가 아예 실행되지 않게 하기 위함.
- IMU/watch도 세그먼트별로 여러 파일이 있을 수 있어서 `dict[str, Path]` →
  `dict[str, list[Path]]`로 바꿨다. export 시 그 리스트의 파일들을 전부 열어
  라벨 구간에 걸치는 행을 모아 합친다 - 이전엔 마지막 세그먼트 파일만 남기고
  앞쪽 세그먼트는 스캔 과정에서 조용히 버려지는 버그가 있었다.

## 12. 무거운 계산은 백그라운드 스레드로, QWidget 조립은 메인 스레드로 분리

- 카메라 timestamp csv 파싱(세그먼트 하나에 수만~수십만 행)과 오디오 세그먼트
  이어붙이기(수백 MB 파일 읽기/쓰기)가 실측 몇 초~수십 초씩 걸려서, 메인(UI)
  스레드에서 동기적으로 실행하면 그동안 Qt 이벤트 루프가 멈춰 OS가 "응답 없음"을
  띄우는 문제가 두 지점(최종 저장, 라벨링 화면 진입) 모두에서 실제로 발생했다.
- QWidget은 반드시 메인 스레드에서 만들어야 해서, "무거운 순수 계산"(`back/`
  계층, Qt 미의존)과 "위젯 조립"(`front/` 계층)을 분리했다: `QThread` 워커
  (`ExportWorker`, `LabelingPageDataLoader`)가 계산만 먼저 끝내고, 그 결과(이미
  계산된 카메라 인덱스, 오디오 스티칭 결과 등)를 받아서 메인 스레드가 위젯을
  빠르게 조립한다.
- export 워커는 세부 단계(카메라 스트림별/오디오/IMU 등)를 콜백으로 보고해서
  진행률을 표시하고, `DraftStore.mark_committed()`는 백그라운드 스레드가 아니라
  완료 신호를 받은 **메인 스레드에서만** 호출한다 - draft_store 파일 I/O를
  두 스레드가 동시에 건드리지 않게 하기 위함.
- Qt 시그널로 (튜플 키를 가진, 커스텀 객체를 값으로 담은) `dict`를 그대로
  넘기면 PySide6가 스레드 간 큐잉 시 C++ 타입 변환에 실패하는 걸 실측으로
  확인했다 - 이런 복잡한 파이썬 객체는 시그널 타입을 `dict`/`list`가 아니라
  `object`로 선언해서 그대로 통과시켜야 한다.
- `QMediaPlayer.setSource()`도 비동기라, 미디어가 실제로 로드되기 전에
  `setPosition()`으로 seek을 걸면 조용히 무시될 수 있다는 것도 실측으로
  확인했다 - `mediaStatusChanged`로 로드 완료 시점을 감지해서 그때 목표
  위치를 다시 적용하는 방어 로직이 필요했다. 이 로딩이 끝나기 전에 화면을
  넘기면 라벨러가 재생을 누르는 순간 의도한 위치가 아니라 맨 처음부터
  재생되는 문제로 이어져서, 화면 전환 자체를 이 로딩 완료 시점까지 미룬다.
- 여러 시나리오 페이지가 `SegmentExporter` 인스턴스를 공유하는 구조라, 서로
  다른 시나리오에서 동시에 "최종 저장"을 누르면 캐시/카운터 등 공유 가변
  상태를 두 스레드가 동시에 건드리는 경쟁 상태가 생길 수 있다. "최종 저장"을
  누를 때마다 독립된 `SegmentExporter`를 새로 만들어서 공유 상태 자체를
  없애는 방식으로 해결했다(아래 13번 덕분에 매번 새로 만들어도 세그먼트
  번호가 안 꼬인다).

## 13. 세션을 이어서 작업하는 것(재시작/재개)을 원칙적으로 지원

- 라벨링을 하다가 앱을 끄고 나중에 같은 트라이얼을 다시 열어 이어서 하는 게
  실제 워크플로우로 확인됐다. 이를 위해:
  - `SegmentNamer`가 생성될 때 `session_dir`을 스캔해서 이미 있는 세그먼트
    번호 중 최댓값부터 이어간다 - 이전엔 항상 0부터 시작해서, 재시작하면
    이전 결과(예: `distraction_segment001~003`)를 새 `001`이 조용히 덮어쓸
    위험이 있었다.
  - `DraftStore`가 `committed=True`인 draft의 실제 출력 폴더(`segment_dir`,
    새로 추가된 필드)가 디스크에 여전히 있는지 확인해서 진행 상황/완료
    표시를 계산한다 - 라벨러가 결과물을 수동으로 지워도 "완료"로 계속
    보이는 걸 막기 위함. 이 필드가 생기기 전에 이미 커밋된 옛 기록은
    확인할 방법이 없으니 그대로 완료로 인정한다(하위호환).
  - "작업 중인 구간들" 목록이 원래는 미완료 draft만 보여줬는데, 완료된
    것까지 전부(`[완료]` 표시) 보여주도록 바꿨다 - 최종 저장을 눌러도 작업
    이력이 화면에서 사라지지 않게 하기 위함. 단, 완료된 항목은 수정/삭제를
    막아서 "최종 저장 = 되돌릴 수 없는 커밋" 원칙은 그대로 유지한다(정말
    지우고 싶으면 앱이 아니라 파일시스템에서 직접 폴더를 지우는, 의도적으로
    번거로운 경로만 남겨둠 - `committed_window_ids()`가 그 상황을 정상적으로
    받아들이도록 이미 되어 있음).

## 14. 비디오 프레임 탐색 최적화 - 정확도를 검증한 뒤에만 빠른 경로 사용

- export 시 자르려는 구간이 세그먼트 파일 뒤쪽에 있으면(예: 14만 프레임짜리
  파일의 5천 번째 프레임부터), 프레임 0부터 순차 디코딩하며 세는 기존 방식이
  스트림 하나당 수십 초씩 걸려 전체 export가 1분 가까이 걸리고 OS가 "응답
  없음"을 띄우는 원인이 됐다(51초 중 대부분이 이 부분이었음을 프로파일링으로
  확인).
- `cv2.VideoCapture.set(POS_FRAMES, N)`으로 탐색한 뒤 실제 도달한 위치
  (`cap.get`)를 다시 확인해서, 요청한 지점 이전(또는 정확히 그 지점)에
  도달했으면 거기서부터 순차 디코딩으로 마저 채우는 방식으로 바꿨다. 이
  프로젝트가 쓰는 mp4v 코덱에서 요청한 프레임에 정확히 도달하는 것을 픽셀
  단위로 실측 검증했다(`np.array_equal` == True). 혹시 다른 파일/코덱에서
  탐색이 요청 지점을 넘어서 버리면(정확도가 깨질 수 있는 상황) 안전하게
  프레임 0부터 순차 디코딩하는 기존 방식으로 자동 폴백하고, 그 사실을
  `export_warnings`에 기록해서 라벨러에게 알린다 - "빠른 길이 실제로
  정확하다고 확인될 때만 쓰고, 아니면 느려도 확실한 방법으로 돌아간다"는
  원칙.

## 15. 물리적 센서 장착 특성 보정 (road 카메라 180도 회전)

- road 카메라(rgb+infrared)가 광축 기준 180도 돌아간 채 장착되어 있어서,
  화면 표시(`front/stream_player.py`)와 최종 export(`back/segment_exporter.py`)
  둘 다 `cv2.rotate(frame, cv2.ROTATE_180)`을 적용한다(상하좌우 동시 반전 -
  좌우만 뒤집는 거울 반전과는 다름). 재인코딩 손실압축 때문에 픽셀 단위
  완전 일치는 안 되지만(평균 차이 ~4/255, 압축 노이즈 수준), "회전 안
  됐다고 가정"했을 때의 차이(~30/255)와 비교해서 회전이 올바르게 적용됐음을
  검증했다.

## 16. 재생 중 "구간만 미리보기" (자유 재생과 구분되는 임시 상태)

- "작업 중인 구간들" 목록에서 항목을 클릭하면 그 구간 시작으로 이동하고,
  재생을 누르면 그 구간 **끝에서 자동으로 멈추도록**(`PlaybackController.
  set_playback_limit`) 해서, 완료/미완료 구간의 내용을 클릭 한 번으로 확인할
  수 있으면서도 이어지는 다른 구간까지 그냥 계속 재생되지 않게 했다. 타임라인
  직접 클릭, 다른 태스크로 전환, 시작/끝점 지정, 프레임 이동, 영상 화면 클릭
  등 "이제 자유롭게 다루겠다"는 신호가 되는 모든 조작에서 이 한계를 해제하도록
  각 진입점을 전부 점검했다.
