# DMS 라벨링 툴 설치/실행 안내

사용하시는 컴퓨터에 맞는 부분만 따라하시면 됩니다.

- **리눅스(우분투)** → 아래 리눅스 사용자 부분
- **윈도우** → 아래 윈도우 사용자 부분

두 경우 모두 라벨링 시작하기부터는 화면이 동일합니다.

## 리눅스 사용자

### 준비물

- **우분투 22.04 이상** (22.04, 24.04 등 - 그보다 오래된 버전은 지원 안 함)
- 인터넷 연결(ffmpeg 설치 시에만 필요, 그 외엔 필요 없음)

### 1. 설치

받으신 압축 파일(dms_labeling_ubuntu2204.tar.gz)을 원하는 위치에 풀어주세요.

1. 파일 관리자에서 압축 파일을 더블클릭 → 여기에 압축 풀기
   또는 터미널에서:
   ```bash
   tar xzf dms_labeling_ubuntu2204.tar.gz
   cd dms_labeling_dist
   ```

2. **아이콘과 함께 앱 목록/작업표시줄에 등록** (권장 - 이 단계를 해야 아이콘이
   보이고, ffmpeg도 자동으로 같이 설치됩니다. 안 하면 실행 파일을 매번 직접
   찾아 더블클릭해야 하고, 그마저도 기본 회색 아이콘으로만 뜹니다):

   반드시 압축을 푼 폴더(dms_labeling_dist) **안으로 들어가서** 실행해야
   합니다 - 스크립트가 지금 있는 폴더 기준으로 실행 파일을 찾기 때문에,
   다른 위치에서 실행하면 못 찾습니다.
   ```bash
   cd 압축_푼_폴더/dms_labeling_dist
   ./assets/install_linux.sh
   ```
   ffmpeg가 이미 설치돼 있지 않으면 중간에 관리자 비밀번호(본인 컴퓨터
   로그인 비밀번호)를 한 번 물어봅니다. 설치 완료! 라는 메시지가 뜨면
   성공입니다.

### 2. 실행

**방법 A - 앱 목록에서 실행** (권장, 1번의 2단계를 했을 경우)
- 화면 왼쪽 위/아래 프로그램 표시(모든 프로그램 보기) 버튼을 누르고 DMS
  또는 라벨링으로 검색
- 아이콘이 있는 항목을 클릭해서 실행 (작업표시줄에 고정도 가능)

**방법 B - 압축 푼 폴더에서 직접 실행** (2단계를 건너뛰었을 경우)
- 풀어놓은 폴더(dms_labeling_dist) 안의 dms_labeling 파일을 더블클릭
  (이 경우 기본 회색 아이콘으로 뜸)
- 더블클릭해도 안 열리면, 터미널에서:
  ```bash
  cd 압축_푼_폴더/dms_labeling_dist
  ./dms_labeling
  ```
- 이 방법은 install_linux.sh를 안 거치므로 ffmpeg 자동 설치도 안 됩니다 -
  필요하면 직접 sudo apt install ffmpeg를 실행해주세요.

### 문제가 생겼을 때 (리눅스)

| 증상 | 원인/해결 |
|---|---|
| 더블클릭해도 실행이 안 됨 | 터미널에서 chmod +x dms_labeling 한 번 실행 후 다시 시도 |
| 권한이 없습니다 오류 | 위와 동일 |
| 영상 저장(최종 저장)이 안 됨 | ffmpeg가 설치돼 있는지 확인 (ffmpeg -version으로 확인, 안 뜨면 sudo apt install ffmpeg 실행하거나 위 1번의 2단계를 다시 실행) |
| 화면이 아예 안 뜨고 조용히 종료됨 | 우분투 버전이 22.04보다 오래됐을 가능성 - 문의 바랍니다 |
| 라벨 항목(동사/명사 드롭박스)이 비어있음 | DMS_Actions.xlsx 파일이 dms_labeling 실행 파일과 같은 폴더에 있는지 확인 (압축을 풀 때 폴더 구조가 흐트러지지 않았다면 정상적으로 같이 있음) |
| 앱 목록에서 실행해도 아이콘이 안 보이고 회색 아이콘만 나옴 | ./assets/install_linux.sh를 dms_labeling_dist 폴더 **안에서** 실행했는지 확인 (다른 폴더에서 실행하면 실행 파일을 못 찾아서 등록이 제대로 안 됨) |

## 윈도우 사용자

### 준비물

- **윈도우 10 이상**
- 인터넷 연결(ffmpeg 설치 시에만 필요, 그 외엔 필요 없음)

### 1. 설치

받으신 압축 파일(dms_labeling_windows.zip)을 원하는 위치에 풀어주세요.

1. 압축 파일을 오른쪽 클릭 → 압축 풀기(또는 더블클릭 후 모두 추출).

2. **ffmpeg 설치** (터미널 - 명령 프롬프트 또는 PowerShell을 실행해서):
   ```powershell
   winget install ffmpeg
   ```
   winget이 안 되는(오래된) 윈도우라면, [ffmpeg 공식 사이트](https://ffmpeg.org/download.html)에서
   윈도우용 빌드를 내려받아 압축을 풀고, 그 안의 bin 폴더 경로를
   환경 변수 PATH에 추가해야 합니다.

3. 설치 후 처음 실행할 때 **Windows가 PC를 보호했습니다**라는 파란
   경고 창이 뜰 수 있습니다 — 정상입니다(서명되지 않은 프로그램이라
   기본적으로 뜨는 경고). 추가 정보 클릭 → 실행 버튼을 누르면 실행됩니다.

### 2. 실행

압축을 푼 폴더 안의 dms_labeling.exe 파일을 더블클릭합니다. 자주 쓸
경우, 파일을 오른쪽 클릭 → 시작 화면에 고정 또는 작업 표시줄에
고정을 해두면 다음부터 편합니다.

### 문제가 생겼을 때 (윈도우)

| 증상 | 원인/해결 |
|---|---|
| Windows가 PC를 보호했습니다 경고가 뜸 | 정상 - 추가 정보 → 실행 클릭 |
| 영상 저장(최종 저장)이 안 됨 / 재생이 안 됨 | ffmpeg가 설치돼 있는지 확인. 명령 프롬프트에서 ffmpeg -version 입력해서 버전이 나오는지 확인 |
| 라벨 항목(동사/명사 드롭박스)이 비어있음 | DMS_Actions.xlsx 파일이 dms_labeling.exe와 같은 폴더에 있는지 확인 |
| 더블클릭해도 아무 반응이 없음 | 명령 프롬프트를 열고 그 폴더로 이동해서 dms_labeling.exe를 직접 실행하면 에러 메시지가 보임 - 그 메시지를 캡처해서 문의 |

## 라벨링 시작하기 (공통)

1. 프로그램이 열리면 라벨링할 데이터가 들어있는 폴더(홈 디렉토리)를 선택합니다.
2. 세션/트라이얼을 고르고, 라벨링할 시나리오(distraction/drowsiness/cognitive)를
   선택하면 라벨링 화면으로 넘어갑니다.
