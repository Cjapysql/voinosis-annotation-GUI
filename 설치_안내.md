# DMS 라벨링 툴 설치/실행 안내

## 준비물

- **우분투 22.04 이상** (22.04, 24.04 등 - 그보다 오래된 버전은 지원 안 함)
- 인터넷 연결(ffmpeg 설치 시에만 필요, 그 외엔 필요 없음)

## 1. 설치

받으신 압축 파일(`dms_labeling_ubuntu2204.zip`)을 원하는 위치에 풀어주세요.

1. 파일 관리자에서 압축 파일을 더블클릭 → "여기에 압축 풀기"
   또는 터미널에서:
   ```bash
   unzip dms_labeling_ubuntu2204.zip
   cd dms_labeling_dist
   ```

2. 터미널을 열고 아래 명령을 딱 한 번만 입력해서 ffmpeg를 설치합니다(이미 설치돼
   있다면 안 해도 됩니다):
   ```bash
   sudo apt install ffmpeg
   ```
   (관리자 비밀번호를 물어보면 본인 컴퓨터 로그인 비밀번호를 입력하시면 됩니다.)

3. **아이콘과 함께 앱 목록/작업표시줄에 등록** (권장 - 이 단계를 해야 아이콘이
   보입니다. 안 하면 실행 파일을 매번 직접 찾아 더블클릭해야 하고, 그마저도
   기본 회색 아이콘으로만 뜹니다):

   반드시 압축을 푼 폴더(`dms_labeling_dist`) **안으로 들어가서** 실행해야
   합니다 - 스크립트가 "지금 있는 폴더 기준"으로 실행 파일을 찾기 때문에,
   다른 위치에서 실행하면 못 찾습니다.
   ```bash
   cd 압축_푼_폴더/dms_labeling_dist
   ./assets/install_linux.sh
   ```
   "설치 완료!"라는 메시지가 뜨면 성공입니다.

## 2. 실행

**방법 A - 앱 목록에서 실행** (권장, 1번의 3단계를 했을 경우)
- 화면 왼쪽 위/아래 "프로그램 표시(모든 프로그램 보기)" 버튼을 누르고 "DMS"
  또는 "라벨링"으로 검색
- 아이콘이 있는 항목을 클릭해서 실행 (작업표시줄에 고정도 가능)

**방법 B - 압축 푼 폴더에서 직접 실행** (3단계를 건너뛰었을 경우)
- 풀어놓은 폴더(`dms_labeling_dist`) 안의 `dms_labeling` 파일을 더블클릭
  (이 경우 기본 회색 아이콘으로 뜸)
- 더블클릭해도 안 열리면, 터미널에서:
  ```bash
  cd 압축_푼_폴더/dms_labeling_dist
  ./dms_labeling
  ```

## 3. 라벨링 시작하기

1. 프로그램이 열리면 라벨링할 데이터가 들어있는 폴더(홈 디렉토리)를 선택합니다.
2. 세션/트라이얼을 고르고, 라벨링할 시나리오(distraction/drowsiness/cognitive)를
   선택하면 라벨링 화면으로 넘어갑니다.

## 문제가 생겼을 때

| 증상 | 원인/해결 |
|---|---|
| 더블클릭해도 실행이 안 됨 | 터미널에서 `chmod +x dms_labeling` 한 번 실행 후 다시 시도 |
| "권한이 없습니다" 오류 | 위와 동일 |
| 영상 저장(최종 저장)이 안 됨 | ffmpeg가 설치돼 있는지 확인 (`ffmpeg -version`으로 확인, 안 뜨면 위 1번의 2단계 다시 실행) |
| 화면이 아예 안 뜨고 조용히 종료됨 | 우분투 버전이 22.04보다 오래됐을 가능성 - 문의 바랍니다 |
| 라벨 항목(동사/명사 드롭박스)이 비어있음 | `DMS_Actions.xlsx` 파일이 `dms_labeling` 실행 파일과 같은 폴더에 있는지 확인 (압축을 풀 때 폴더 구조가 흐트러지지 않았다면 정상적으로 같이 있음) |
| 앱 목록에서 실행해도 아이콘이 안 보이고 회색 아이콘만 나옴 | `./assets/install_linux.sh`를 `dms_labeling_dist` 폴더 **안에서** 실행했는지 확인 (다른 폴더에서 실행하면 실행 파일을 못 찾아서 등록이 제대로 안 됨) |
