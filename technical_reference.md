# DMS 라벨링 툴 — 구현/설계/이슈 통합 문서

## 0. 이 문서에 대하여

이 문서는 원래 따로 있던 `design_considerations.md`("왜 이렇게 설계했나")와
`session_issues_log.md`("어떤 버그를 찾아서 어떻게 고쳤나") 두 문서를 하나로
병합하고, 여기에 `docs/*.md`(파일별 구현 메커니즘 문서) 22개의 내용까지
종합해서, "어떻게 구현되어 있고, 왜 이렇게 설계했고, 어떤 버그를 찾아서
어떻게 고쳤는가"를 주제 하나마다 한 번에 볼 수 있도록 만든 단일 참조
문서다. 저 두 파일은 이 문서로 대체되어 삭제됐다.

**구성**:
- **1부**: 아키텍처 개요 (짧음 - 자세한 내용은 `README.md`/`CLAUDE.md`/
  `docs/module_map.html` 참고).
- **2부**: 설계 결정 + 구현 메커니즘 + 버그 히스토리를 주제별로 통합한
  본문. 옛 `design_considerations.md`의 17개 주제를 뼈대로 삼아 번호를
  그대로 유지하고, 그 주제와 관련된 옛 `session_issues_log.md`의 버그
  히스토리를 같은 절 안에 흡수했다. 대응하는 설계 주제가 없던 나머지
  이슈 절(UI 세부 버그, Git/배포 작업 이슈, 진단 스크립트 개발 이슈 등)은
  18번부터 이어서 추가했다 - 두 원본 문서의 내용은 전부 이 문서 안에
  그대로 보존되어 있다.
- **3부**: 파일별 구현 참조. `docs/*.md` 22개의 내용을 모듈 하나당 한
  절씩 모아서 재수록했다(2부의 주제별 서술과 겹치는 부분이 있지만, 2부는
  "왜/어떤 버그"를 중심으로 주제를 넘나들며 서술하고, 3부는 "파일 하나의
  전체 구현"을 빠짐없이 훑는 용도라 관점이 다르다). `docs/*.md` 개별
  파일은 계속 남아있고 이 문서와 별개로 파일 하나만 빠르게 찾아볼 때
  쓴다 - 자세한 위치 관계는 `docs/README.md` 참고.

이 문서 전체에 적용되는 서술 원칙: 평가적/주관적 표현("가장 중요한",
"핵심적인" 등)을 쓰지 않고 사실만 기술한다. 대화 인용은 실제 사용자
발화를 옮긴 것이고, 문맥 파악에 필요한 경우에만 남겼다.

## 1부. 아키텍처 개요

DMS(운전자 모니터링 시스템) 멀티센서 주행 녹화 데이터에 `distraction`(주의
분산), `drowsiness`(졸음), `cognitive`(인지) 세 시나리오로 라벨을 붙이는
PySide6 데스크톱 앱. 확정된 라벨 구간은 절대시간(unix time) 기준으로 모든
센서 파일(카메라 최대 7개, 오디오, IMU, watch, radar)에서 동시에
프레임/샘플 단위로 잘라 `session_XXX_id_XXX/{scenario}/{segment}/...`
구조로 저장한다.

```
labeling_tool/
  back/     # 데이터 레이어: 파싱 / 정렬 / 컷 / 저장 (Qt 의존성 없음)
  front/    # PySide6 UI: back의 클래스들을 가져다 화면과 상호작용에 연결
```

`back/`은 11개 파일(`models.py`, `session_loader.py`, `survey_parser.py`,
`timestamp_index.py`, `audio_stitcher.py`, `radar_index.py`, `coverage.py`,
`draft_store.py`, `segment_exporter.py`, `label_taxonomy.py`,
`video_codec.py`)로 구성되고, Qt를 전혀 참조하지 않는다. `front/`는 진입점
2개(`main.py`/`run_app.py`)와 10개 파일(`main_window.py`, `start_page.py`,
`labeling_page_loader.py`, `labeling_page.py`, `export_worker.py`,
`stream_player.py`, `playback_controller.py`, `widgets/timeline_widget.py`,
`widgets/video_panel.py`, `widgets/label_forms.py`)로 구성되고 `back`을
가져다 쓰지만 반대 방향은 없다. 파일 간 의존 관계 다이어그램과 추천 읽는
순서는 `docs/module_map.html`(정적 이미지: `.png`/`.jpg`)에 그려져 있다.

데이터가 흐르는 순서(개략): `session_loader.py`가 원본 폴더를 스캔 →
`survey_parser.py`가 survey json에서 자동 마커(`TaskWindow`)를 만듦 →
`timestamp_index.py`/`audio_stitcher.py`/`radar_index.py`가 센서별로
절대시각 ↔ 로컬 인덱스 변환을 담당 → 라벨링 중엔 `draft_store.py`에만
상태가 쌓임(아직 아무 것도 안 잘림) → "최종 저장" 시점에
`segment_exporter.py`가 실제로 모든 센서를 잘라 저장. `front/`는
`main_window.py`가 `start_page.py`(트라이얼 선택) →
`labeling_page_loader.py`(무거운 계산을 백그라운드로) →
`labeling_page.py`(실제 라벨링 화면, `stream_player.py`/
`playback_controller.py`/`widgets/*`를 엮음) → `export_worker.py`(최종
저장을 백그라운드로) 순으로 화면을 조립한다.

세 시나리오는 세그멘테이션 자유도가 다르다: `distraction`은 라벨러가
자유롭게 구간을 나누고, `drowsiness`/`cognitive`는 survey json에서
자동 계산된 구간을 기본값으로 쓰되 라벨러가 프레임 단위로 조정할 수
있다(자세한 내용은 2부 5번). 배포는 리눅스(Docker 기반 크로스버전 빌드)와
윈도우(GitHub Actions) 두 경로를 지원한다 - 자세한 내용은 `DEPLOY.md`.

## 2부. 설계 결정 + 구현 메커니즘 + 버그 히스토리

### 1. 시간 정렬 (Synchronization)

#### 설계 결정

모든 센서는 `t_sec`(유닉스 epoch 초) 하나의 공통 축 위에서 정렬한다.
리샘플링해서 공통 그리드를 만드는 게 아니라, 센서마다 독립적인 "절대시각
→ 로컬 인덱스" 변환 함수를 갖고, 라벨 구간(`start_ts`, `end_ts`) 하나를
그대로 각 센서에 넘겨서 각자 알아서 자기 좌표로 바꾸게 했다
(`back/timestamp_index.py`, `back/radar_index.py`).

비디오를 시간 정렬의 기준(reference)으로 삼는다. 카메라는 프레임마다
촘촘하게 실측 timestamp가 남지만, 오디오는 청크 단위라 sparse해서
세그먼트 경계 판단을 오디오 자체 데이터로 하면 엉뚱한 세그먼트를 짚는
사고가 실제로 발생했다(아래 버그 히스토리 참고). 그래서
`AudioTimestampIndex`는 가능하면 비디오가 계산한 `segment_starts`를
받아쓴다(정확한 우선순위 규칙은 4번 참고).

fps/샘플레이트를 메타데이터로 신뢰하지 않는다. `cv2.VideoCapture.get(CAP_PROP_FPS)`는
컨테이너에 박힌 명목값(예: 정확히 30.0)이라 실측과 다르다 - 실측
timestamp의 (첫~끝 실제 경과시간) ÷ (진짜 프레임 수)로 역산한 fps를 쓴다
(`CameraTimestampIndex._calibrate_fps()`). 이걸 무시하면 세그먼트 끝쪽에서
최대 4~5초까지 어긋난다.

정렬 기준 시각으로는 "저장 완료 시각"이 아니라 "센서가 실제 캡처한 시각"을
쓴다. timestamp csv엔 두 가지 시각이 다 있는데(`동기화기준시각_sec` vs
`저장완료시각_sec`), 후자는 디스크 저장 지연만큼 밀려 있어 다른 센서와
같은 축이 아니다.

float64 정밀도가 정렬에 충분한지 실측으로 검증했다. 이 프로젝트의 시각
값 크기대(2026년, 10자리 정수부)에서 float64 최소 표현 간격은 약
0.24마이크로초로, 필요한 최소 단위(오디오 샘플 간격 62.5μs, 비디오 프레임
간격 33ms)보다 훨씬 촘촘해서 문제없다. 실측 데이터로 Decimal 대조
검증(인접 프레임 오차 1.2피코초, 세그먼트 전체 오차 0.18나노초)까지
수행했다.

#### 발견된 버그와 수정

여러 라운드에 걸쳐 실측 데이터로 검증하며 다음 버그들을 발견하고 고쳤다.

- **`CameraTimestampIndex`가 "행 번호 카운팅" 방식이라 엉뚱한 세그먼트
  파일을 가리킴**: csv가 세그먼트 일부만 담고 있을 때 행 번호와 실제
  프레임 번호가 어긋났다. → 시간 산술 기반(`segment_starts` + fps)으로
  완전히 재작성해서 해결.
- **"csv 행 개수 = 그 세그먼트의 실제 프레임 수"라는 가정이 틀림**:
  타임스탬프를 남기는 로거와 실제 인코딩이 별개 파이프라인이라 스트림마다
  최대 수백 개까지 차이나서, 뒷부분 프레임이 도달 불가능해지는 문제가
  있었다. → 행 개수 세기를 아예 없애고 절대시각 기반 계산으로 전환해서
  해결.
- **fps를 cv2 명목값 그대로 써서 세그먼트 뒤쪽으로 갈수록 최대 4~5초
  드리프트**: → 실측 timestamp span ÷ 실제 프레임 수로 fps를 역산해서
  보정(`_calibrate_fps`)해서 해결.
- **오디오가 비디오와 완전히 다른 시간대의 소리로 재생됨**:
  `AudioTimestampIndex`가 자기 sparse csv만으로 독자적으로 세그먼트 시작
  시각을 추정하다가, 앞/뒤 세그먼트를 헷갈렸다(csv에 세그먼트 일부만
  남아있었는데 그걸 첫 번째로 오인). → 비디오가 계산한 `segment_starts`를
  오디오가 받아쓰는 구조(`reference_segment_starts`)로 변경해서 해결
  (이후 4번에서 다시 한 번 이 참조 방식 자체가 재설계됨).
- **정렬이 거의 다 맞았는데 "1초도 안 되는 아주 미세한 밀림"이 남아있음**:
  오디오 세그먼트의 실제 길이가 비디오가 선언한 세그먼트 길이보다 항상
  더 김(세그먼트당 최대 1.5초). → `audio_stitcher.py`에서 세그먼트별로
  "다음 세그먼트 시작 시각"까지만 쓰고 초과분은 잘라내는 방식으로 수정,
  byte 단위로 정확히 일치함을 상관계수/`np.array_equal`로 검증해서 해결.

### 2. 세그먼트 파일(녹화 중단/재개) 처리

#### 구현 방식과 설계 결정

녹화가 중간에 멈췄다 재개되면 기존 파일에 이어쓰지 않고 새 파일이
생긴다. 그래서 카메라/오디오 모두 하나의 논리적 스트림이 물리적으로 여러
파일(seg 번호로 구분)로 쪼개져 있을 수 있고, 번호가 연속이 아닐 수도
있다(001, 003, 006 등) - 정렬 순서만 신뢰하고 번호 값 자체는 신뢰하지
않는다.

재생용으로는 여러 오디오 세그먼트를 미리 하나의 연속 wav로
이어붙인다(`back/audio_stitcher.py`). `PlaybackController`가 파일 하나를
마스터 시계로 쓰는 구조라 "경과시간 = 파일 내 위치"라는 단순 선형식이
끝까지 유지돼야 하기 때문이다. 세그먼트 사이 무음 구간은 실제로 무음을
채워 넣고, 세그먼트 실제 길이가 비디오가 선언한 길이보다 길면(실측상 항상
그랬음, 세그먼트당 최대 1.5초) 초과분을 잘라낸다.

최종 export(자르기)는 재생용 이어붙이기와 별개로, 원본 세그먼트 파일에서
직접 잘라낸다(`back/segment_exporter.py`) - 재인코딩 방식(ffmpeg -ss
키프레임 탐색이 아니라 cv2로 프레임 단위 순차 디코딩)이라 정확도가 더
중요한 곳이기 때문이다.

#### 발견된 버그와 수정

- **세그먼트로 쪼개진 오디오 파일(`{mic}_seg001.wav`, `_seg002.wav`...)을
  지원 안 함**: → `AudioStreamFiles` 도입 + `back/audio_stitcher.py`로
  세그먼트를 하나의 연속 wav로 이어붙이는 방식 도입해서 해결.
- **마이크 이름 매칭이 잘못됨**: → 스캔 정규식 수정해서 해결.
- **초기 UI 렌더링 단계에서 오디오가 절대 시간 기준으로 동기화되어
  재생되는지 불확실, 재생이 안 되는 것처럼 보임**: → 위 두 수정과 10번
  절의 마스터 시계 재설계로 이어져 해결.

### 3. 원본 데이터 수집 파이프라인이 계속 바뀐다는 전제

#### 배경과 설계 결정

수집 파이프라인이 개발 도중 최소 두 번 바뀌었다: (1) 카메라/오디오/IMU/
watch 폴더 구조가 "seg 번호가 파일명에 붙는 flat 구조"에서 "seg 번호가
폴더명인 중첩 구조"로, (2) 카메라/오디오 timestamp csv의 컬럼 스키마가
영문 `frame_idx,t_sec,...`에서 한글 `저장영상프레임번호,동기화기준시각_sec,...`로.

`back/session_loader.py`(파일 탐색)는 신/구 폴더 구조를 자동 판별해서 둘
다 지원하도록 만들었다 - 이미 수집된 과거 트라이얼 데이터를 계속 쓸 수
있어야 하기 때문이다. 반면 `back/timestamp_index.py`(csv 컬럼 파싱)는
신규 스키마만 지원하도록 단순화했다 - 앞으로의 모든 신규 레코딩이 이
스키마로 고정되는 게 확정됐기 때문(오래된 트라이얼의 재처리 필요성보다
코드 단순성을 우선).

IMU/watch csv는 이 스키마 변경의 영향을 받지 않았다(`frame_idx,t_sec,...`
그대로) - 변경이 파이프라인 전체가 아니라 카메라/오디오 로거에만 있었던
것으로 보인다.

신규 스키마엔 "복제 프레임" 개념이 추가됐다(`복제프레임여부`,
`프레임종류`) - 카메라 드라이버가 다음 프레임을 제때 못 받으면(USB
대역폭 경합, CPU 스케줄링 지연, 디스크 I/O 백프레셔 등으로 추정) 직전
프레임을 복사해서 넣어 mp4의 fps를 일정하게 유지한다. 실측 간격(정상
간격의 정확히 2배)으로 "프레임 1개 주기만큼만 밀렸다 바로 복귀"하는
패턴임을 확인했다. 현재는 이 플래그를 무시하고 진행하기로 함 - 복제 행은
그냥 직전 행과 동일한 시각이 한 번 더 찍히는 것뿐이라 gap 감지나
시간→인덱스 계산에 영향이 없기 때문이다(46251개 중 426개, 약 0.9% 비율
관측).

#### 발견된 버그와 대응 경위

- **새 트라이얼(`id9276_trial3_...`) 2개에서 진단 스크립트 결과가
  비어있음**: 원인 확인 결과 카메라/오디오/IMU/watch 폴더 구조가 "seg
  번호가 파일명에 있는 flat 구조"에서 "seg 번호가 `seg{NNN}/` 하위
  폴더인 중첩 구조"로 완전히 바뀌어 있었다. 기존 스캔 정규식이 하나도 못
  찾아서 발생. → `back/session_loader.py`가 신/구 폴더 구조를 자동
  판별해서 둘 다 지원하도록 수정("둘 다 지원"을 명시적으로 선택 - 과거
  트라이얼도 계속 써야 하므로). 카메라 7스트림/오디오 2마이크/IMU/watch
  전부 신규 트라이얼 2개에서 정상 탐색됨을 검증. **[해결]**
- **카메라/오디오 timestamp csv의 컬럼 스키마도 완전히 바뀜**(IMU/watch는
  안 바뀜): `frame_idx,t_sec,...` → `저장영상프레임번호,...,동기화기준시각_sec,...,
  복제프레임여부,프레임종류`(오디오는 `저장오디오청크번호` + `누적바이트수`
  등 추가). 옛 컬럼명을 하드코딩하고 있던 `_load_timestamps()`에서
  `KeyError` 발생이 사전에 예상됨을 확인 → 처음엔 구/신 스키마 컬럼명을
  후보 목록으로 두고 자동 판별하는 방식을 제안했으나, "구 버전은 이제
  쓰일 일이 없다고 가정해"라는 방향 전환에 따라 신규 컬럼명을 직접
  하드코딩하는 단순한 방식으로 변경. 새 트라이얼 데이터로
  `CameraTimestampIndex`/`AudioTimestampIndex` 정상 동작 검증
  (`segment_starts`, fps 보정, `frame_at_time`, 샘플 구간 계산 전부
  확인). **[해결]**
- **`check_av_frames.py`의 오디오 시간 스팬 계산(`_csv_time_span`)도 옛
  컬럼명을 참조하고 있어 동일하게 수정 필요**: → 신규 컬럼명으로 수정해서
  해결.

### 4. 세그먼트별 timestamp csv 자기완결성 재설계 (3번 구조 변경 대응의 후속)

옛 `design_considerations.md` 11번 절 + `session_issues_log.md` 9번 절을
병합. 3번 항목에서 다룬 구조 변경 대응의 후속 작업이다.

#### 배경

"그럼 지금 자르면 어떤식으로 자른 지점이 저장되는 구조지?"라는 질문에서
시작해, t_sec(절대 unix 시각)만 draft/annotation에 영구 저장되고, 로컬
프레임/샘플 인덱스는 자르는 시점에 그때그때 다시 계산되는 휘발성 값이라는
원칙을 확인/설명했다. "근데 몇 번째 인지는 알아야 하지 않나? 결국은 자를
때는 몇 번째 프레임인지로 자른다는 거지?"라는 후속 질문에는, t_sec는
"진실의 근원"이고 실제 컷은 t_sec을 그 세그먼트 파일의 `segment_starts`+fps로
역산해 로컬 프레임 번호로 변환한 뒤 수행되며, 이 변환값 자체는 저장하지
않고 항상 새로 계산한다고(세그먼트 파일 구성이나 fps 보정값이 나중에
바뀌어도 안전하도록) 확인했다.

신규 수집 파이프라인은 세그먼트 하나하나가 자기 timestamp csv를 따로 갖고
있다는 게 확인됐다("세그먼트가 나뉘어도 타임스탬프는 올바르게 찍혀") -
기존 구조(csv 하나를 여러 세그먼트가 공유, gap 감지로 나눔)와 근본적으로
다른데, 3번 항목에서 반영한 코드는 이 케이스를 구분하지 못하는 상태였다.

#### 구현 방식과 설계 결정

`CameraStreamFiles`/`AudioStreamFiles.segment_files`를 `(seg_num,
media_path)` 2튜플에서 `(seg_num, media_path, timestamp_csv_path)`
3튜플로 확장, `back/session_loader.py`가 신규 구조 스캔 시 세그먼트
폴더별 csv를 개별적으로 찾아 매핑하도록 수정.

`back/timestamp_index.py`를 전면 재작성 - "csv를 공유하는 세그먼트가 몇
개인지"로 신/구조를 자동 판별하는 `_load_segment_t_secs()`를 도입(혼자
쓰는 csv는 통째로 신뢰하고 신규 구조로, 공유 csv는 기존 gap 감지 로직을
유지해 구조로 하위호환), `CameraTimestampIndex`가 더 이상 별도
`timestamp_csv` 인자를 받지 않고 `segment_files` 하나만으로 자기
완결적으로 계산하도록 생성자를 단순화했다.

오디오도 카메라와 같은 원칙(자기 데이터 우선, 부족한 세그먼트만 보조
참조)을 적용하도록 `AudioTimestampIndex`도 재작성했다.
`reference_segment_starts`가 "무조건 비디오 값을 그대로 받아쓰는" 방식(1번
절에서의 예전 해결책)에서 "자기 데이터로 못 채운 세그먼트만 메꾸는 보조
수단"으로 축소됐고, 즉시 계산이 아니라 콜러블로 지연 평가되도록 바꿔서
불필요한 무거운 계산도 함께 없앴다(12번 성능 문제와 직결). 이로써 과거
코드 리뷰에서 지적됐던 "`_export_audio`가 `reference_segment_starts`를 안
넘긴다"는 문제도 자연히 해소됐다(이제 항상 지연 콜러블로 넘김).

#### 발견된 버그와 수정

- **IMU/watch가 세그먼트 여러 개일 때 마지막 파일만 남고 앞쪽이 조용히
  사라지는 버그**: 카메라/오디오와 같은 근본 원인(스캔 시
  `dict[str, Path]`로 마지막 매치만 저장)이 IMU/watch 스캔 코드에도 그대로
  있었다. → `dict[str, list[Path]]`로 변경, `_export_imu`/`_export_watch`/
  `_filter_csv_by_time`도 리스트 전체를 순회해 시간 범위에 걸치는 행을
  모두 병합하도록 수정해서 해결.

### 5. 초안(Draft)과 최종 확정(Export)의 명확한 분리

라벨링 도중의 모든 상태(타임스탬프+라벨 쌍)는 `back/draft_store.py`의
`LabelDraft`에만 존재하고, 이 시점까진 아무 것도 실제로 잘리지 않는다.
자유롭게 수정/삭제 가능하다.

"최종 저장" 버튼을 눌러야만 `SegmentExporter`가 호출되어 실제로 모든
센서 파일을 프레임/샘플 정확도로 잘라 `session_XXX_id_XXX/{scenario}/
{segment}/` 구조로 저장한다 - 이 동작은 되돌릴 수 없는 것으로 취급한다.
이 원칙은 6번(세그멘테이션 자유도), 13번(세션 재개) 항목의 구현 전체를
관통한다.

### 6. 시나리오별로 다른 세그멘테이션 자유도

#### 설계 결정

`distraction`은 라벨러가 완전히 자유롭게 구간을 나눌 수 있다(하나의 task
window를 여러 서브세그먼트로 쪼갤 수 있음) - segment 번호도 시나리오별
독립 카운터.

`drowsiness`/`cognitive`는 survey json에서 자동 계산된 구간이
기본값(가이드라인)이지만, 라벨러가 프레임 단위로 정밀 조정할 수 있게
했다 - 자동 계산이 항상 프레임 단위로 정확하다고 보장할 수 없기
때문이다.

`drowsiness` 구간은 `driving_task.start_time - 60초 ~ start_time`으로
고정 계산된다 - 설문 응답(음성)이 그 앞뒤 오디오에 섞여 들어가지 않게
하려는 의도된 규칙.

`cognitive`는 시나리오 전체에 걸친 카운터 없이, `pre_nback1~3,
pre_cbt1~3, post_nback1~3, post_cbt1~3` 같은 고정된 이름을 쓴다 - task
종류/순서가 이미 설문 구조로 정해져 있기 때문이다.

#### 확정 경위

"시나리오마다 세그멘테이션 방식이 다르게 되어 있는 게 맞냐"는 질문에서
distraction(자유 분할) vs drowsiness/cognitive(고정 계산) 구조를
확인했다. **distraction 외 시나리오는 원래 수동 정밀 조정이 안 됐다** →
drowsiness/cognitive도 자동 계산 구간을 "가이드라인" 기본값으로 삼되
라벨러가 프레임 단위로 정밀 조정 가능하게 변경해서 해결. "cognitive_before_driving
json에 이미 타임스탬프가 있는데 활용 안 하냐"는 질문에는 활용 구조를
확인/반영했다.

### 7. 계층 분리 (back / front)

`back/`은 Qt를 전혀 참조하지 않는 순수 데이터 계층 - 다른 프론트엔드나
배치 처리 스크립트에서도 그대로 재사용 가능하게 하려는 의도다. `front/`는
`back`을 가져다 쓰지만 반대 방향(back → front)은 금지된다. 진단/검증용
스크립트(`check_av_frames.py` 등)도 `back`만 임포트해서 Qt 없이
headless로 동작한다.

### 8. 비개발자 배포

#### 설계 결정

라벨러가 파이썬 환경을 직접 구성하지 않아도 되도록 PyInstaller로 단일
실행 파일을 빌드한다(`dms_labeling.spec`, `build.sh`). 배포 대상과 같은
OS/아키텍처에서 빌드해야 하는 제약이 있어서, 여러 우분투 버전에 배포할
계획이면 Docker로 가장 오래된 대상 버전(우분투 22.04) 안에서 빌드하는
`build_linux_2204.sh`를 쓴다(glibc는 하위 호환이라, 오래된 버전에서
빌드한 결과물은 더 새 버전에서도 그대로 동작). 윈도우는 PyInstaller가
크로스 컴파일이 안 되므로 GitHub Actions의 윈도우 러너에서 빌드한다
(`.github/workflows/build-windows.yml`). 배포/설치 절차 전체는
`DEPLOY.md`(개발자용)와 `설치_안내.md`(라벨러용)에 정리돼 있다.

`DMS_Actions.xlsx`는 실행 파일 번들에 넣거나, 없으면 실행 파일 옆에
두면 자동으로 찾도록 해서 배포 유연성을 확보했다(`run_app.py._find_default_xlsx()`,
3부 "진입점" 참고). xlsx가 아예 없어도 "기타"(자유 텍스트) 라벨링은
가능하게 해서 완전히 막히지 않게 함.

#### 발견된 버그와 점검 이력

- **"그럼 이제 이걸 빌드한다고 하면?"이라는 질문**: 신규 모듈이
  `dms_labeling.spec`의 `hiddenimports`에 다 반영됐는지 점검 필요성 확인
  → `find back front -name "*.py"` 결과와 spec의 기존 목록을 직접
  대조해 누락 3건 발견: `back.audio_stitcher`, `front.export_worker`,
  `front.labeling_page_loader` → 모두 추가. 부수 확인: `requirements.txt`는
  새 PyPI 의존성이 없어 변경 불필요, `DMS_Actions.xlsx`는 spec의 기존
  `datas` 로직이 자동 포함해서 별도 수정 불필요, 신규 `QThread` 서브클래스는
  `PySide6.QtCore`가 이미 hiddenimports에 있어 별도 처리 불필요. **[해결]**
- **후속 발견**: 이 점검 이후 추가된 `back/coverage.py`가
  `hiddenimports` 목록에는 반영되지 않은 채 커밋되지 않고 남아있던 것을,
  `README.md`/`DEPLOY.md`/`CLAUDE.md` 문서 전면 정비 작업 중 spec 파일을
  다시 대조하다가 발견 → `back.coverage`를 목록에 추가하고
  `back/coverage.py` 자체도 커밋에 포함(그 전까지는 `front/`에서 이미
  참조되고 있었는데도 파일 자체가 git에 올라가 있지 않은 상태였음).
  **[해결]**
- **Git/배포 작업 중 발생한 문제 3건**(자세한 내용은 20번 참고):
  브랜치 체크아웃 시 작업트리가 옛 코드로 덮어써짐, git commit이 저장소
  로컬 한정 `user.name`/`user.email` 설정 필요, git push 자격증명 부재.
  **[모두 해결]**

### 9. 실측 데이터에 대한 방어적 처리 (확인 안 된 가정에 대해)

watch CSV 컬럼명(`timestamp` vs `t_sec`)이 실제 하드웨어로 확정 검증되지
않아서, `_filter_csv_by_time`이 두 후보를 다 시도한다 - 못 찾으면 예외를
던지는 대신 빈 파일로 조용히 넘어가게 했다(하드웨어 미확정 상태에서 전체
export가 죽는 것보다 나은 선택). 이 항목은 아직 실측 하드웨어로 검증되지
않은 채로 남아있다(4부 TODO 참고).

IMU 폴더는 대소문자 표기가 실측 데이터마다 다를 수 있어서(`IMU` vs
`imu`) 후보 이름을 순서대로 시도하는 `_resolve_dir` 헬퍼를 둔다
(`back/session_loader.py`, 실제로 이 방식을 쓰는 곳은 IMU 하나뿐).

### 10. 오프라인/헤드리스 환경에서의 검증

개발 환경에 디스플레이/오디오 장치가 없어서, 다음 방식을 조합해
검증한다:
- `QT_QPA_PLATFORM=offscreen`으로 `QApplication`/각 페이지가 예외 없이
  생성되는지 확인.
- `widget.grab()`으로 실제 렌더링 결과를 PNG로 저장해 레이아웃 확인. 이
  과정에서 `TimelineWidget`의 좌표 계산이 시간 범위를 벗어난 마커에서
  정수 오버플로우로 세그폴트를 일으키는 버그를 발견해 `_ts_to_x()`에
  안전 범위 clamp를 추가했다.
- PulseAudio `module-null-sink`로 가상 오디오 장치를 만들어
  `QMediaPlayer`가 실제 이벤트 루프를 2.5초 돌리며 `positionChanged`가
  시간에 따라 진짜로 움직이는지 확인(0→64→192→320ms 진행 확인) - 오디오
  동기화 메커니즘 자체의 실증.
- `back/`의 함수를 실제 trial 데이터(실측 timestamp csv, 실제 mp4/wav)에
  직접 호출해서 프레임 수/바이트/시각 값을 계산값과 비교. mp4
  재인코딩이 손실 압축이라 완전 바이트 동일 비교가 안 되는 경우(video)엔
  평균 픽셀 절대 차이(mean abs diff)로 "같은 내용의 압축 잡음"(관측상
  2~7 범위)과 "실제로 다른 내용"(관측상 26~30 범위)을 구분한다.

### 11. 재생 중 마스터 시계 설계

#### 설계 결정

여러 카메라 패널을 동시에 갱신해야 하는데, `QMediaPlayer.positionChanged`
신호만 믿고 화면을 갱신하면 실측 약 15Hz(64ms 간격)로만 신호가 와서
영상이 뚝뚝 끊긴다. 그래서 오디오 위치 변경 시점을 벽시계
(`time.monotonic()`)에 앵커링해두고, 별도의 33ms 타이머가 그 사이를
외삽해서 부드럽게 갱신하도록 분리했다(`PlaybackController._display_timer`,
`_on_display_tick`).

#### 발견된 버그와 수정

- **재생 중 영상 프레임레이트가 낮아 보임(끊김)**: `QMediaPlayer.positionChanged`가
  실측 약 15Hz로만 발생 → 오디오 위치를 벽시계에 앵커링해두고 별도 33ms
  타이머로 외삽하는 구조로 전환해서 해결.
- **seek 후 영상이 완전히 멈추는 회귀**: `seek_to()`가 오디오 플레이어
  상태에 따라 조건부로만 갱신되던 것을, 항상 동기적으로
  `_update_all_frames` 호출하도록 수정해서 해결.
- **재생 중 wall-clock 외삽이 실제 오디오 위치보다 앞서나가는 조기 정지
  버그**(코드 리뷰에서 지적됐던 항목): `_on_display_tick()`이
  `positionChanged`로 마지막 확인된 `_audio_anchor_ms`에 벽시계 경과
  시간을 더해 오디오 위치를 추정하고, 그 추정치만으로 "구간 미리보기
  한계"/"전체 끝" 도달 여부를 판단해 정지하고 있었다 - 오디오 백엔드가
  버퍼링 등으로 잠깐 멈칫하면 벽시계는 계속 흐르는데 실제 오디오는 못
  따라와서, 추정치가 먼저 한계에 도달해 실제로는 구간이 다 재생되기
  전에 정지해버릴 수 있는 구조적 위험이었다(정상 재생 중에도
  `positionChanged` 신호 간격(~64ms) 자체가 추정 오차 여지를 줌). →
  추정치가 한계에 도달해도 곧바로 정지하지 않고, 마지막으로 확인된 실제
  오디오 위치(`_audio_anchor_ms` 기준 `real_abs_ts`)가 아직 한계 미만이면
  화면만 한계값으로 고정해두고 정지를 보류, 다음 틱들에서 실제 위치가
  진짜 한계에 도달했을 때만 `pause()`를 호출하도록 변경해서 해결.

### 12. 무거운 계산은 백그라운드 스레드로, QWidget 조립은 메인 스레드로 분리

#### 배경

카메라 timestamp csv 파싱(세그먼트 하나에 수만~수십만 행)과 오디오
세그먼트 이어붙이기(수백 MB 파일 읽기/쓰기)가 실측 몇 초~수십 초씩
걸려서, 메인(UI) 스레드에서 동기적으로 실행하면 그동안 Qt 이벤트 루프가
멈춰 OS가 "응답 없음"을 띄우는 문제가 두 지점(최종 저장, 라벨링 화면
진입) 모두에서 실제로 발생했다.

#### 설계 결정

QWidget은 반드시 메인 스레드에서 만들어야 해서, "무거운 순수
계산"(`back/` 계층, Qt 미의존)과 "위젯 조립"(`front/` 계층)을
분리했다: `QThread` 워커(`ExportWorker`, `LabelingPageDataLoader`)가
계산만 먼저 끝내고, 그 결과(이미 계산된 카메라 인덱스, 오디오 스티칭
결과 등)를 받아서 메인 스레드가 위젯을 빠르게 조립한다.

export 워커는 세부 단계(카메라 스트림별/오디오/IMU 등)를 콜백으로
보고해서 진행률을 표시하고, `DraftStore.mark_committed()`는 백그라운드
스레드가 아니라 완료 신호를 받은 메인 스레드에서만 호출한다 -
draft_store 파일 I/O를 두 스레드가 동시에 건드리지 않게 하기 위함.

여러 시나리오 페이지가 `SegmentExporter` 인스턴스를 공유하는 구조라,
서로 다른 시나리오에서 동시에 "최종 저장"을 누르면 캐시/카운터 등 공유
가변 상태를 두 스레드가 동시에 건드리는 경쟁 상태가 생길 수 있다.
"최종 저장"을 누를 때마다 독립된 `SegmentExporter`를 새로 만들어서 공유
상태 자체를 없애는 방식으로 해결했다(13번 덕분에 매번 새로 만들어도
세그먼트 번호가 안 꼬임 - 자세한 경위는 13번 참고).

#### 발견된 버그와 수정

- **최종 저장(커밋) 시 실제로 저장이 안 되는 것처럼 보임**: 스크린샷으로
  제보, "저장이 아예 안된다고"를 반복하며 심각성 강조 → 프로파일링으로
  원인 특정: `_write_video_cut`이 자르려는 구간까지 프레임 0부터 순차
  디코딩(세그먼트당 최대 ~5700 프레임 낭비 × 스트림 7개)해서 스트림당
  수십 초, 전체 약 51초가 걸렸고, 그동안 UI가 완전히 멈춰 "응답 없음"이
  떠서 실제로는 끝까지 실행되고 있었는데도 "저장이 안 되는 버그"로
  보였던 것. 두 단계로 수정:
  - **1차 수정 (탐색 최적화)**: `cv2.VideoCapture.set(POS_FRAMES, N)`으로
    탐색한 뒤 실제 도달한 위치(`cap.get`)를 다시 확인해서, 요청한 지점
    이전(또는 정확히 그 지점)에 도달했으면 거기서부터 순차 디코딩으로
    마저 채우는 방식으로 바꿨다(빠름). 이 프로젝트가 쓰는 mp4v 코덱에서
    요청한 프레임에 정확히 도달하는 것을 픽셀 단위로 실측
    검증했다(`np.array_equal` == True). 혹시 다른 파일/코덱에서 탐색이
    요청 지점을 넘어서 버리면(정확도가 깨질 수 있는 상황) 안전하게 프레임
    0부터 순차 디코딩하는 기존 방식으로 자동 폴백하고, 그 사실을
    `export_warnings`에 기록해서 라벨러에게 알린다 - "빠른 길이 실제로
    정확하다고 확인될 때만 쓰고, 아니면 느려도 확실한 방법으로
    돌아간다"는 원칙. 폴백이 실제로 발생하면 최종 저장 완료 시
    `_finish_export_ui`가 `QMessageBox.warning`으로 그 사실을 라벨러에게
    보여준다. **[해결]**
  - **2차 수정 (지연 평가)**: 그럼에도 남아있던 지연 원인이
    `_export_audio`가 `_reference_camera_segment_starts()`를 매번
    즉시(eager) 호출해 최대 6개 카메라 스트림의 인덱스를 통째로 새로
    만들고 있었기 때문임을 확인 → 오디오 자신의 데이터로 이미 충분한
    경우(대부분)엔 이 계산 자체가 필요 없으므로, 콜러블로 감싸 지연
    평가(lazy)하도록 변경(4번 절과 동일 수정). **[해결]**
- **"저장 중 이라고는되는데 진행 상황 정도를 알 수 는 없나?"라는 요청**:
  → export를 메인 스레드에서 완전히 분리하기로 결정:
  `front/export_worker.py`의 `ExportWorker(QThread)` 신규 작성.
  `SegmentExporter.export_draft()`에 `on_progress` 콜백 인자를 추가해
  카메라 스트림별/오디오/IMU·워치·레이더 단계마다 진행 메시지를
  보고하도록 수정, `LabelingPage`가 "저장 중... (n/전체구간) 카메라
  내보내는 중: driver_rgb (3/7)" 같은 결합된 진행률 텍스트를 표시. export
  도중에는 draft 수정 관련 버튼/구간 선택 콤보를 잠가서 동시 조작으로
  인한 상태 꼬임도 방지. **[해결]**
- **"라벨링 시작"도 화면 전환 시 약 5초간 응답 없음이 뜨는, 동일 근본
  원인의 문제**: 직접 프로파일링으로 `LabelingPage(...)` 생성자 실행이
  5.30초 걸리는 것을 확인(카메라 인덱스 구성 + 오디오 스티칭이 원인). →
  `front/labeling_page_loader.py`의 `LabelingPageDataLoader(QThread)` 신규
  작성 - 카메라 인덱스 딕셔너리, 기본 마이크 선택, 오디오 스티칭 결과를
  백그라운드에서 미리 계산하고, `LabelingPage` 생성자는 이 미리 계산된
  결과를 필수 인자로 받아 위젯만 빠르게 조립하도록 시그니처를 변경.
  **[해결]**
  - 이 과정에서 **Qt 시그널로 (튜플 키 + 커스텀 객체 값을 가진) `dict`를
    넘기면 PySide6가 `_pythonToCppCopy: Cannot copy-convert ... (dict) to
    C++` 오류로 죽는 실제 크래시를 발견**했다 - 시그널 타입을
    `Signal(dict, object, object)`에서 `Signal(object, object, object)`로
    변경해 해결. 이 교훈은 이후 (튜플 키를 가진, 커스텀 객체를 값으로
    담은) `dict`를 스레드 간 Qt 시그널로 넘기는 모든 곳에 일반화해서
    적용됐다.
- **`QMediaPlayer.setSource()`도 비동기라, 미디어가 실제로 로드되기 전에
  `setPosition()`으로 seek을 걸면 조용히 무시될 수 있다는 것도 실측으로
  확인**: `mediaStatusChanged`로 로드 완료 시점을 감지해서 그때 목표
  위치를 다시 적용하는 방어 로직이 필요했다(자세한 경위는 18번 참고).

### 13. 세션을 이어서 작업하는 것(재시작/재개)을 원칙적으로 지원

#### 배경과 확정 경위

라벨링을 하다가 앱을 끄고 나중에 같은 트라이얼을 다시 열어 이어서 하는
게 실제 워크플로우로 확인됐다. "그럼 지금 이전에 진행되었던 흔적은
확인하나?"라는 질문에서 시작해 점검한 결과, `DraftStore`는 JSON으로
영구 저장되지만 `SegmentNamer`의 세그먼트 번호 카운터는 메모리
전용(`__init__`에서 항상 0부터 시작)이라 재시작 시 이전 폴더를 덮어쓸
위험이 있음을 확인했다. "라벨링 policy에 따르면 정합한가?"라는 질문에,
문서화된 규칙(시나리오별 카운터 독립성)이 재시작-지속성까지 보장하진
않지만 "데이터를 조용히 잃어버리면 안 된다"는 더 상위 원칙에 따라
수정이 필요하다고 설명하고 확정했다.

#### 구현 방식

- `SegmentNamer`가 생성될 때 `session_dir`을 스캔해서 이미 있는
  세그먼트 번호 중 최댓값부터 이어간다 - 이전엔 항상 0부터 시작해서,
  재시작하면 이전 결과(예: `distraction_segment001~003`)를 새 `001`이
  조용히 덮어쓸 위험이 있었다.
- `DraftStore`가 `committed=True`인 draft의 실제 출력 폴더(`segment_dir`,
  새로 추가된 필드)가 디스크에 여전히 있는지 확인해서 진행 상황/완료
  표시를 계산한다("진행하다가 프로그램을 끄고 라벨링 했던 거 중에
  삭제를 하고 다시 시작하면 어떻게 행동하지?"라는 엣지 케이스 질문에
  대응) - 라벨러가 결과물을 수동으로 지워도 "완료"로 계속 보이는 걸
  막기 위함. 이 필드가 생기기 전에 이미 커밋된 옛 기록은 확인할 방법이
  없으니 그대로 완료로 인정한다(하위호환).
- "작업 중인 구간들" 목록이 원래는 미완료 draft만 보여줬는데, 완료된
  것까지 전부(`[완료]` 표시) 보여주도록 바꿨다("한번 최종 커밋을 누르고
  나면 작업중인 구간 목록에서 직전까지 작업 했던게 사라지면 안되"라는
  요청) - 최종 저장을 눌러도 작업 이력이 화면에서 사라지지 않게 하기
  위함. 단, 완료된 항목은 수정/삭제를 막아서 "최종 저장 = 되돌릴 수 없는
  커밋" 원칙은 그대로 유지한다(정말 지우고 싶으면 앱이 아니라
  파일시스템에서 직접 폴더를 지우는, 의도적으로 번거로운 경로만
  남겨둠).

#### 동시성 - 매번 새 `SegmentExporter`를 만드는 이유

동시에 여러 시나리오에서 최종 저장을 눌러도 되는지에 대한 질문("그럼
지금 최종 커밋을 누르고 진행중일 때 다시 시나리오 선택이나 이런 다른
동작을 해도 되나?")에서 검토가 시작됐다. `SegmentExporter`가 세션당
하나로 공유되는 구조라, 서로 다른 시나리오 페이지에서 동시에 최종
저장을 누르면 `_camera_index_cache`/`export_warnings`/
`SegmentNamer._counters` 같은 공유 가변 상태를 두 스레드가 동시에
건드리는 실제 경쟁 상태 위험이 있음을 확인했다.

- 1차 제안("`is_exporting` 같은 잠금 플래그로 막자")은 거부됐다.
- 2차 제안("최종 커밋을 누를 때마다 `SegmentExporter`를 새로 만들자")으로
  확정, 단 "작업 중인 구간 이력이 사라지면 안 된다"는 요구를 동시에
  재확인했다.
- "아니면 최종 커밋 누를 때마다 새로 스레드를 만드는건?"이라는 대안이
  재문의됐으나, 이미 스레드(`ExportWorker`)는 매번 새로 만들고 있었고
  문제였던 건 그 안에서 쓰는 `SegmentExporter` 인스턴스 공유였음을
  재설명 → 결국 "최종 저장" 클릭마다
  `SegmentExporter(self.trial, self.exporter.session_dir)`를 새로
  생성해서 쓰는 방식으로 구현, `SegmentNamer`가 이미 디스크 스캔
  기반(위 항목)이라 매번 새 인스턴스를 만들어도 번호가 꼬이지 않음을
  근거로 안전성을 확보했다. **[해결]**

#### 그 외 발견된 버그

- **`annotations/` 폴더를 export 도중/이후 수동으로 지운 상태에서 다시
  저장 시도하면 에러**: 실제 스크린샷으로 `[Errno 2] No such file or
  directory` 확인, 단 카메라/오디오/IMU/워치/레이더/annotation.json 등
  실제 데이터 파일은 이미 다 정상적으로 써진 뒤였고 마지막 csv 기록
  단계에서만 실패하는 상황이었음 → `export_draft()`가 매 호출마다(최초
  1회가 아니라) `annotations_dir`와 시나리오별 csv+헤더 존재를
  재확인/재생성하도록 수정(자가 치유). **[해결]**

### 14. 비디오 프레임 탐색 최적화 - 정확도를 검증한 뒤에만 빠른 경로 사용

12번 항목의 1차 수정에서 이미 다룬 내용의 설계 원칙 요약: export 시
자르려는 구간이 세그먼트 파일 뒤쪽에 있으면(예: 14만 프레임짜리 파일의
5천 번째 프레임부터), 프레임 0부터 순차 디코딩하며 세는 기존 방식이
스트림 하나당 수십 초씩 걸려 전체 export가 1분 가까이 걸리고 OS가
"응답 없음"을 띄우는 원인이 됐다(51초 중 대부분이 이 부분이었음을
프로파일링으로 확인).

`cv2.VideoCapture.set(POS_FRAMES, N)`으로 탐색한 뒤 실제 도달한 위치
(`cap.get`)를 다시 확인해서, 요청한 지점 이전(또는 정확히 그 지점)에
도달했으면 거기서부터 순차 디코딩으로 마저 채우는 방식으로 바꿨다. 이
프로젝트가 쓰는 mp4v 코덱에서 요청한 프레임에 정확히 도달하는 것을
픽셀 단위로 실측 검증했다(`np.array_equal` == True). 혹시 다른
파일/코덱에서 탐색이 요청 지점을 넘어서 버리면(정확도가 깨질 수 있는
상황) 안전하게 프레임 0부터 순차 디코딩하는 기존 방식으로 자동
폴백하고, 그 사실을 `export_warnings`에 기록해서 라벨러에게 알린다 -
"빠른 길이 실제로 정확하다고 확인될 때만 쓰고, 아니면 느려도 확실한
방법으로 돌아간다"는 원칙.

이 seek+검증+폴백 로직은 17번 항목의 export 패딩 기능과 공존한다 -
패딩은 이 최적화가 처리하는 "실제 조각을 읽는" 부분과는 별도로 그
앞뒤에 추가된 것이라 서로 건드리지 않는다.

### 15. 물리적 센서 장착 특성 보정 (road 카메라 180도 회전)

#### 배경과 확정 경위

"그리고 전에 말했던 road 카메라 영상 저장, 보여줄때 모두 180도
회전시켜줘"라는 요청 - road 카메라가 실제로 거꾸로 장착돼 있어 화면/저장
결과 모두 뒤집혀 나오는 문제였다. "좌우 반전도? 음 카메라가 거꾸로
메달려 있던 건데 어떻게 해야하지?"라는 반문에는, 180도 회전은 좌우만
뒤집는 거울 반전이 아니라 상하좌우를 동시에 뒤집는 것이며, 광축을
기준으로 카메라 자체가 뒤집혀 매달린 상황(장면 전체가 위아래가 뒤집힌
것이지 좌우 대칭이 아님)에는 이게 수학적으로 정확한 보정임을 설명해서
확정했다.

#### 구현 방식

road 카메라(rgb+infrared)가 광축 기준 180도 돌아간 채 장착되어 있어서,
화면 표시(`front/stream_player.py`)와 최종 export(`back/segment_exporter.py`)
둘 다 `cv2.rotate(frame, cv2.ROTATE_180)`을 적용한다(상하좌우 동시 반전 -
좌우만 뒤집는 거울 반전과는 다름, `flip_180=True`를 road position
스트림에만 지정).

#### 검증

처음엔 `np.array_equal`로 정확도를 검증하려 했으나 mp4v 손실 압축 때문에
회전 여부와 무관하게 완전 일치가 나오지 않는 문제를 발견했다 → 평균
픽셀 차이(mean pixel diff) 비교로 검증 방법을 교정: 올바르게 회전한
프레임 vs 기대 프레임 = 평균 차이 약 3.9(압축 노이즈 수준), 회전 안
했다고 가정한 프레임 vs 기대 프레임 = 평균 차이 약 30.5(실제로 다른
화면) → 회전 로직이 올바름을 확인했다. **[해결]**

이 회전-전-원본-보관 원칙은 17번 항목의 export 패딩 기능 구현 중
재확인/재적용됐다(패딩용으로 프레임을 재사용할 때 회전 전 원본을
저장해야 하는 이유).

### 16. 재생 중 "구간만 미리보기" (자유 재생과 구분되는 임시 상태)

#### 배경과 확정 경위

"작업 중인 구간들" 목록의 항목을 누르면 그 구간을 재생하나"라는
질문에서 시작해, "클릭해서 구간으로 가면 그냥 쭉 재생이 되는게 아니라
그 짤린 구간에 대해서만 재생하게 해줘"로 요구사항이 확정됐다.

#### 구현 방식

`PlaybackController.set_playback_limit(end_ts)`를 신규 도입(도달 시 자동
정지), `_on_draft_item_clicked`가 구간 시작으로 seek + 타임라인 확대 +
limit 설정을 함께 수행한다. 이렇게 해서 "작업 중인 구간들" 목록에서
항목을 클릭하면 그 구간 시작으로 이동하고, 재생을 누르면 그 구간 끝에서
자동으로 멈추도록 해서, 완료/미완료 구간의 내용을 클릭 한 번으로 확인할
수 있으면서도 이어지는 다른 구간까지 그냥 계속 재생되지 않게 했다.

#### 한계 해제 지점 전수 점검

"그럼 지금 구간 미리보기 한계 해제가 되는 case는 어떻게 되지?"라는 자체
점검 요청에 응해 전수 조사한 결과, 타임라인 직접 클릭/`_load_task_window`
(다른 태스크 전환)에서는 해제되지만 시작/끝점 지정(`_on_mark_start`/
`_on_mark_end`), 가이드라인 리셋(`_on_reset_to_guideline`), 프레임 단위
이동(`_step_frame`), 영상 화면 클릭에서는 해제 로직이 빠져있음을
발견했다. 위 모든 진입점에 `set_playback_limit(None)` 호출을 추가하고,
`VideoPanel`에 `clicked` 시그널을 새로 만들어 영상 패널 클릭도 감지되게
했다. 타임라인 직접 클릭, 다른 태스크로 전환, 시작/끝점 지정, 프레임
이동, 영상 화면 클릭 등 "이제 자유롭게 다루겠다"는 신호가 되는 모든
조작에서 이 한계를 해제하도록 각 진입점을 전부 점검했다. **[해결]**

(이 과정에서 "다른 항목 클릭 시 그 구간의 끝으로 갱신 갱신 말고
해제로"라고 정정하려다 스스로 "이거는 무시해 헛소리했어"로 철회한
해프닝이 있었음 - 기존 동작 그대로 유지, 실제 반영된 변경 없음.)

### 17. export 시 센서 간 길이를 맞추는 패딩 - "정직하게 없는 대로 둔다" 원칙의 수정

#### 배경과 원칙 변경 경위

원래는(초기 설계) "라벨 구간이 어느 센서의 실제 녹화 범위와 부분적으로만
겹치면, 그 센서는 겹치는 만큼만 짧게 잘리고 나머지는 그냥 없는 대로
둔다"는 원칙이었다 - 없는 데이터를 지어내지 않는다는 취지. 하지만
실제로 카메라와 오디오/워치/IMU/레이더가 각자 독립적으로 세그먼트가
나뉘어 있다 보니(다른 센서보다 유독 자주 끊기는 센서가 있을 수 있음),
같은 라벨 구간을 잘라도 센서마다 실제 파일 길이가 서로 달라져서
다운스트림에서 여러 센서를 프레임/샘플 단위로 맞춰 쓰기 어렵다는 문제가
있었다.

"비디오와 오디오가 어느정도 겹치면 오디오나 비디오 저장할 때 패딩
처리하자 길이 맞추게 근데 보통 비디오 기준으로 자를테니까"라는 요청으로
시작됐다. "거꾸로 비디오가 없을 때도 있나?"라는 자체 점검 질문에는,
실제로 확인한 결과 카메라도 오디오와 똑같이 독립적인 세그먼트 구조라
카메라 쪽에만 공백이 생기는 것도 이론적으로 가능함을 확인했다(이 세션에서
다룬 트라이얼들은 우연히 항상 카메라가 더 온전했을 뿐). 패딩 범위를
어디까지 할지는 "일부라도 실제 데이터가 있을 때만 패딩해서 길이를
맞추고, 완전히 하나도 없으면 지금처럼 파일 자체를 안 만든다"로
확정됐다(전체 무음/공백 프레임만으로 된 파일을 새로 만들어내는 것까지는
안 하기로).

비디오 패딩 방식은 "그럼 지금 비디오가 짧으면 재생은 어떻게 되지?"라는
질문에 실제 코드(`CameraTimestampIndex.frame_at_time`)를 확인해 답변한
데서 결정됐다: 재생 중 카메라 공백에 들어가면 `None`(데이터 없음)이
아니라 공백의 중간 지점을 기준으로 더 가까운 쪽의 실제 경계
프레임(공백 앞쪽 절반이면 직전 프레임, 뒤쪽 절반이면 다음 프레임)으로
클램프된다는 걸 확인 → "지금처럼 가까운 쪽으로 정지"하는 방식을 export
패딩에도 그대로 적용하기로 확정했다(재생과 export의 공백 처리가
일관되게).

#### 구현 방식

라벨 구간 안에 실제 데이터가 하나라도 있는 센서는, 없는 부분(구간 시작
전/끝/중간 어디든)을 채워서 항상 `end_ts - start_ts` 길이 그대로
나오도록 바뀌었다. 완전히 하나도 없는 센서는(그 구간에 아예 녹화가
없었던 경우) 예전처럼 파일 자체를 안 만든다 - "일부라도 있으면
정직하게 채워서 맞추고, 아예 없으면 지어내지 않는다"로 원칙이 조정된
것.

- **`back/timestamp_index.py`의 `CameraTimestampIndex`/
  `AudioTimestampIndex.time_range_to_file_ranges()`**가 각 조각의 절대
  시작/끝 시각(+ 오디오는 샘플레이트, 비디오는 fps)도 같이 반환하도록
  확장(기존엔 로컬 인덱스만 반환해서 조각 사이 공백의 실제 길이를 계산할
  방법이 없었음). 튜플이 3원소(`path, local_start, local_end`)에서
  6원소(`path, local_start, local_end, 절대시작, 절대끝, fps/샘플레이트`)로
  확장. 호출부가 `back/segment_exporter.py` 안에 딱 한 곳씩(카메라/오디오
  각각)뿐이라 안전하게 변경할 수 있었다.
- **오디오**: 없는 구간을 실제 무음(`np.zeros`, 채널 수는 첫 조각을 미리
  열어서 확인)으로 채운다 - 재생용 연속 wav를 만드는
  `back/audio_stitcher.py`가 트라이얼 전체에 대해 이미 하던 방식을,
  라벨 구간 하나짜리 범위에 그대로 적용한 것.
- **비디오**: 무음에 해당하는 개념이 없어서, 재생 중 이미 하고 있던
  동작과 똑같이 공백 구간의 절반은 직전 실제 프레임을, 나머지 절반은
  다음 실제 프레임을 반복해서 채운다(`CameraTimestampIndex.frame_at_time()`이
  재생 시 쓰는 "더 가까운 쪽 프레임으로 클램프"와 동일한 원칙 - 새로
  발명한 규칙이 아니라 이미 재생에서 라벨러가 보고 있던 동작을 export
  결과물에도 그대로 반영한 것). 요청 구간의 맨 앞/뒤에 공백이 걸쳐서
  한쪽에만 실제 프레임이 있는 경우는 있는 쪽 프레임으로 전부 채운다.
  `_peek_frame()` 헬퍼(seek 후 한 프레임만 읽는 가벼운 함수)와
  `write_repeated()` 헬퍼(경계 프레임을 지정한 개수만큼 반복 기록)로
  구현했다. 기존 seek+검증+폴백 최적화(14번)는 실제 조각을 읽는 부분에
  그대로 유지, 패딩은 그 앞뒤에 별도로 추가된 것이라 서로 안 건드린다.

#### 발견/수정된 버그

구현 중 다음 실수를 발견해 수정했다: `flip_180`(road 카메라) 스트림에서
마지막 프레임을 기억해뒀다가 나중에 패딩용으로 반복 재사용할 때, 이미
회전된 프레임을 다시 회전시켜서(`write_repeated`가 내부적으로 또
회전함) 원상복구되는 버그가 있었다 - 회전 전 원본 프레임을 기억하도록
수정해서 해결(15번의 "회전 전 원본을 보관해야 한다"는 원칙이 재확인된
사례).

#### 검증

실제 트라이얼(`id002_trial1_20260812_145219_jang`, 카메라가 세그먼트 4개
경계마다 26~148초 진짜 공백이 있음)로 확인:
- **비디오**: seg4→seg5 사이 51.4초 공백을 포함한 65초 구간을 잘라서,
  공백 앞쪽 절반 프레임이 seg4 마지막 프레임과 픽셀 차이 ~1.9(압축
  노이즈 수준), seg5 첫 프레임과는 차이 ~26(뚜렷이 다름)임을 확인 -
  반대쪽도 대칭적으로 확인. 출력 프레임 수도 요청 길이(65.00s)에 정확히
  맞음(1951프레임 ≈ 65.03s).
- **오디오**: 같은 방식으로 자른 wav의 공백 구간 샘플이 정확히 `0.0`,
  경계 바로 안쪽은 실제 신호(0이 아님)임을 확인. 길이도 요청한 65.000s와
  정확히 일치.
- 공백이 없는 정상 케이스(기존 동작)도 회귀 없이 그대로 요청 길이만큼
  정확히 잘리는 것 확인, `export_draft()` 전체 파이프라인(IMU/워치/레이더/
  annotation.json 포함)도 정상 동작 확인. **[해결]**

### 18. 화면 전환/재생 타이밍 관련 연쇄 버그

design_considerations.md에는 대응하는 절이 없던, session_issues_log.md
11번 절의 내용. 12번 항목(백그라운드 스레드)과 시기적으로 이어지는
UI 타이밍 버그들이다.

- **"지금 어느 세션에 처음 들어왔을 때 task1으로 되어 있잖아 근데 실제
  위치는 왜 task1이 아니지? task1을 다시 선택해도 이동하지 않아"라는
  스크린샷 제보**: 두 가지 별개 원인을 확인했다.
  1. `QComboBox.currentIndexChanged`는 이미 선택된 항목을 다시 선택해도
     신호가 발생하지 않음(인덱스가 안 바뀌므로) - task2로 갔다가 task1로
     돌아오면 되는 것과 일치하는 증상. `window_combo`의 연결을
     `currentIndexChanged`에서 `activated`(사용자가 명시적으로 고를 때마다
     항상 발생)로 변경. **[해결]**
  2. "처음 들어와서 보면 task1을 가리키고 있는데 재생을 누르면 처음부터
     재생된다"는 후속 제보 → `PlaybackController.play()`가
     `QMediaPlayer.setPosition()` 직후 곧바로 `position()`을 다시 읽어
     앵커를 재설정하고 있었는데, `setPosition()`이 비동기라 아직 반영
     전(0)인 상태를 읽어버려 앵커가 0으로 덮어써지는 경쟁 상태였다. →
     `play()`에서 `position()` 재조회를 없애고, `seek_to()`가 이미
     설정한 `_audio_anchor_ms`를 그대로 신뢰하도록 수정. **[해결]**
  - 위 수정 이후에도 남아있던 증상 재확인 결과 원인이 하나 더 있었다:
    크고 이어붙인 wav 파일은 로딩이 끝나기 전에 건 `setPosition()`이
    조용히 무시될 수 있음(`QMediaPlayer`가 아직 `LoadedMedia` 상태가
    아니므로) → `_on_audio_media_status_changed`를 새로 추가해
    `LoadedMedia`/`BufferedMedia` 상태가 되는 시점에 앵커 위치를 다시
    적용하도록 함. **[해결]**
  - 근본적으로 "그럼 애초에 그냥 로드가 다 완료되고 나서 화면을 띄워
    주는게 어때?"라는 제안으로, 위 개별 땜질 대신 오디오가 실제로 재생
    준비된(`is_audio_ready`) 시점까지 화면 전환 자체를 미루는
    구조(`audio_ready` 시그널, `MainWindow._show_page_when_audio_ready`)로
    최종 정리됐다. **[해결]**
- **로딩 중 표시 UI를 넣었다가 롤백**: "그럼 로딩되는 동안 로딩 중
  이라고 띄워줄까?" → 실제 구현("distraction 오디오 로딩 중.") 후
  "안보이는데? 그리고 문구도 그냥 로딩 중 이라고만 하면되"라는 피드백으로
  문구를 단순화했으나, 최종적으로 "그러면 그냥 로딩중 띄우지 마 했던거
  다시 롤백해"로 텍스트 표시 자체를 철회했다. 단, 버튼 잠금(로딩 중
  시나리오 선택 버튼 비활성화)은 "그 버튼 잠금은 좋은거 같은데?"로 유지
  확정됐다. **[텍스트 표시는 롤백, 버튼 잠금은 유지]**
- **스페이스바가 포커스된 위젯에 따라 재생/정지 토글이 안 먹힐 때가
  있음**: → `LabelingPage._build_ui()` 끝에서 모든 자식 `QWidget`에
  `eventFilter`를 설치해, 어떤 위젯이 포커스를 갖고 있어도 스페이스바는
  항상 재생/정지를 토글하도록 수정. **[해결]**
- **타임라인에 태스크 구간 라벨 텍스트가 겹쳐서 안 보임**(구간이 촘촘할
  때): → `paintEvent`에서 라벨을 2행으로 교대 배치(`QFontMetrics`로 이전
  라벨의 끝 x좌표를 추적해 겹치면 다음 행으로). **[해결]**
- **타임라인을 확대(zoom)하면 재생헤드가 화면 밖으로 나가버림**: →
  `set_playhead()`가 재생헤드가 뷰 경계 근처(`PLAYHEAD_FOLLOW_MARGIN_RATIO=5%`)에
  오면 뷰 범위 자체를(폭은 유지한 채) 이동시켜 자동으로 따라가도록
  수정, 더불어 수동 이동을 위한 "◀ 이동"/"이동 ▶" 버튼과
  뷰-스크롤바 양방향 동기화(재진입 방지 플래그 포함)를 추가. **[해결]**
- **오디오 마이크 선택 관련 혼란**: 기본 오디오 스트림을 "데이터가 있는
  첫 번째 것"으로 자동 선택하던 방식이 라벨러 입장에서 어떤 마이크가
  재생되는지 알기 어려웠음 → 마이크 선택 콤보박스(`audio_combo`) 추가,
  기본값 "ecms1_audio", 전환 시 재생 중이던 위치를 유지한 채 소스만
  교체(재생 속도도 재적용). **[해결]**
- **재생 배속 조절 요청**: "배속에 0.25배, 0.5배, 0.75배 도 넣어줘" →
  `speed_combo`에 `["0.25x","0.5x","0.75x","1.0x","1.5x","2.0x"]` 추가,
  위치 점프 없이 배속만 재조정하도록 구현. **[해결]**
- **시나리오 화면에서 재생 중 뒤로 가기를 눌러도 오디오/영상이 계속
  재생됨**: "지금 하나의 시나리오에서 영상과 음성이 재생중일때 다시
  뒤로 돌아가서 시나리오 선택으로 돌아가도 기존에 재생중이던게
  정지되지 않아"라는 제보 → `MainWindow._on_back_from_labeling()`이
  화면 전환 전에 `page.playback.pause()`를 호출하도록 수정. **[해결]**
- **"작업 중인 구간들" 목록 클릭 시 재생 동작 설계**: "클릭해서 구간으로
  가면 그냥 쭉 재생이 되는게 아니라 그 짤린 구간에 대해서만 재생하게
  해줘"로 요구사항 확정 → `set_playback_limit(end_ts)` 도입, 자세한
  내용은 16번 참고. **[해결]**

### 19. UI 입력/포커스 문제, 파일 탐색 다이얼로그 및 앱 종료 문제

design_considerations.md에는 대응하는 절이 없던, session_issues_log.md
4번 + 13번 절의 내용.

- **프레임 단위 이동이 클릭 한 번씩만 되고 꾹 눌러도 연속 이동이 안
  됨**: → `setAutoRepeat(True)` + delay/interval 설정. **[해결]**
- **비디오 패널을 클릭해도 키보드 포커스(스페이스/방향키)가 안 잡힘**:
  → `setFocusPolicy(Qt.ClickFocus)` 추가. **[해결]**
- **"처음 runapp을 하고 찾아보기로 세션을 찾을 때 디렉토리가 현제
  labeling tool 디렉토리를 열게 되어 있나?"라는 질문**: 확인 결과
  특별히 지정 안 돼 있어 OS/Qt 기본값(마지막으로 연 위치 등)을 따르고
  있었다. "/home/을 기본으로" 요청 후 구현했으나 "왜 home 디렉토리
  선택이 출력되지?"라며 거부(툴 사용 거절)된 뒤, "아니 그냥 디폴트로
  찾아보기를 누르면 home에서부터 시작되게 해줘 그리고 문구도 '디렉토리
  선택'으로 바꿔줘"로 요구사항이 재정리됐다 → `StartPage._browse()`가
  `/home/`을 시작 경로로 여는 `QFileDialog`를 열도록 수정, 다이얼로그
  제목도 "디렉토리 선택"으로 변경. **[해결]**
- **파일 다이얼로그가 메인 창 뒤에 숨어서 다시 앞으로 가져올 수 없음 /
  다이얼로그가 열린 채로는 메인 창을 닫을 수 없음**: 원인은 블로킹 정적
  호출(`QFileDialog.getExistingDirectory`)과 네이티브 OS 다이얼로그의
  창 관리 방식이 얽힌 문제였다. 1차로 `DontUseNativeDialog` 옵션만
  추가했으나 부분적 해결에 그쳤음 → "메인 창을 닫으면 그냥 전체가
  끝나게 하고 싶은거야"로 진짜 원하는 동작이 명확화된 뒤, `_browse()`를
  `QFileDialog` 인스턴스를 직접 만들어 `setFileMode(Directory)` +
  `ShowDirsOnly`/`DontUseNativeDialog` 옵션 + `fileSelected` 시그널
  연결 + `.open()`(비블로킹) 호출로 재작성(다이얼로그 인스턴스를
  `self._browse_dialog`에 보관해 GC 방지), `MainWindow.closeEvent`에
  `QApplication.instance().quit()`을 추가해 다른 창이 열려 있어도 메인
  창을 닫으면 앱 전체가 확실히 종료되도록 함. **[해결]**

### 20. 도메인 로직 관련 문제 제기 (질문 → 설계 반영)

design_considerations.md에는 대응하는 절이 없던, session_issues_log.md
5번 절 중 위 1~17번 항목에 아직 반영되지 않은 나머지 내용.

- "cognitive_pre_cbt3 같은 경우는 타임스탬프가 있어서 맞아야 하는 거
  아니냐"는 문제 제기 → survey json 기반 자동 계산 로직 점검 계기가
  됐다.
- "모든 센서 프레임을 t_sec 축에 대해 정렬시켜야 한다"는 원칙 제기 →
  이후 모든 정렬 로직이 이 원칙(공통 t_sec 축, 센서별 독립 변환)을
  따르도록 설계됐다(1번 항목).
- 실제 센서 데이터 형태 설명(IMU가 accel/gyro로 나뉘고 데이터+타임스탬프가
  하나로 합쳐진 csv라는 점, radar/watch는 데이터 들어오면 재개하기로
  함) → 확인/반영.
- **drowsiness의 KSS 점수, cognitive의 난이도가 자유 입력 필드로 돼
  있었는데 "어차피 파싱이니까" 고정 표시(QLabel)로 바꿔달라는 요청**: →
  `kss_spin`(QSpinBox) → `kss_label`, `difficulty_field` →
  `difficulty_label`로 변경. **[해결]**
- **구간 겹침 검증, 데이터 없음 이유 안내, task window 안내, 진행 상황
  파악을 한 번에 처리해달라는 요청**: → `find_overlap`, `no_data_banner`,
  `progress_label`, `coverage_warning_label` 등 일괄 추가. **[해결]**

### 21. DMS_Actions.xlsx 라벨 taxonomy 채우기

design_considerations.md에는 대응하는 절이 없던, session_issues_log.md
15번 절의 내용.

- **드롭다운이 비어있는 문제의 근본 원인이 xlsx 파일 부재였음**: "지금
  원래 라벨링 페이지의 드롭박스를 채울 내용들을 비워 뒀는데 여기의
  문서에서 찾아"라며 PDF 스크린샷 여러 장(Table 2.1 Area/Verb/Noun/
  Abstract Noun 영문 taxonomy, Table 2.2, Fig 2.1, Table 2.3 실제 한국어
  지시문 표현, Table 2.4) 제공 → 두 표를 교차 참조해서(예: "왼쪽
  사이드미러를 조정하세요" → 동사 "조정하다", 명사 "왼쪽 사이드미러")
  6개 Area(좌우 창문/운전석/백미러 및 선바이저/중앙 콘솔 및 화면/조수석/
  뒷좌석)별 한국어 동사/명사 목록을 정리, `openpyxl`로
  `back/label_taxonomy.py`가 기대하는 형식(A열=Area명 첫 행만, B열=Verb,
  C열=Noun, D열=Abstract Noun 참고용)에 맞춰 프로젝트 루트에
  `DMS_Actions.xlsx` 신규 생성. `load_dms_actions()` 호출 및 실제
  `DistractionLabelForm` 드롭다운 채움까지 검증. **[해결]**
- **"그리고 -세부 는 뭐였지?"라는 질문**: → 원본 표에서 상위 명사
  항목의 하위 세분류를 나타내는 표기(예: 상위 항목 아래 딸린 세부
  옵션)임을 원문 대조로 확인/설명.

### 22. 진단 스크립트(`check_av_frames.py`) 개발 중 제기된 문제

design_considerations.md에는 대응하는 절이 없던, session_issues_log.md
7번 절의 내용.

- **Jupyter 커널이 죽음**: 시스템 파이썬(`/usr/bin/python3`)엔 pip조차
  없어서 ipykernel 재설치가 불가능한 상태였음 → 프로젝트 `.venv` 커널로
  전환해야 함을 확인(시스템 파이썬이 아니라).
- **"둘에서 왜 behavior_color seg001의 프레임 수가 다르게 읽히는지"**
  (노트북에서 직접 센 값과 스크립트 결과가 다름): → 원인 확인 후
  스크립트가 실제로 열어서 세는 방식으로 신뢰도를 향상시켰다.
- "콘솔에 출력하지 말고 txt 파일로 저장해달라"는 요청 →
  `check_av_frames.py`, 이후 만든 모든 진단 스크립트
  (`inspect_new_timestamp_schema.py`, `describe_files.py`)에 동일 패턴
  적용. **[해결, 이후 관례로 정착]**
- **"오디오도 실제 프레임 수/샘플 수를 보여줘, (헤더를) 신뢰하지
  말고"**: → wav 헤더 값과 별개로 `readframes`로 끝까지 실제로 읽어서
  진짜 샘플 수를 직접 카운트하는 로직 추가. **[해결]**
- **"오디오는 timestamp 수와 (실제 샘플 수가) 다를 수 있는 거 아니냐"는
  지적**: 오디오 csv 한 행은 raw 샘플 1개가 아니라 가변 길이 청크라서,
  행 개수를 샘플 수와 직접 비교하는 게 원래 의미 없었다. 사용자 지적이
  맞았음 → 행 개수 비교 대신 시간 스팬(초) 단위 비교(`_csv_time_span`)로
  수정. **[해결]**

### 23. 초기 UI 렌더링 문제

design_considerations.md에는 대응하는 절이 없던, session_issues_log.md
1번 절의 내용 - 세션 초반의 초기 확인 사항들.

- **RGB/IR 카메라 해상도가 다르게 출력됨, 화면 전체가 안 나옴**: →
  확인 및 설명.
- **라벨 폼 드롭박스가 클릭해도 비어있음**: → 원인 확인(21번 항목의
  xlsx 부재 문제로 이어짐).
- **오디오가 절대 시간 기준으로 동기화되어 재생되는지 불확실, 재생이
  안 되는 것처럼 보임**: → 2번 항목(오디오 재생 문제)으로 이어져 해결.

### 24. Git/배포 작업 중 발생한 문제

design_considerations.md에는 대응하는 절이 없던, session_issues_log.md
6번 절의 내용.

- **브랜치 체크아웃 시 작업트리가 그 브랜치의 옛날(버그 있는) 코드로
  덮어써짐**(backend 브랜치 체크아웃 시 `back/`, frontend 브랜치
  체크아웃 시 `front/`가 반복적으로 발생): → `git checkout main --
  back/`(또는 `front/`)으로 복구, 이후 브랜치 전환마다 `grep`으로 파일
  내용 재확인하는 습관으로 대응.
- **git commit이 "Author identity unknown"으로 실패**: → 전역이 아닌
  이 저장소 로컬 한정으로만 `user.name`/`user.email` 설정(사용자가
  명시적으로 이 방식을 선택). **[해결]**
- **git push가 자격증명 없음으로 실패**(gh CLI 미설치, 저장된 인증
  없음): → 사용자가 직접 터미널에서 push 실행. **[해결, 이후 반복적으로
  재발 - 이 세션(에이전트) 환경 자체에 자격증명이 저장되지 않는 구조라
  매번 사용자가 직접 push해야 함]**

## 3부. 파일별 구현 참조

각 소스 파일의 전체 구현을 빠짐없이 훑는 참조 절. 2부가 "주제 하나가
여러 파일에 걸쳐 어떻게/왜/어떤 버그로 이어졌는지"를 다뤘다면, 이
3부는 "파일 하나 안에 있는 모든 것"을 훑는다. 읽는 순서는 의존성 순
(back부터, `back/` 안에서는 최하위 계층부터, 그 다음 `front/`를 흐름
순으로) - `docs/module_map.html`의 추천 순서와 같다.

### `back/models.py`

라벨링 도메인의 기본 데이터 타입 정의. Qt에 의존하지 않는 순수
`@dataclass` 모음이라 `back/` 어디서든, 그리고 `front/`에서도 그대로
가져다 쓴다. 다른 `back/` 모듈을 하나도 import하지 않는 최하위 계층 -
의존성 그래프의 뿌리.

**`Scenario`(str Enum)**: `DISTRACTION`/`DROWSINESS`/`COGNITIVE`. `str`을
상속해서 `Scenario.DISTRACTION == "distraction"`이 참이 되고, 문자열
위치에 바로 써도(폴더명 조합, JSON 직렬화) 동작한다. 세 시나리오의
세그멘테이션 자유도 차이는 2부 6번 참고.

**`TaskWindow`/`DistractionTaskWindow`/`CognitiveTaskWindow`**: survey
json에서 자동으로 파싱되어 타임라인에 미리 그려지는 구간(라벨러가
만드는 게 아니라 `back/survey_parser.py`가 생성). 공통 필드:
`window_id`, `start_time`/`end_time`(**시간대 정보가 없는(naive)
datetime** - survey json의 ISO 문자열에 오프셋이 없어서 그대로 파싱한
결과), `start_ts`/`end_ts`(프로퍼티, `.timestamp()`) - **여기서 시간대
이슈가 발생한다**: naive datetime의 `.timestamp()`는 "지금 이 프로그램이
돌고 있는 시스템의 로컬 시간대"를 그대로 써서 epoch로 변환한다. 이
프로젝트는 지금까지 전부 KST로 설정된 머신에서만 실행돼서 우연히 문제가
없었지만, 다른 시간대의 머신에서 돌리면 이 값이 실제 센서 t_sec 축과
어긋난다(코드 어디에도 명시적인 `Asia/Seoul` 지정은 없음). `extra`는
원본 json 필드를 통째로 보존하는 dict - 최종 export 시 `annotation.json`의
`survey_extra`로 그대로 옮겨진다.

`DistractionTaskWindow`는 `distraction_task_id`, `distraction_task_text`,
`kss_score`/`kss_label`을 추가로 갖는다. `drowsiness_window` 프로퍼티가
`(self.start_time - timedelta(seconds=60), self.start_time)`을 반환한다 -
2부 6번의 drowsiness 60초 고정 규칙이 이 값 그대로다. `front/main_window.py`에서
drowsiness 시나리오의 `task_windows`는 별도로 파싱하지 않고 distraction과
같은 리스트를 재사용한다 - drowsiness 구간이 독립된 survey 섹션이 아니라
distraction task window에 내장된 서브구간이기 때문. `front/labeling_page.py`가
`isinstance(window, DistractionTaskWindow)`로 분기해서 시나리오에 따라
`drowsiness_window`를 쓸지 원래 구간을 쓸지 결정한다.

`CognitiveTaskWindow`는 `task_name`(`pre_nback1` 같은 고정 이름 - 최종
export 폴더명이 됨), `task_type`("nback"|"cbt"), `difficulty`, `phase`를
추가로 갖는다.

**`LabelDraft`**: 라벨러가 실제로 확정하기 전까지 임시로 들고 있는 작업
중 라벨(2부 5번 Draft/Export 분리 원칙의 타입). `draft_id`(uuid),
`start_ts`/`end_ts`(**여기는 `TaskWindow`와 달리 처음부터
float(unix epoch)** - `datetime`을 거치지 않고 `PlaybackController`가
다루는 절대시각을 그대로 저장, t_sec이 "진실의 근원"이라는 원칙이 이
필드에 반영됨), `source_window_id`, `label_fields`, `is_free_text_override`,
`committed`, `segment_dir`(최종 export된 실제 폴더 경로 -
`DraftStore.is_committed_and_present()`가 `committed`뿐 아니라
`Path(segment_dir).exists()`까지 확인해서 라벨러가 결과 폴더를 수동으로
지워도 "완료"로 잘못 남아있지 않게 함, 2부 13번).

### `back/session_loader.py`

원본 수집 데이터 폴더(`<home_dir>/bags/<trial_folder_name>/`)를
스캔해서, 센서별로 어떤 파일이 있는지 정리한 `TrialData`를 만든다. 파일
존재 여부와 이름 패턴만 다루고, 파일 안의 timestamp 값을 읽거나
해석하지는 않는다(그건 `timestamp_index.py`, `radar_index.py`,
`coverage.py`의 역할).

원본 폴더 구조:
```
camera/{modality}/seg{NNN}/{position}_{modality}.mp4
camera/{modality}/seg{NNN}/{position}_{modality}_timestamps.csv
audio/seg{NNN}/{mic}.wav
audio/seg{NNN}/{mic}_timestamps.csv (또는 {mic}_timestamp.csv, 둘 다 시도)
IMU/seg{NNN}/camera_accel.csv, camera_gyro.csv
watch/seg{NNN}/watch_*.csv
radar/radar_raw/seg{NNN}/  (이 파일이 아니라 RadarTimestampIndex가 직접 스캔)
```

`seg{NNN}` 폴더 하나마다 그 세그먼트만의 media 파일과 자기 timestamp
csv를 따로 갖는다(2부 4번). `position`/`modality` 원본 표기
(`front`/`behavior`/`road`, `color`/`infrared`/`depth`)는
`CAMERA_POSITION_ALIASES`/`CAMERA_MODALITY_ALIASES`로 각각
`driver`/`behavior`/`road`, `rgb`/`infrared`/`depth`로 정규화되어
저장된다 - 이후 코드 전체가 정규화된 이름만 다룬다. seg 번호는 연속이
아닐 수 있다(001, 003, 006 등) - 정렬 순서만 신뢰한다. 세그먼트
파일명에 mic/position/modality만 있고 seg 번호가 없기 때문에(`seg{NNN}/`
폴더명에만 seg 번호가 있음), 스캔 코드는 파일명이 아니라 상위 폴더명에서
seg 번호를 읽는다.

**데이터 클래스**: `CameraStreamFiles`(`position`/`modality` + `segment_files:
list[(seg_num, media_path, timestamp_csv_path|None)]`), `AudioStreamFiles`(같은
3튜플 형태), `TrialData`(`cameras: dict[(position,modality), CameraStreamFiles]`,
`audio: dict[mic_name, AudioStreamFiles]`, `imu: dict["accel"|"gyro",
list[Path]]`, `radar: dict["raw_dir", Path]`, `watch: dict[signal_name,
list[Path]]`, `survey_dir: Path`).

**`SessionLoader`**: `list_trials()`가 `home_dir/bags/` 아래 디렉토리
이름을 정렬해서 반환. `load_trial(trial_folder_name)`이 실제 스캔
진입점(`_scan_*` 메서드들로 카메라/오디오/IMU/레이더/워치를 채움).
`_resolve_dir(trial_dir, *name_candidates)`는 폴더 이름 대소문자 표기가
실측 데이터마다 다를 수 있어서(`IMU` 폴더가 대문자로 온 사례) 후보
이름을 순서대로 시도한다(2부 9번).

`_scan_cameras`/`_scan_camera_files`: 한 세그먼트 폴더 안에 여러
스트림(예: `front_color`, `behavior_color`, `road_color`)의 mp4+csv가
섞여 있다는 전제로, 먼저 그 폴더의 모든 `*_timestamps.csv`를
`(position, modality)` 키로 모아두고, 각 mp4를 처리할 때 자기 키에
해당하는 csv만 짝지어준다.

`_scan_audio`/`_scan_audio_files`: 같은 패턴으로 `audio/seg{NNN}/`을
스캔. timestamp csv 파일명이 `{mic}_timestamp.csv`(단수)와
`{mic}_timestamps.csv`(복수) 두 형태를 실측 데이터에서 다 시도한다.

`_scan_imu`/`_scan_watch`가 `dict[str, list[Path]]`인 이유: 값이
`Path` 하나가 아니라 `list[Path]`인 이유는 세그먼트가 여러 개면 같은
센서 이름의 csv가 세그먼트 개수만큼 생기기 때문이다(2부 4번의 버그
수정 경위).

`_scan_radar`: 다른 스캔 메서드와 달리 개별 파일을 찾지 않는다 -
`radar/radar_raw/` 디렉토리 경로 하나만 `TrialData.radar["raw_dir"]`에
기록해두고, 그 안을 실제로 읽는 일은 `back/radar_index.py`가 맡는다.

### `back/survey_parser.py`

트라이얼 폴더의 `survey/*.json`(실험 프로토콜 진행 중 자동 기록된 설문
응답)을 파싱해서 `TaskWindow` 리스트를 만든다.

survey json의 `section_name` 종류: `intro`(피험자 기본 정보, `TaskWindow`로
변환 안 됨), `cognitive_before_driving`(`pre_nback1~3`, `pre_cbt1~3`),
`cognitive_after_driving`(`post_nback1~3`, `post_cbt1~3`), `driving`
(distraction task window + drowsiness 서브구간 내장).

`__init__(survey_dir)`/`_load_all()`: `survey_dir` 아래 모든 `*.json`을
읽어서 `section_name` 필드 기준으로 모아둔다. 같은 `section_name`의
파일이 여러 개 있을 수 있다는 전제로 리스트에 담지만, 실제 파싱
메서드들은 `docs[0]`(정렬 순서상 가장 먼저 발견된 파일)만 사용한다 -
같은 섹션 파일이 여러 개면 첫 번째 이후는 읽히지 않는다.

`parse_cognitive_windows()`/`_parse_cognitive_section(section_name,
phase)`: `cognitive_before_driving`(`phase="pre"`)과
`cognitive_after_driving`(`phase="post"`)을 각각 파싱해서 합친다. 한
섹션 안에서는 `payload.cognitive_task_results` 리스트를 순서대로
순회하며, `task_type`("nback"|"cbt")별로 등장 순서대로 1부터 번호를
매겨 `task_name`을 `f"{phase}_{task_type}{번호}"`로 조합한다. 이 함수
자체엔 "task_type당 3개까지만" 같은 검증/제한이 없다 - 실제로 한
트라이얼의 `cognitive_after_driving` json에 `cognitive_before_driving`의
결과가 그대로 중복 포함되어 12개(정상은 6개)가 들어있는 수집 파이프라인
버그가 있었음을 확인했다(코드가 아니라 해당 트라이얼의 survey json
파일 자체를 수정해서 해결, 파서 코드는 그대로 둠).

`parse_distraction_windows()`: `driving` 섹션의
`payload.driving_task_results` 리스트를 순서대로 순회하며 `window_id`를
`f"driving_task_{i}"`(1부터)로 부여한다.

`parse_all()`: `{"cognitive": [...], "distraction": [...]}` 반환.
`drowsiness`용 window는 따로 없다 - `front/main_window.py`가
`parsed["distraction"]`을 `Scenario.DROWSINESS` 키에도 재사용한다.

`_parse_dt(s)`: `datetime.fromisoformat(s)` - 결과 datetime이 naive가
되는 이유는 위 `back/models.py` 절 참고.

### `back/timestamp_index.py`

절대시각(t_sec)과 센서 파일 안의 로컬 프레임/샘플 위치를 서로 변환하는
모듈. `CameraTimestampIndex`(영상)와 `AudioTimestampIndex`(오디오) 두
클래스로 구성된다.

**컬럼 스키마**: 카메라는 `저장영상프레임번호`(정렬 순번),
`동기화기준시각_sec`(정렬 기준 시각). 오디오는 `저장오디오청크번호`,
`동기화기준시각_sec`. 정렬 기준 시각으로 `저장완료시각_sec`이 아니라
`동기화기준시각_sec`을 쓰는 이유는 2부 1번 참고. `복제프레임여부`
컬럼은 무시(2부 3번).

**fps를 실측 기반으로 보정하는 이유**: 2부 1번 참고.
`CameraTimestampIndex._calibrate_fps()`가 담당, 실측 timestamp가 없는
세그먼트는 cv2가 보고하는 명목 fps를 최후 폴백으로 쓴다.

**세그먼트 시작 시각을 못 구한 세그먼트 채우기(`_fill_segment_starts`)**:
csv가 없거나 비어있는 세그먼트는 이웃 세그먼트의 시작 시각 + 자기 파일
길이(`durations`)로 앞/뒤 방향 모두 이어붙여서 역산한다. 이웃도 실측이
없어서 못 채우는 자리는 `None`으로 남고, 하나라도 `None`이 남으면 그
스트림 전체를 빈 `segment_starts = []`로 처리한다.

**`CameraTimestampIndex` 생성자 순서**: (1) `_load_segment_t_secs()` -
세그먼트별 t_sec 리스트(2부 4번의 신/구조 자동 판별 로직), (2)
`_probe_segment_info()` - 각 세그먼트 파일을 열어 프레임 수/명목 fps를
직접 읽음, (3) `_calibrate_fps()`, (4) `_compute_segment_starts()` -
`self.segment_starts`(공개 속성, 다른 센서가 참조로 받아쓸 수 있음).

메서드: `first_t_sec`/`last_t_sec`(프로퍼티, 스트림 전체 최이른/최늦은
시각). `_segment_bounds(i)`. `frame_at_time(t)` - 절대시각 t에 가장
가까운 (파일 경로, 로컬 프레임 인덱스) 반환, 범위 밖이면 가장 가까운
세그먼트의 처음/끝 프레임으로 클램프(`None` 반환 안 함) - 2부 17번
export 패딩의 모델이 된 재생 중 동작. `time_range_to_file_ranges(t_start,
t_end)` - 절대시간 구간을 `[(파일경로, local_start, local_end, 절대시작,
절대끝, fps), ...]`로 변환. `frame_at_time`과 달리 범위 밖이면 그
세그먼트를 결과에서 제외한다(클램프하지 않음). 절대시작/절대끝/fps는
2부 17번(export 패딩)을 위해 추가된 필드.

**`AudioTimestampIndex`**: 카메라와 컬럼 스키마는 비슷하지만, csv 한
행이 raw 샘플 1개가 아니라 청크(가변 길이) 이벤트라서 1:1 대응시킬 수
없다. 세그먼트 시작 절대시각만 구한 뒤, 그 안에서는 샘플 레이트가
일정하다는 가정으로 선형 계산한다. 생성자의 `reference_segment_starts`
파라미터는 2부 4번 참고(자기 데이터로 못 채운 세그먼트만 보조로
채우는 용도, `list[float]` 또는 `() -> list[float]|None` 콜러블 둘 다
받음 - 콜러블 지연 평가의 성능상 이유는 2부 12번). `time_range_to_file_ranges`는
카메라와 같은 원칙, 반환 단위만 로컬 샘플 인덱스.

### `back/audio_stitcher.py`

세그먼트로 쪼개진 wav 파일 여러 개를, 세그먼트 사이의 무음(gap)까지
실제 무음 데이터로 채워서 하나의 연속된 wav로 합친다. 재생 전용 - 최종
저장(export)은 이 결과물을 쓰지 않고 원본 세그먼트 파일을 직접 다시
연다(`back/segment_exporter.py`). 왜 필요한지, 세그먼트 경계 처리
방식(공백 무음 채움 + 다음 세그먼트 시작 시각까지만 자름)은 2부 2번
참고.

`build_continuous_audio(audio, cache_dir, reference_segment_starts=None)`
반환값: `(이어붙인 wav 경로, 그 wav 0초 지점의 절대 unix time)` 또는
세그먼트가 없으면 `None`. **캐시 확인**: 출력 파일(`{mic_name}_stitched.wav`)이
이미 있고 그 mtime이 모든 원본 세그먼트 파일의 mtime보다 최신이면 다시
만들지 않고 그대로 반환한다(원본 세그먼트 파일 자체는 안 바뀌므로,
한 번 만든 뒤로는 대부분 이 캐시가 히트함).

호출 시점: 페이지 로드 시(`LabelingPageDataLoader.run()`, 백그라운드
스레드) 기본 마이크에 대해 1회, 마이크 전환 시(`_on_audio_stream_changed()`)
메인 스레드에서 동기 호출(이미 캐시된 wav가 있으면 바로 반환되므로
빠름).

### `back/radar_index.py`

`radar/radar_raw/seg{NNN}/` 폴더들을 스캔해서 프레임 단위 절대시각
인덱스를 만든다. `back/session_loader.py`가 개별 파일 목록을 미리
만들어주지 않고 디렉토리 경로만 넘겨주므로, 이 모듈이 폴더 스캔부터
직접 한다.

원본 파일 구조(세그먼트 하나): `radar_data_raw_int16_<timestamp>.bin`
(int16 원시 IQ 데이터), `radar_frame_index_<timestamp>.csv`(frame_idx,
ros_time_sec, kst_time, offset_int16_before, num_int16,
shape_chirps_rx_adc, frame_time_s, sample_rate_ksps), `used_cfg.cfg`
(mmWave 센서 설정 스냅샷, TI SDK 03.05, xWR68xx), `used_cfg_sha256.txt`,
`radar_capture_summary_<timestamp>.json`. 프레임의 바이트 위치는
`offset_int16_before`(int16 단위) × 2 = byte offset, 길이는
`num_int16` × 2 bytes. 세그먼트 폴더마다 자기 `used_cfg.cfg` 스냅샷을
따로 가지므로, 라벨 구간이 서로 다른 cfg를 쓴 두 세그먼트에 걸치면
두 cfg를 모두 보존해서 내보낸다.

`RadarFrame`(dataclass): `bin_path`, `local_frame_idx`, `t_sec`,
`offset_int16_before`, `num_int16`, `cfg_path`, `cfg_sha256`, `shape`.

`RadarTimestampIndex.__init__(radar_raw_dir)`/`_load()`: `seg`로 시작하는
디렉토리를 이름순으로 정렬해서 각각 스캔, csv를 한 행씩 읽어
`RadarFrame`을 만들어 `self.frames`에 추가(프레임 개수가 카메라보다
훨씬 적어서 전부 메모리에 올리는 방식). 세그먼트 폴더 순서가 곧
시간순이라고 가정하지만, 혹시 어긋나는 경우를 대비해 전체 스캔이
끝난 뒤 `self.frames.sort(key=lambda fr: fr.t_sec)`로 절대시각 기준
재정렬한다(4부 TODO의 성능 관련 항목과 연결).

`time_range_to_frames(t_start, t_end) -> list[RadarFrame]`: `bisect`로
이분 탐색해 범위의 `RadarFrame`들을 슬라이스로 반환. 카메라/오디오의
`time_range_to_file_ranges`가 (파일 경로, local start, local end)
튜플을 반환하는 것과 달리, 여기는 프레임 객체 자체를 반환한다.

### `back/coverage.py`

센서별 대략적인 커버리지(세그먼트마다 첫 시각 ~ 마지막 시각)를 가볍게
조회하는 모듈. `CameraTimestampIndex`/`AudioTimestampIndex`/
`RadarTimestampIndex`처럼 프레임/샘플 단위로 정확하게 인덱싱하지 않고,
각 csv의 첫 행과 마지막 행 시각만 읽는다.

왜 필요했는지: "이 구간에 데이터가 없습니다" 경고 배너
(`coverage_warning_label`)가 원래 카메라만 체크하고 있어서, 오디오/IMU/
워치/레이더가 빠진 구간을 라벨러가 화면상으로 알아챌 방법이 없었다.
카메라는 이미 `CameraTimestampIndex`로 프레임 단위 정확한 인덱스가
있지만, 이걸 다른 센서에도 똑같이 적용하면 매번 무거운 파싱이 필요해서
경고 배너처럼 정확도보다 속도가 중요한 곳에는 안 맞았다.

세그먼트를 하나로 뭉치면 안 되는 이유: 센서 하나의 전체 커버리지를
"첫 세그먼트 시작 ~ 마지막 세그먼트 끝" 하나의 구간으로 뭉쳐서
표현하면, 세그먼트 사이에 실제로 녹화가 없던 공백까지 "커버됨"으로
오인하게 된다(실제로 겪은 사례: 오디오가 세그먼트 사이에 약 1시간 공백이
있었는데, 하나로 뭉친 범위로 보면 그 공백이 안 보임). 그래서
`compute_sensor_coverage()`는 센서 하나당 세그먼트별 (시작, 끝) 쌍의
리스트를 반환하고, 특정 구간에 데이터가 있는지는 `range_overlaps_any()`로
그 리스트의 세그먼트들과 개별적으로 겹치는지 확인한다.

`_csv_first_last_t_sec(csv_path, t_col_candidates)`: 헤더에서
`_SENSOR_T_COLS = ("동기화기준시각_sec", "t_sec", "ros_time_sec",
"timestamp")` 중 실제로 있는 컬럼을 찾아 첫/마지막 데이터 행의 값을
반환. `compute_sensor_coverage(trial)`: `TrialData`를 받아
`{표시용 이름: [(세그먼트1 시작,끝), ...]}` 딕셔너리를 만든다(카메라는
포함 안 함 - 호출부가 이미 정확한 `CameraTimestampIndex`를 가지고
있음). `range_overlaps_any(start_ts, end_ts, segments)`.

세그먼트 경계를 gap 감지 없이 그대로 신뢰하는 이유: 세그먼트 폴더
구조(`seg{NNN}/`)가 이미 정확한 세그먼트 경계이므로, `back/timestamp_index.py`의
gap 감지 로직(`_split_by_gap`)을 쓰지 않는다.

### `back/draft_store.py`

라벨 draft(확정 전 작업 중 상태)를 트라이얼별 JSON 파일 하나에
저장/불러오는 저장소(2부 5번). draft 파일 경로는 원본 raw 데이터 폴더
바깥, `<home_dir>/.labeling_drafts/{trial_folder_name}.json`이다.

`__init__(draft_path)`/`_load()`/`save()`: 생성 시 파일이 있으면 즉시
로드. `save()`는 매번 전체 재작성(부분 업데이트 아님) - 모든 변경
메서드 끝에 `self.save()`를 호출해서 상태 변화가 항상 즉시 디스크에
반영된다.

draft 조회 메서드 3종: `drafts_for_scenario`(미완료만),
`all_drafts_for_scenario`(전부, "작업 중인 구간들" 목록 표시용 - 2부
13번), `committed_window_ids`(완료 + 디스크 존재 확인까지 거친 것들의
`source_window_id` 집합).

`is_committed_and_present(draft)`: "완료"로 볼지 판단하는 유일한 기준
메서드. `committed=True`이고 `segment_dir`이 있으면
`Path(segment_dir).exists()`까지 확인. 세 곳(진행 상황 배너, `[완료]`
표시, 수정/삭제 차단)이 전부 이 메서드를 거치도록 통일됐다(2부 13번의
경위).

`find_overlap(scenario, start_ts, end_ts, exclude_draft_id=None)`: 같은
시나리오의 미완료 draft들 중 겹치는 첫 번째 draft를 찾는다. 커밋된
draft는 검사 대상에서 제외(커밋된 구간은 어차피 수정 불가).

`mark_committed(draft_id, segment_dir=None)`: `SegmentExporter.export_draft()`가
성공적으로 끝난 뒤에만, 메인 스레드에서 호출된다(2부 12번).

### `back/segment_exporter.py`

확정된 `LabelDraft`를 받아서 모든 센서 데이터를 절대시각 기준으로
프레임/샘플 정확도로 잘라 `session_XXX_id_XXX/{scenario}/{segment_name}/`
구조로 저장하고, `annotation.json` + `annotations/{scenario}.csv`를
남긴다. Draft/Export 분리 원칙(2부 5번)에서 "Export"에 해당하는 유일한
진입점.

**`SegmentNamer`**: 시나리오별 독립 카운터로 세그먼트 폴더 이름을
만든다. `distraction`/`drowsiness`는 `{scenario}_segment{NNN:03d}`,
`cognitive`는 카운터 없이 `cognitive_task_name`을 그대로 폴더명으로
쓴다. `__init__(session_dir)`이 디스크를 스캔해서 최댓값부터 카운터를
이어간다(2부 13번의 재개 지원).

**`SegmentExporter.__init__(trial, session_dir)`**: `SegmentNamer`,
`annotations/{scenario}.csv` 3개 사전 생성, `_camera_index_cache`/
`_audio_index_cache`/`_radar_index`(인스턴스당 1회 계산 캐시),
`export_warnings`(seek 폴백 발생 시 기록 - 2부 14번).

**`export_draft(draft, cognitive_task_name=None, source_window=None,
on_progress=None)`**: 세그먼트 이름/경로 결정 → `_export_cameras` →
`_export_audio` → `_export_imu`/`_export_watch` → `_export_radar` 순서로
자름(각 단계 전 `on_progress` 콜백) → `annotation.json` 저장
(`source_window.extra`까지 `survey_extra`로 보존) →
`annotations/{scenario}.csv`에 한 줄 추가(없으면 매 호출마다
재생성 - 2부 13번의 자가 치유). `on_progress`는 평범한 콜러블(Qt
시그널 아님) - `front/export_worker.py`가 Qt 시그널로 다시 감싼다.

**카메라 export**: `_export_cameras`가 모든 카메라 스트림을 순회,
`time_range_to_file_ranges()`로 파일 범위를 구해 `_write_video_cut`에
넘김(비어있으면 파일 자체를 안 만듦). `road` 스트림은 `flip_180=True`
(2부 15번). `_write_video_cut`: seek+검증+폴백 최적화는 2부 14번, 공백
채움(패딩)은 2부 17번 - `_peek_frame()`/`write_repeated()` 헬퍼로 구현,
회전 전 원본 프레임을 보관해야 하는 이유도 2부 17번/15번.

**오디오 export**: `_get_audio_index`가 마이크 이름별로
`AudioTimestampIndex` 캐시. `_reference_camera_segment_starts(expected_count)`가
`_CAMERA_REFERENCE_PRIORITY`(`driver_rgb` 최우선) 순서로 기준 카메라
스트림을 고른다 - 카메라마다 fps 실측 오차가 미세하게 달라서 매번 다른
스트림을 기준으로 삼으면 세그먼트 판단이 흔들릴 수 있기 때문(이 우선순위
목록은 `front/labeling_page.py`, `front/labeling_page_loader.py`에도
각각 독립적으로 존재 - 세 곳이 같은 순서를 유지해야 함). `_export_audio`가
`reference_starts`를 콜러블로 감싸 넘기는 지연 평가 이유는 2부 4번/12번.
무음 채움은 2부 17번.

**IMU/워치 export**: `_export_imu`/`_export_watch`가 `dict[str,
list[Path]]`를 순회하며 `_filter_csv_by_time`을 호출.
`_filter_csv_by_time(csv_paths, t_start, t_end, out_path,
ts_col_candidates=("timestamp", "t_sec"))`: 같은 센서의 세그먼트별 csv
파일들을 전부 열어서 시간 범위에 걸치는 행만 모아 합친다(리스트 전체를
순회하는 이유는 2부 4번의 버그 수정 경위, 컬럼명 방어 처리는 2부 9번).

**레이더 export**: `_get_radar_index`가 1회 생성해 캐시. `_export_radar`가
`RadarFrame` 리스트를 순회하며 원본 `.bin`에서 바이트를 직접 읽어 새
`radar_raw.bin`에 이어붙이고, 새 `radar_frame_index.csv`에 재번호를
매겨 기록(`new_offset_int16`이 0부터 다시 누적). 구간 중간에 레이더
설정(`cfg`)이 바뀐 경우를 대비해 사용된 cfg 전부를 sha256 기준 중복
제거해서 보존한다.

**동시성**: 매번 새 `SegmentExporter` 인스턴스를 만드는 이유는 2부
13번 참고.

### `back/label_taxonomy.py`

`DMS_Actions.xlsx`(distraction 라벨 폼의 Area/Verb/Noun 드롭다운 목록
원본)를 파싱해서 `Area -> {verbs, nouns}` 구조로 변환한다. xlsx 실측
구조는 2부 21번 참고(A열=Area, B열=Verb, C열=Noun, D열=Abstract Noun
참고용).

`AreaTaxonomy`(dataclass): `name`, `verbs: list[str]`, `nouns: list[str]`.

`load_dms_actions(xlsx_path) -> list[AreaTaxonomy]`: `openpyxl`로 첫
시트를 열어(`data_only=True`) 2행부터 순회. A열에 값이 있으면 새
`AreaTaxonomy`를 시작, 그 이후 행들은 B/C열 값을 중복 없이 추가(A열이
비어있는 행은 직전 Area 블록에 계속 포함 - 병합 셀 흉내).

드롭다운 채우기 헬퍼: `area_names(areas)`(모든 Area 이름 + `"기타"`),
`verbs_for`/`nouns_for`(해당 Area의 목록 + `"기타"`, Area를 못 찾으면
`["기타"]`만 반환). `"기타"`(자유 서술) 선택 시 텍스트 입력창을 보여주는
처리는 `front/widgets/label_forms.py`가 담당, 그 여부는
`LabelDraft.is_free_text_override`에 필드별로 기록된다.

### `back/video_codec.py`

OS/OpenCV 빌드마다 mp4 인코딩이 실제로 되는 fourcc가 달라서, 여러
후보를 순서대로 시도해보고 실제로 열리는 첫 번째 코덱을 골라 쓰는
유틸.

`get_working_fourcc(sample_size=(64,48), fps=30.0) -> str`: OS별 후보
목록(Windows는 `avc1`/`H264`/`mp4v` 순, Darwin은 `avc1`/`mp4v`, Linux는
`mp4v`/`avc1`/`H264`)을 순서대로 시도, 임시 파일에 실제로
`cv2.VideoWriter`를 열어보고 성공하는 첫 코덱을 반환(어느 후보도 안
되면 `mp4v`를 최후 안전값으로 반환). 결과는 프로세스 전역
`_cached_fourcc`에 캐시. 참고 빌드 환경(Ubuntu 24, OpenCV 4.13)에서는
`mp4v`가 확인된 동작 코덱이다.

`make_video_writer(out_path, fps, frame_size) -> cv2.VideoWriter`:
`get_working_fourcc()`로 코덱을 고른 뒤 `VideoWriter`를 만들어 반환.
`_write_video_cut`이 프레임 크기를 처음 알게 된 시점에 호출한다.

### 진입점: `front/main.py` / `run_app.py`

`front/main.py`는 `front/` 안에서 직접 실행하는 용도
(`python main.py [xlsx경로]`), `MainWindow(dms_actions_xlsx=xlsx_path)`를
만들고 `show()`. xlsx를 생략하면 "기타"만 있는 상태로 뜬다(2부 8번).

`run_app.py`는 프로젝트 루트에서 실행하는 진입점이자 PyInstaller가
빌드에 쓰는 스크립트. `_resource_base()`가 `sys.frozen`(PyInstaller
번들)이면 `sys._MEIPASS`, 아니면 프로젝트 루트를 반환해서 `sys.path`에
추가한다. `_find_default_xlsx()`가 명령줄 인자가 없을 때 (1) 번들된
실행 파일과 같은 디렉토리, (2) 번들 내부 리소스, (3) 현재 작업 디렉토리
순으로 시도한다 - 이 우선순위 덕에 배포된 실행 파일 옆에 새 xlsx를
놓으면 재빌드 없이 라벨 목록만 갱신할 수 있다(2부 8번).

### `front/main_window.py`

최상위 조립점: `StartPage` → 시나리오 선택 화면 → `LabelingPage`(시나리오별)를
`QStackedWidget`으로 전환하며 앱 전체 흐름을 관장한다.

출력/draft 경로 규칙: export 결과는
`<home_dir>/labeled_output/session_{trial_num:03d}_id_{subject_id}/`,
draft 임시 파일은 `<home_dir>/.labeling_drafts/{trial_folder_name}.json`.
원본 raw 데이터 폴더는 절대 건드리지 않는다. `_TRIAL_NAME_RE = r"^id(?P<id>\w+?)_trial(?P<trial>\d+)_"`로
트라이얼 폴더명에서 `subject_id`/`trial_num`을 뽑는다.

`__init__(dms_actions_xlsx=None)`: xlsx 경로가 있으면 `load_dms_actions()`로
`self.areas`를 미리 로드. `_labeling_pages: dict[Scenario, LabelingPage]`로
시나리오별 페이지를 캐시(한 번 만들면 재사용).

`closeEvent(event)`: `QApplication.instance().quit()`을 호출 - 메인 창을
닫으면 다른 창이 열려있어도 앱 전체를 종료한다(2부 19번).

`_on_session_selected(home_dir, trial_folder_name)`: `SessionLoader.load_trial()`
→ `SurveyParser.parse_all()`로 시나리오별 task window 딕셔너리를 만든다.
`DraftStore`와 `SegmentExporter`를 세션당 하나씩 만든다(`SegmentExporter`는
이후 "최종 저장"마다 새로 만들어짐 - 2부 13번, 이 `self._exporter`는
`session_dir`을 얻는 용도로만 계속 쓰임).

`_open_labeling(scenario)`/`_finish_open_labeling(...)` - 2단계 페이지
생성: 이미 만들어둔 페이지가 있으면 바로 전환. 없으면 시나리오 버튼
전부 잠금 → `LabelingPageDataLoader`를 백그라운드로 시작(2부 12번) →
`loaded` 시그널이 오면 미리 계산된 4개 값을 `LabelingPage` 생성자에
전달, 캐시에 저장, `_show_page_when_audio_ready()` 호출.

`_show_page_when_audio_ready(page)`: `page.playback.is_audio_ready`가
이미 참이면 바로 전환, 아니면 시나리오 버튼을 잠그고 `audio_ready`
시그널을 기다렸다가 전환한다(2부 18번의 경위).

`_on_back_from_labeling(page)`: `page.playback.pause()`를 먼저 호출한
뒤 시나리오 선택 화면으로 전환한다(2부 18번의 경위).

### `front/start_page.py`

앱을 열면 가장 먼저 보이는 화면. home 디렉토리(하위에 `bags/`가 있는
경로)를 입력/선택하고, 그 안의 트라이얼(세션) 폴더 목록에서 하나를
골라 "시작"을 누르면 다음 단계로 넘어간다.

시그널: `session_selected(str, str)` - `(home_dir, trial_folder_name)`.

`_browse()` - 비블로킹 파일 다이얼로그: 구현 경위와 이유는 2부 19번.
시작 경로는 `/home/`, 다이얼로그 인스턴스는 `self._browse_dialog`에
보관해 GC로 창이 조기에 닫히지 않게 한다.

`_refresh_trials()`: `SessionLoader.list_trials()`를 호출해 트라이얼
콤보박스를 채운다. 목록이 비어있으면 `<home_dir>/bags` 경로를 보여주며
안내.

`_on_start()`: home 디렉토리와 트라이얼이 둘 다 선택돼 있는지 확인 후
`session_selected` emit.

### `front/labeling_page_loader.py`

`LabelingPage`를 실제로 만들기 전에 필요한 무거운 계산(카메라 세그먼트별
timestamp csv 파싱, 오디오 세그먼트 이어붙이기, 센서 커버리지 조회)을
백그라운드 스레드에서 먼저 끝내두는 로더. 왜 필요한지는 2부 12번.

`_DISPLAY_STREAM_KEYS`/`_reference_segment_starts`: `front/labeling_page.py`의
`DISPLAY_STREAMS` 순서와 같은 우선순위 목록을 이 파일에 별도로(중복해서)
갖고 있다 - 오디오 세그먼트 경계 판단의 기준으로 쓸 카메라 스트림을
여러 파일에서 항상 같은 순서로 고르기 위함. `back/segment_exporter.py`의
`_CAMERA_REFERENCE_PRIORITY`도 같은 목록을 별도로 갖고 있다 - 세 곳이
각자 같은 리스트를 유지해야 한다.

`LabelingPageDataLoader(QThread)`: `loaded = Signal(object, object,
object, object)` - `(camera_indices, default_mic_name|None,
audio_result|None, sensor_coverage)`. 타입을 `dict`가 아니라 `object`로
선언한 이유는 2부 12번의 크래시 경위. `run()`: (1) 모든 카메라 스트림에
대해 `CameraTimestampIndex` 생성, (2) 기본 마이크(`"ecms1_audio"` 우선)로
`build_continuous_audio` 호출, (3) `compute_sensor_coverage(self.trial)`
호출, (4) 예외 시 `failed`, 아니면 `loaded` emit.

### `front/labeling_page.py`

세 시나리오(distraction/drowsiness/cognitive) 공통 라벨링 화면. 타임라인
+ 6분할 영상 + 라벨 폼 + draft 목록 + 최종 저장을 한 페이지 안에서
다룬다. `back/`의 거의 모든 모듈과 `front/`의 나머지 컴포넌트를 이
파일이 엮는다.

모듈 상수: `DISPLAY_STREAMS`(화면에 표시할 카메라 조합, `(position,
modality, 제목)` 6개 - depth는 화면엔 안 띄우고 백엔드에서만 저장됨,
4부 TODO). `MARKER_COLORS`(시나리오별 타임라인 마커 색).

생성자는 `camera_indices`, `default_mic_name`, `audio_result`,
`sensor_coverage` 네 값을 필수로 받는다(전부 `LabelingPageDataLoader`가
미리 계산). 순서: `_compute_total_range()` → `_build_ui()` →
`_build_playback()` → 첫 task window 로드 → `_refresh_progress_label()`.

키보드 처리: `showEvent`가 페이지 표시 시 포커스를 가져옴.
`eventFilter`(`_build_ui()` 끝에서 모든 자식 위젯에 설치)가 스페이스바를
항상 재생/일시정지 토글로 가로챈다(2부 18번). `keyPressEvent`: 스페이스,
←/→(1초 이동), Shift+←/→(`_step_frame`로 프레임 단위 이동).

`_compute_total_range()`: 카메라 + 오디오 + task window 시각만 전체
탐색 범위에 포함시킨다(IMU/워치/레이더는 경고 배너에서만 다룸).

`_build_ui()`: 좌측(stretch=3)에 타임라인 + 6분할 영상 그리드 + 재생
컨트롤, 우측(stretch=2)에 시나리오 제목/배너/진행상황/라벨 폼/구간
관리 버튼들. `window_combo`가 `activated`에 연결된 이유는 2부 18번.
`audio_combo`/`speed_combo`의 기본값은 시그널 연결 전에 설정된다(연결
후 설정하면 `self.playback`이 아직 없어 핸들러 호출 시 에러).

`_build_playback()`: `PlaybackController`를 만들고 `DISPLAY_STREAMS`
순서로 `StreamPlayer`(`road`면 `flip_180=True`)를 만들어 등록. 스트림이
없는 패널은 `set_unavailable()`.

오디오 마이크/배속 전환: `_select_audio_stream`, `_load_selected_audio`
(`_reference_segment_starts()`로 참조 시작 시각 구해 재빌드),
`_on_audio_stream_changed`(재생 중이었으면 멈추고 다시 로드 후 seek,
원래 재생 중이었으면 재생 재개), `_on_speed_changed`.

타임라인 ↔ 스크롤바 동기화: `_on_timeline_view_changed`/
`_on_scrollbar_changed`가 `_syncing_scrollbar` 플래그로 재진입을 방지하며
양방향 동기화한다.

`_load_task_window(window)`: current_window 갱신 → 마커 다시 그림 →
시나리오별 라벨 폼 프리필 → `_compute_guideline(window)`으로 가이드라인
구간을 구해 확대+seek(distraction은 가이드라인이 없어 window 전체로) →
`_missing_sensor_coverage()`로 경고 배너 갱신 → `_refresh_draft_list()`.
`_compute_guideline`: drowsiness+`DistractionTaskWindow`면
`window.drowsiness_window`, cognitive+`CognitiveTaskWindow`면
`window.start_ts`/`end_ts` 그대로, 그 외(distraction)는 `None`.

`_missing_sensor_coverage(start_ts, end_ts)`: 카메라는 7개 스트림 중
하나라도 대략적 범위가 겹치면 있다고 판정, 오디오/IMU/워치/레이더는
`self.sensor_coverage`를 이름별로 `range_overlaps_any()`로 개별
확인(2부 9번, `back/coverage.py` 절 참고). (소스에 `return missing`
다음에 도달 불가능한 `return False`가 한 줄 남아있음 - 예전 리팩터링의
잔재로 실행에는 영향 없음.)

시작/끝점 지정, 가이드라인 리셋: `_on_mark_start`/`_on_mark_end`(구간
미리보기 한계 해제 후 현재 재생헤드 위치를 기록), `_on_reset_to_guideline`.

draft 저장/조회/수정/삭제: `_on_save_draft`(pending 선택 검증 +
`find_overlap` 확인 후 저장, `boundaries_locked=False`면 pending 선택을
리셋해 다음 서브구간을 바로 이어 정의 가능), `_on_draft_item_clicked`
(2부 16번), `_on_delete_draft`/`_on_edit_draft`(`is_committed_and_present()`로
커밋된 draft 차단, 수정은 "지우고 새로 저장" 방식), `_refresh_draft_markers`
(미완료만 빨간 마커), `_refresh_draft_list`(완료 포함 전체에서 현재
window 것만 필터링, 완료는 `[완료]` 접두어).

구간 미리보기 한계 해제 지점 목록은 2부 16번 참고.

최종 저장(export) 흐름 `_on_final_commit`: 미완료 draft 모음 → 확인
다이얼로그 → 각 draft에 `source_window`/`cognitive_task_name` 결합 →
draft 조작 버튼 잠금 → **공유 `self.exporter`를 그대로 안 쓰고**
`SegmentExporter(self.trial, self.exporter.session_dir)`로 새 인스턴스
생성(2부 13번) → `ExportWorker` 시작(4개 시그널 연결). 진행 상황은
"저장 중... (n/전체구간) {단계 메시지}"로 표시. 완료/실패 시 성공한
항목들에 `mark_committed()` 호출 후 `_finish_export_ui()`로 UI 복구,
성공/경고(seek 폴백 있었음)/실패 3가지 다이얼로그.

### `front/export_worker.py`

"최종 저장" 배치를 백그라운드 스레드에서 돌리는 워커. 왜 필요한지는
2부 12번.

시그널: `progress(int, int)`(완료된 draft/전체), `step_progress(str)`
(`on_progress` 콜백을 그대로 emit으로 연결 - `back/`은 Qt 의존 금지라
이 워커가 다시 감싸는 것), `finished_ok(list, list)`(커밋 목록 +
`export_warnings`), `failed(list, str)`(실패 전까지 완료된 목록 + 에러
메시지 - 부분 성공 상태를 메인 스레드가 반영할 수 있도록).

`__init__(exporter, jobs, parent=None)`: `jobs: list[tuple[LabelDraft,
cognitive_task_name|None, source_window|None]]`. `exporter`는 "최종
저장"마다 새로 만든 인스턴스(2부 13번).

`run()`: `export_warnings`를 먼저 초기화 → jobs를 순서대로
`export_draft()` 호출, 완료마다 `progress` emit → 전부 끝나면
`finished_ok` emit. **`DraftStore.mark_committed()`는 이 워커 안에서
호출하지 않는다** - 메인 스레드가 완료/실패 시그널을 받은 뒤 호출한다
(2부 12번의 파일 I/O 분리 원칙).

### `front/stream_player.py`

카메라 스트림 하나에 대해 "절대시각 t에 해당하는 프레임을 가져와라"라는
요청을 실제 `cv2.VideoCapture` 호출로 옮기는 얇은 래퍼.
`CameraTimestampIndex`가 "이 시각은 어느 파일의 몇 번째 프레임인지"까지만
계산하고, 실제로 파일을 열어 프레임을 읽는 건 이 클래스가 한다.

`__init__(index, flip_180=False)`: `flip_180=True`면 road 카메라 보정
적용(2부 15번). 내부 상태: `_cap`, `_cap_path`, `_local_pos`(마지막으로
읽은 로컬 프레임 인덱스), `_last_frame`(캐시).

`frame_at_time(t)`: (1) `index.frame_at_time(t)`로 위치 조회, `None`이면
그대로 반환. (2) 파일이 다르면 새로 연다. (3) 로컬 인덱스가 방금 읽은
것과 같으면 `_last_frame` 그대로 반환. (4) 직전+1이면 `cap.read()`
그대로(순차 재생, 빠름). (5) 그 외(뒤로 이동, 앞으로 건너뛰기,
스크러빙)엔 `cap.set(POS_FRAMES, ...)` 후 `read()`. 이 순차/탐색 자동
판단은 export 쪽의 seek+검증+폴백 로직(2부 14번)과 별개다 - 재생은
`cap.set()`이 정확하다고 그대로 신뢰하고 쓰지만(화면에 살짝 어긋난
프레임이 보여도 치명적이지 않음), export 쪽은 실제 도달 위치를
검증하고 어긋나면 순차 디코딩으로 폴백한다(잘라낸 결과물의 정확도가
중요하므로).

`release()`: 열려있는 `cap`을 닫는다.

### `front/playback_controller.py`

대시보드 마이크 오디오(`QMediaPlayer`)를 마스터 시계로 삼아, 그 재생
위치를 기준으로 절대시각을 계산해서 등록된 카메라 스트림들의 해당
프레임을 가져와 화면을 갱신하는 컨트롤러. 왜 오디오 신호를 직접 화면
갱신에 안 쓰는지는 2부 11번.

상태: `stream_players`/`frame_callbacks`(`register_stream(key,
stream_player, on_frame)`으로 등록), `_audio_base_abs_ts`, `_audio_anchor_ms`/
`_audio_anchor_wall`, `_playback_rate`, `_playback_limit_ts`(2부 16번).

공개 메서드: `set_audio(wav_path, base_abs_ts)`(소스 설정/교체),
`is_audio_ready`(프로퍼티, 오디오가 없으면 `True` - 기다릴 게 없음,
있으면 `mediaStatus()` 확인), `set_playback_rate(rate)`(경과 시간을 옛
배속 기준으로 먼저 앵커에 반영한 뒤 벽시계 기준점을 새로 잡아서 배속
전환 시 위치가 안 튐), `play()`/`pause()`/`stop()`, `seek_to(abs_ts)`,
`release()`.

`play()`가 `position()`을 다시 안 읽는 이유, `seek_to()`가 화면을
동기적으로 갱신하는 이유, `_on_audio_media_status_changed`의 역할은 2부
18번의 버그 경위 참고. `_on_display_tick()`의 조기 정지 방지 로직은
2부 11번 참고.

### `front/widgets/timeline_widget.py`

전체 트라이얼 구간을 가로 바 하나로 그리는 커스텀 위젯. task window
마커, 확정 전 draft 구간, 재생헤드, 확대/이동된 보기 범위를 전부 이
위젯 하나가 그린다.

두 종류의 "범위" - `total_start`/`total_end`(트라이얼 전체)와
`view_start`/`view_end`(현재 확대 범위)를 분리해서 관리한다 - 전체
세션(수십 분)이 고정폭 바 하나에 그대로 매핑되면 몇 초짜리 액션을
픽셀 단위로 정밀하게 다루기 어렵기 때문. 화면 좌표 변환(`_ts_to_x`/
`_x_to_ts`)은 항상 view 기준.

시그널: `position_clicked(float)`, `view_range_changed(float, float)`.

범위 조작 메서드: `set_range`, `set_view_range`(total 밖으로 못 나가게,
`MIN_VIEW_SPAN_SEC=2.0`초보다 안 좁아지게 clamp), `pan_by(fraction)`,
`zoom_to_fit(start_ts, end_ts, padding_ratio=0.2)`, `reset_view()`.

`set_playhead(ts)` - 자동 따라가기: 재생헤드가 확대된 view 가장자리
근처(`PLAYHEAD_FOLLOW_MARGIN_RATIO=5%` 안쪽)까지 오면 확대 폭은 유지한
채 view 범위 자체를 밀어서 재생헤드가 항상 보이게 한다(2부 18번의
경위).

`paintEvent` 그려지는 순서: 배경 트랙 → task window 마커(라벨 겹침
방지를 위한 2단 교대 배치, 2부 18번) → 확정 전 draft 구간(반투명) →
현재 pending 선택 구간(빨간 반투명) → 재생헤드(빨간 세로선) → 확대
중일 때만 맨 위에 미니 오버뷰 바.

`_ts_to_x`는 결과를 `[-1_000_000, 1_000_000]`으로 clamp한다 - 마커
시각이 보이는 범위를 크게 벗어나면 비율 계산이 극단적으로 커져서 int
캐스팅 시 오버플로우/크래시가 날 수 있는 것을 막는 방어적 처리(2부
10번의 헤드리스 검증 중 발견된 버그).

마우스 입력: 휠(확대/축소, `factor=0.85`), 좌클릭(클릭/드래그 위치를
`position_clicked` emit), 우클릭 드래그(화면 이동/pan).

`TimelineMarker`(dataclass): `label`, `start_ts`, `end_ts`, `color`(기본
`"#4a90d9"`). task window 마커와 draft 마커 둘 다 이 타입으로
표현된다.

### `front/widgets/video_panel.py`

카메라 프레임(`np.ndarray`, BGR) 하나를 표시하는 `QLabel` 기반 패널.
화면 분할(최대 6개 패널)의 각 칸이 이 클래스의 인스턴스.

두 가지 "빈 화면" 상태 구분: `set_unavailable(reason=...)`(이 트라이얼에
해당 스트림 자체가 없음 - 세션 내내 고정), `update_frame(None)`(스트림은
있는데 지금 이 시각엔 프레임이 없음 - gap, 녹화 범위 밖 등, 시시각각
바뀔 수 있음). 화면 문구도 다르다.

`clicked` 시그널: `mousePressEvent`에서 포커스 처리 후 emit. 영상 화면
클릭도 "구간 미리보기 한계" 해제 신호로 취급한다(2부 16번).
`setFocusPolicy(Qt.ClickFocus)`도 같이 설정 - 이게 없으면 패널 클릭 후
재생/탐색 단축키가 안 먹힐 수 있다.

`update_frame(frame)`: `None`이면 "데이터 없음" 텍스트, 아니면
`frame.ndim==2`(흑백/depth 등)면 정규화 후 3채널 변환, 아니면
BGR→RGB, `QImage`→`QPixmap`으로 변환 후 비율 유지 리사이즈.

### `front/widgets/label_forms.py`

시나리오별(distraction/drowsiness/cognitive) 라벨 입력 폼 3종 + 공통
위젯 `ComboWithOther`. `back/label_taxonomy.py`가 제공하는 Area/Verb/Noun
목록을 받아 드롭다운을 채운다.

`ComboWithOther`: 카테고리형 필드 공통 패턴 - 드롭다운 +
`"기타"`(`OTHER_LABEL`) 선택 시 자유 서술 입력란이 나타난다.
`value()`(드롭다운이 `"기타"`가 아니면 그 텍스트, `"기타"`면 자유
서술 텍스트와 함께 `True`), `set_value(value)`(역방향), `set_options(options)`
(`blockSignals`로 감싸서 옵션 교체 중 불필요한 신호 방지).

`ROAD_CONDITIONS`/`WEATHER_OPTIONS`: PDF 목업 기반 placeholder 목록 -
실제 표준 옵션 목록이 확정되면 이 두 리스트만 교체하면 된다.

`DistractionLabelForm(areas)`: 지시문 힌트, Area(`ComboWithOther`),
Verb, Verb-세부(자유 텍스트), Noun, Noun-세부(자유 텍스트), 도로상황,
날씨. Area를 바꾸면 `_on_area_changed`가 그 Area에 맞는 Verb/Noun
옵션을 다시 채운다. `get_label_fields()`/`load_values()`가 저장/수정
시 값을 읽고/되돌려 채운다(Area를 먼저 설정해야 Verb/Noun 옵션이 갱신된
뒤 값 설정 가능하므로 순서가 중요).

`DrowsinessLabelForm`: KSS 점수(읽기 전용 `QLabel`, `set_prefill_kss`),
도로상황, 날씨. 2부 20번의 경위로 `QSpinBox`에서 `QLabel`로 교체됨.

`CognitiveLabelForm`: 태스크(읽기 전용), 난이도(읽기 전용,
`set_prefill`), 날씨. `overrides["difficulty"]`는 항상 `False` 고정 -
"기타" 개념 자체가 없는 필드.

세 폼이 공유하는 인터페이스: `set_hint`/`set_prefill_kss`/`set_prefill`은
시나리오별로 다르게 호출되지만, `get_label_fields()`/`load_values()`는
세 폼 다 같은 시그니처라 `LabelingPage`가 시나리오를 구분하지 않고
`self.label_form`을 다형적으로 다룰 수 있다.

## 4부. 아직 확정 안 된 것 (TODO)

1. **watch 폴더 실제 파일/컬럼명**: `watch_hr.csv` 등의 정확한
   컬럼명(예: `timestamp` vs `t_sec`)을 실측 샘플로 확인 못함 - 현재
   `_filter_csv_by_time`이 두 후보 다 시도하도록 방어적으로 짜뒀지만,
   실제 컬럼명이 다르면 조용히 빈 파일이 나올 수 있음(2부 9번).
2. **distraction_task_id → Area/Verb/Noun 매핑**: `"1_2"` 같은 id가
   xlsx의 verb/noun 순서와 정확히 일치하지 않아서, 자동 프리필이 아니라
   라벨러가 `distraction_task_text` 힌트를 보고 직접 선택하는 방식으로
   남겨둠(매핑표가 따로 있으면 자동 프리필 가능).
3. **레이더 seg 폴더 간 시간 역전 가능성**: `RadarTimestampIndex`는
   안전하게 `ros_time_sec` 기준으로 전체 재정렬하지만, 서로 다른 seg의
   프레임이 실제로 뒤섞여 저장되는 경우가 있다면(정상적으론 없어야 함)
   프레임 하나하나가 별도 파일 I/O를 일으켜 느려질 수 있음 - 필요시
   연속 구간 배치 read로 최적화 가능.
4. **`DISPLAY_STREAMS`가 depth 스트림을 화면에 표시하지 않음**: export
   시에는 다른 카메라와 동일하게 저장되지만, 라벨링 화면의 6분할
   그리드에는 없음 - 필요하면 `front/labeling_page.py`의
   `DISPLAY_STREAMS`에 추가.
5. **`front/labeling_page.py._missing_sensor_coverage`의 도달 불가능한
   `return False`**: 예전 리팩터링의 잔재로 보이며 실행에는 영향 없음 -
   정리해도 되고 안 해도 됨.
6. **`ROAD_CONDITIONS`/`WEATHER_OPTIONS`가 placeholder**: PDF 목업 기반
   예시 목록. 실제 표준 옵션이 확정되면 `front/widgets/label_forms.py`의
   두 리스트만 교체하면 됨.

