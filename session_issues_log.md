# 지금까지 제기된 문제와 발생했던 문제 정리

이 프로젝트 작업 세션 전체에서 사용자가 제기한 문제, 실측 데이터 검증 중 발견된
버그, 그리고 그 해결 여부를 시간 순으로 정리.

## 1. 초기 UI 렌더링 문제

- **RGB/IR 카메라 해상도가 다르게 출력됨, 화면 전체가 안 나옴** → 확인 및 설명.
- **라벨 폼 드롭박스가 클릭해도 비어있음** → 원인 확인.
- **오디오가 절대 시간 기준으로 동기화되어 재생되는지 불확실, 재생이 안 되는
  것처럼 보임** → 아래 오디오 재생 문제로 이어짐. **[해결]**

## 2. 오디오 재생 문제

- **세그먼트로 쪼개진 오디오 파일(`{mic}_seg001.wav`, `_seg002.wav`...)을 지원 안 함**
  → `AudioStreamFiles` 도입 + `back/audio_stitcher.py`로 세그먼트를 하나의 연속
  wav로 이어붙이는 방식 도입. **[해결]**
- **마이크 이름 매칭이 잘못됨** → 스캔 정규식 수정. **[해결]**

## 3. 타임스탬프/정렬 버그 (실측 데이터로 검증하며 여러 라운드에 걸쳐 발견)

- **CameraTimestampIndex가 "행 번호 카운팅" 방식이라 엉뚱한 세그먼트 파일을
  가리킴** — csv가 세그먼트 일부만 담고 있을 때 행 번호와 실제 프레임 번호가
  어긋남. → 시간 산술 기반(`segment_starts` + fps)으로 완전히 재작성. **[해결]**
- **"csv 행 개수 = 그 세그먼트의 실제 프레임 수"라는 가정이 틀림** — 타임스탬프를
  남기는 로거와 실제 인코딩이 별개 파이프라인이라 스트림마다 최대 수백 개까지
  차이남 → 뒷부분 프레임이 도달 불가능해지는 문제 있었음. → 행 개수 세기를 아예
  없애고 절대시각 기반 계산으로 전환. **[해결]**
- **fps를 cv2 명목값 그대로 써서 세그먼트 뒤쪽으로 갈수록 최대 4~5초 드리프트**
  → 실측 timestamp span ÷ 실제 프레임 수로 fps를 역산해서 보정
  (`_calibrate_fps`). **[해결]**
- **"프레임 차이가 1프레임 정도가 아닌 것 같다"는 문제 제기** → 원인이 위 두
  버그(행 카운팅 + fps 미보정)의 누적임을 확인. **[해결]**
- **오디오가 비디오와 완전히 다른 시간대의 소리로 재생됨** — `AudioTimestampIndex`가
  자기 sparse csv만으로 독자적으로 세그먼트 시작 시각을 추정하다가, 앞/뒤 세그먼트를
  헷갈림(csv에 세그먼트 일부만 남아있었는데 그걸 첫 번째로 오인). → 비디오가 계산한
  `segment_starts`를 오디오가 그대로 받아쓰는 구조(`reference_segment_starts`)로
  변경. **[해결]**
- **정렬이 거의 다 맞았는데 "1초도 안 되는 아주 미세한 밀림"이 남아있음** —
  오디오 세그먼트의 실제 길이가 비디오가 선언한 세그먼트 길이보다 항상 더 김(세그먼트당
  최대 1.5초) → `audio_stitcher.py`에서 세그먼트별로 "다음 세그먼트 시작 시각"까지만
  쓰고 초과분은 잘라내는 방식으로 수정, byte 단위로 정확히 일치함을 상관계수/
  `np.array_equal`로 검증. **[해결]**
- **재생 중 영상 프레임레이트가 낮아 보임(끊김)** — `QMediaPlayer.positionChanged`가
  실측 약 15Hz(64ms 간격)로만 발생 → 오디오 위치를 벽시계에 앵커링해두고 별도
  33ms 타이머로 외삽하는 구조(`_display_timer`)로 전환. **[해결]**
- **seek 후 영상이 완전히 멈추는 회귀** — `seek_to()`가 오디오 플레이어 상태에
  따라 조건부로만 갱신되던 것을, 항상 동기적으로 `_update_all_frames` 호출하도록
  수정. **[해결]**

## 4. UI 입력/포커스 문제

- **프레임 단위 이동이 클릭 한 번씩만 되고 꾹 눌러도 연속 이동이 안 됨** →
  `setAutoRepeat(True)` + delay/interval 설정. **[해결]**
- **비디오 패널을 클릭해도 키보드 포커스(스페이스/방향키)가 안 잡힘** →
  `setFocusPolicy(Qt.ClickFocus)` 추가. **[해결]**

## 5. 도메인 로직 관련 문제 제기 (질문 → 설계 반영)

- "cognitive_pre_cbt3 같은 경우는 타임스탬프가 있어서 맞아야 하는 거 아니냐"는
  문제 제기 → survey json 기반 자동 계산 로직 점검 계기가 됨.
- "모든 센서 프레임을 t_sec 축에 대해 정렬시켜야 한다"는 원칙 제기 → 이후 모든
  정렬 로직이 이 원칙(공통 t_sec 축, 센서별 독립 변환)을 따르도록 설계.
- "시나리오마다 세그멘테이션 방식이 다르게 되어 있는 게 맞냐"는 질문 → distraction
  (자유 분할) vs drowsiness/cognitive(고정 계산) 구조 확인.
- **distraction 외 시나리오는 수동 정밀 조정이 안 됨** → drowsiness/cognitive도
  자동 계산 구간을 "가이드라인" 기본값으로 삼되 라벨러가 프레임 단위로 정밀
  조정 가능하게 변경. **[해결]**
- "cognitive_before_driving json에 이미 타임스탬프가 있는데 활용 안 하냐"는 질문
  → 활용 구조 확인/반영.
- 정렬 로직 상세 설명 요청 (여러 차례, csv가 여러 폴더에 걸쳐있는데 다 순회하는지,
  t_sec vs t_kst 차이 등) → 설명 제공, 이번 세션에서 `design_considerations.md`로
  정리.
- 실제 센서 데이터 형태 설명(IMU가 accel/gyro로 나뉘고 데이터+타임스탬프가 하나로
  합쳐진 csv라는 점, radar/watch는 데이터 들어오면 재개하기로 함) → 확인, 반영.
- **drowsiness의 KSS 점수, cognitive의 난이도가 자유 입력 필드로 돼 있었는데
  "어차피 파싱이니까" 고정 표시(QLabel)로 바꿔달라는 요청** → `kss_spin`(QSpinBox)
  → `kss_label`, `difficulty_field` → `difficulty_label`로 변경. **[해결]**
- **구간 겹침 검증, 데이터 없음 이유 안내, task window 안내, 진행 상황 파악을
  한 번에 처리해달라는 요청** → `find_overlap`, `no_data_banner`, `progress_label`,
  `coverage_warning_label` 등 일괄 추가. **[해결]**

## 6. Git/배포 작업 중 발생한 문제

- **브랜치 체크아웃 시 작업트리가 그 브랜치의 옛날(버그 있는) 코드로 덮어써짐**
  (backend 브랜치 체크아웃 시 `back/`, frontend 브랜치 체크아웃 시 `front/`가
  반복적으로 발생) → `git checkout main -- back/`(또는 `front/`)으로 복구,
  이후 브랜치 전환마다 `grep`으로 파일 내용 재확인하는 습관으로 대응.
- **git commit이 "Author identity unknown"으로 실패** → 전역이 아닌 이 저장소
  로컬 한정으로만 `user.name`/`user.email` 설정(사용자가 명시적으로 이 방식을
  선택). **[해결]**
- **git push가 자격증명 없음으로 실패** (gh CLI 미설치, 저장된 인증 없음) →
  사용자가 직접 터미널에서 push 실행, 3개 브랜치(main/backend/frontend) 전부
  성공 확인. **[해결]**

## 7. 진단 스크립트(`check_av_frames.py`) 개발 중 제기된 문제

- **Jupyter 커널이 죽음** — 시스템 파이썬(`/usr/bin/python3`)엔 pip조차 없어서
  ipykernel 재설치가 불가능한 상태였음 → 프로젝트 `.venv` 커널로 전환해야 함을
  확인(시스템 파이썬이 아니라).
- **"둘에서 왜 behavior_color seg001의 프레임 수가 다르게 읽히는지"** (노트북에서
  직접 센 값과 스크립트 결과가 다름) → 원인 확인 후 스크립트가 실제로 열어서 세는
  방식으로 신뢰도 향상.
- "콘솔에 출력하지 말고 txt 파일로 저장해달라"는 요청 → `check_av_frames.py`,
  이후 만든 모든 진단 스크립트(`inspect_new_timestamp_schema.py`,
  `describe_files.py`)에 동일 패턴 적용. **[해결, 이후 관례로 정착]**
- **"오디오도 실제 프레임 수/샘플 수를 보여줘, (헤더를) 신뢰하지 말고"** → wav
  헤더 값과 별개로 `readframes`로 끝까지 실제로 읽어서 진짜 샘플 수를 직접
  카운트하는 로직 추가. **[해결]**
- **"오디오는 timestamp 수와 (실제 샘플 수가) 다를 수 있는 거 아니냐"는 지적** —
  오디오 csv 한 행은 raw 샘플 1개가 아니라 가변 길이 청크라서, 행 개수를 샘플
  수와 직접 비교하는 게 원래 의미 없었음. 사용자 지적이 맞았음 → 행 개수 비교
  대신 시간 스팬(초) 단위 비교(`_csv_time_span`)로 수정. **[해결]**

## 8. 수집 파이프라인 구조 변경 대응 (최근, 진행 중)

- **새 트라이얼(`id9276_trial3_...`) 2개에서 `check_av_frames.py` 결과가
  비어있음("왜 비어있지?")** → 원인 확인: 카메라/오디오/IMU/watch 폴더 구조가
  "seg 번호가 파일명에 있는 flat 구조"에서 "seg 번호가 `seg{NNN}/` 하위 폴더인
  중첩 구조"로 완전히 바뀌어 있었음. 기존 스캔 정규식이 하나도 못 찾아서 발생.
- **`back/session_loader.py`가 신/구 폴더 구조를 자동 판별해서 둘 다 지원하도록
  수정** (사용자가 "둘 다 지원"을 명시적으로 선택 - 과거 트라이얼도 계속 써야
  하므로). 카메라 7스트림/오디오 2마이크/IMU/watch 전부 신규 트라이얼 2개에서
  정상 탐색됨을 검증. **[해결]**
- **카메라/오디오 timestamp csv의 컬럼 스키마도 완전히 바뀜** (IMU/watch는 안
  바뀜) — `frame_idx,t_sec,...` → `저장영상프레임번호,...,동기화기준시각_sec,...,
  복제프레임여부,프레임종류` (오디오는 `저장오디오청크번호` + `누적바이트수` 등
  추가). `back/timestamp_index.py`의 `_load_timestamps()`가 옛 컬럼명을 하드코딩
  하고 있어서 신규 트라이얼에서 `KeyError` 발생 예상됨을 사전에 확인.
- **정렬 기준 시각으로 어떤 컬럼을 쓸지 결정 필요** (`동기화기준시각_sec` vs
  `저장완료시각_sec`) → `동기화기준시각_sec`(실제 캡처 시각) 채택, 다른 센서의
  t_sec과 같은 축이어야 하므로.
- **"복제프레임여부" 컬럼을 이번 수정에서 어떻게 처리할지** → 일단 무시하고
  진행하기로 결정 (복제 행은 직전 행과 동일 시각이 한 번 더 찍히는 것뿐이라
  gap 감지/선형 계산에 영향 없음을 확인).
- **"복제 프레임이 무슨 뜻이냐"는 질문** → 실측 데이터로 카메라 드라이버가 다음
  프레임을 제때 못 받으면 직전 프레임을 복사해 mp4 fps를 유지한다는 메커니즘
  확인 (46251개 중 426개, 약 0.9%).
- **"드라이버 지연/드롭"이 왜 생기는지 설명 요청** → USB 대역폭 경합, CPU 스케줄링
  지연, 디스크 I/O 백프레셔, ROS 토픽 큐 오버플로 등 일반적 원인 설명, 실측
  간격(정상 간격의 정확히 2배)으로 "프레임 1개 주기만큼만 밀렸다 바로 복귀"하는
  패턴임을 확인.
- **"precision(정밀도) 면에서 문제 없냐"는 질문 (2차례, 절대값 → 프레임 정렬
  수준의 짧은 시간차까지)** → float64 ULP(~0.24마이크로초)와 실제 데이터로
  Decimal 대조 검증(인접 프레임 오차 1.2피코초, 세그먼트 전체 오차 0.18나노초)
  → 정렬에 필요한 최소 단위(마이크로초~밀리초)보다 훨씬 촘촘해 문제없음 확인.
- **survey json과 timestamp csv의 시간 표기 형식 비교 요청** → json은 ISO
  `T` 구분자 + epoch 없음, csv는 공백 구분 KST 문자열 + epoch초 값 둘 다 제공.
  json 형식 자체는 구/신 트라이얼 동일(이번 변경과 무관)함을 확인.
- **`back/timestamp_index.py` 실제 수정** — 처음엔 구/신 스키마 컬럼명을 후보
  목록으로 두고 자동 판별하는 방식을 제안했으나, "구 버전은 이제 쓰일 일이
  없다고 가정해"라는 방향 전환에 따라 신규 컬럼명(`저장영상프레임번호`/
  `저장오디오청크번호`/`동기화기준시각_sec`)을 직접 하드코딩하는 단순한 방식으로
  변경. 새 트라이얼 데이터로 `CameraTimestampIndex`/`AudioTimestampIndex` 정상
  동작 검증(segment_starts, fps 보정, frame_at_time, 샘플 구간 계산 전부 확인).
  **[해결]**
- **`check_av_frames.py`의 오디오 시간 스팬 계산(`_csv_time_span`)도 옛 컬럼명을
  참조하고 있어 동일하게 수정 필요** → 신규 컬럼명(`저장오디오청크번호`/
  `동기화기준시각_sec`)으로 수정. **[해결]**

## 9. 세그먼트별 timestamp csv 자기완결성 재설계 (8번 구조 변경 대응의 후속)

- **"그럼 지금 자르면 어떤식으로 자른 지점이 저장되는 구조지?"라는 질문** →
  t_sec(절대 unix 시각)만 draft/annotation에 영구 저장되고, 로컬 프레임/샘플
  인덱스는 자르는 시점에 그때그때 다시 계산되는 휘발성 값이라는 원칙을 확인/설명.
- **"근데 몇 번째 인지는 알아야 하지 않나? 결국은 자를 때는 몇 번째 프레임인지로
  자른다는 거지?"라는 후속 질문** → 맞다고 확인: t_sec는 "진실의 근원"이고, 실제
  컷은 t_sec을 그 세그먼트 파일의 `segment_starts`+fps로 역산해 로컬 프레임
  번호로 변환한 뒤 수행됨. 이 변환값 자체는 저장하지 않고 항상 새로 계산함
  (세그먼트 파일 구성이나 fps 보정값이 나중에 바뀌어도 안전하도록).
- **신규 수집 파이프라인은 세그먼트 하나하나가 자기 timestamp csv를 따로 갖고
  있다는 게 확인됨** ("세그먼트가 나뉘어도 타임스탬프는 올바르게 찍혀") — 기존
  구조(csv 하나를 여러 세그먼트가 공유, gap 감지로 나눔)와 근본적으로 다른데,
  8번 섹션에서 반영한 코드는 이 케이스를 구분하지 못하는 상태였음. →
  `CameraStreamFiles`/`AudioStreamFiles.segment_files`를 `(seg_num, media_path)`
  2튜플에서 `(seg_num, media_path, timestamp_csv_path)` 3튜플로 확장,
  `back/session_loader.py`가 신규 구조 스캔 시 세그먼트 폴더별 csv를 개별적으로
  찾아 매핑하도록 수정. **[해결]**
- **`back/timestamp_index.py` 전면 재작성** — "csv를 공유하는 세그먼트가 몇
  개인지"로 신/구조를 자동 판별하는 `_load_segment_t_secs()` 도입(혼자 쓰는
  csv는 통째로 신뢰, 공유 csv는 기존 gap 감지 로직 유지), `CameraTimestampIndex`가
  더 이상 별도 `timestamp_csv` 인자를 받지 않고 `segment_files` 하나만으로
  자기 완결적으로 계산하도록 생성자를 단순화. **[해결]**
- **오디오도 동일 원칙을 적용할지 논의** — "그러면 지금 로드 할 때 어떻게 로드를
  하지? 센서들의 데이터를 로드할 때"라는 질문에 로드 순서/의존관계를 설명한 뒤,
  오디오도 카메라와 같은 원칙(자기 데이터 우선, 부족한 세그먼트만 보조 참조)을
  따라야 한다는 방향으로 `AudioTimestampIndex`도 재작성. `reference_segment_starts`가
  "무조건 비디오 값을 그대로 받아쓰는" 방식(3번 섹션에서의 예전 해결책)에서
  "자기 데이터로 못 채운 세그먼트만 메꾸는 보조 수단"으로 축소됐고, 즉시 계산이
  아니라 콜러블로 지연 평가되도록 바꿔서 불필요한 무거운 계산도 함께 없앰(10번
  성능 문제와 직결). 이로써 예전 `/code-review`에서 지적됐던 "`_export_audio`가
  `reference_segment_starts`를 안 넘긴다"는 문제도 자연히 해소됨(이제 항상
  지연 콜러블로 넘김). **[해결]**
- **IMU/watch가 세그먼트 여러 개일 때 마지막 파일만 남고 앞쪽이 조용히 사라지는
  버그 발견** — 카메라/오디오와 같은 근본 원인(스캔 시 `dict[str, Path]`로
  마지막 매치만 저장)이 IMU/watch 스캔 코드에도 그대로 있었음. →
  `dict[str, list[Path]]`로 변경, `_export_imu`/`_export_watch`/
  `_filter_csv_by_time`도 리스트 전체를 순회해 시간 범위에 걸치는 행을 모두
  병합하도록 수정. **[해결]**

## 10. "응답 없음" 성능 버그 2건과 백그라운드 스레드 전환

- **"그리고 응답 없음이 뜨는 이유도 필요해 아니 실행중인건데 왜 응답 없음이야"라는
  문제 제기** → Qt 이벤트 루프가 메인 스레드에서 무거운 동기 작업 중이면 OS가
  "응답 없음"으로 표시한다는 것(실제로 멈춘 게 아니라 화면 갱신을 못 할 뿐)을 설명.
- **최종 저장(커밋) 시 실제로 저장이 안 되는 것처럼 보임** — 스크린샷으로 제보,
  "저장이 아예 안된다고"를 반복하며 심각성 강조 → 프로파일링으로 원인 특정:
  `_write_video_cut`이 자르려는 구간까지 프레임 0부터 순차 디코딩(세그먼트당
  최대 ~5700 프레임 낭비 × 스트림 7개)해서 스트림당 수십 초, 전체 약 51초가
  걸렸고, 그동안 UI가 완전히 멈춰 "응답 없음"이 떠서 실제로는 끝까지 실행되고
  있었는데도 "저장이 안 되는 버그"로 보였던 것.
  - 1차 수정: `cv2.VideoCapture.set(POS_FRAMES, N)`으로 탐색 후 실제 도달 위치를
    `cap.get()`으로 재확인, 요청 지점 이하로 도달했으면 그대로 신뢰하고 순차
    디코딩으로 마저 채움(빠름). 넘어서 버렸으면 프레임 0부터 순차 디코딩하는
    기존 방식으로 자동 폴백 + `export_warnings`에 원인 기록. "그렇지 그러면 위에서
    설명한 수정을 하자 그리고 만약 폴백을 하게 되면 설명하는 화면을 띄워주자"라는
    요청대로, 폴백이 실제로 발생하면 최종 저장 완료 시 `_finish_export_ui`가
    `QMessageBox.warning`으로 그 사실을 라벨러에게 보여줌. mp4v 코덱에서는 요청
    프레임에 정확히 도달함을 픽셀 단위(`np.array_equal`)로 검증. **[해결]**
  - 2차 수정: 그럼에도 남아있던 지연 원인이 `_export_audio`가
    `_reference_camera_segment_starts()`를 매번 즉시(eager) 호출해 최대 6개
    카메라 스트림의 인덱스를 통째로 새로 만들고 있었기 때문임을 확인 → 오디오
    자신의 데이터로 이미 충분한 경우(대부분)엔 이 계산 자체가 필요 없으므로,
    콜러블로 감싸 지연 평가(lazy)하도록 변경(9번 섹션과 동일 수정). **[해결]**
  - "그럼 대략 1분 정도 걸린다고 생각하면 되나?"라는 질문에, 위 최적화 이후에도
    여러 센서를 동시에 자르는 특성상 어느 정도 시간이 걸릴 수 있음을 설명 → 아래
    진행 상황 표시 + 완전 백그라운드 처리로 방향을 잡음.
- **"저장 중 이라고는되는데 진행 상황 정도를 알 수 는 없나?"라는 요청** → export를
  메인 스레드에서 완전히 분리하기로 결정: `front/export_worker.py`의
  `ExportWorker(QThread)` 신규 작성. `SegmentExporter.export_draft()`에
  `on_progress` 콜백 인자를 추가해 카메라 스트림별/오디오/IMU·워치·레이더
  단계마다 진행 메시지를 보고하도록 수정, `LabelingPage`가 "저장 중...
  (n/전체구간) 카메라 내보내는 중: driver_rgb (3/7)" 같은 결합된 진행률 텍스트를
  표시. export 도중에는 `_set_draft_controls_enabled(False)`로 draft 수정 관련
  버튼/구간 선택 콤보를 잠가서 동시 조작으로 인한 상태 꼬임도 방지. **[해결]**
- **"라벨링 시작"도 화면 전환 시 약 5초간 응답 없음이 뜨는, 동일 근본 원인의
  문제 발견** → 직접 프로파일링으로 `LabelingPage(...)` 생성자 실행이 5.30초
  걸리는 것을 확인(카메라 인덱스 구성 + 오디오 스티칭이 원인). →
  `front/labeling_page_loader.py`의 `LabelingPageDataLoader(QThread)` 신규
  작성 — 카메라 인덱스 딕셔너리, 기본 마이크 선택, 오디오 스티칭 결과를
  백그라운드에서 미리 계산하고, `LabelingPage` 생성자는 이 미리 계산된 결과를
  필수 인자(`camera_indices`, `default_mic_name`, `audio_result`)로 받아
  위젯만 빠르게 조립하도록 시그니처를 변경. 사용자가 "그렇게 수정하자"로
  확정. **[해결]**
  - 이 과정에서 **Qt 시그널로 (튜플 키 + 커스텀 객체 값을 가진) `dict`를
    넘기면 PySide6가 `_pythonToCppCopy: Cannot copy-convert ... (dict) to
    C++` 오류로 죽는 실제 크래시를 발견** → 시그널 타입을
    `Signal(dict, object, object)`에서 `Signal(object, object, object)`로
    변경해 해결. **[해결]**

## 11. 화면 전환/재생 타이밍 관련 연쇄 버그

- **"지금 어느 세션에 처음 들어왔을 때 task1으로 되어 있잖아 근데 실제 위치는
  왜 task1이 아니지? task1을 다시 선택해도 이동하지 않아"라는 스크린샷 제보** →
  두 가지 별개 원인 확인:
  1. `QComboBox.currentIndexChanged`는 이미 선택된 항목을 다시 선택해도 신호가
     발생하지 않음(인덱스가 안 바뀌므로) — task2로 갔다가 task1로 돌아오면
     되는 것과 일치하는 증상. `window_combo`의 연결을 `currentIndexChanged`에서
     `activated`(사용자가 명시적으로 고를 때마다 항상 발생)로 변경. **[해결]**
  2. "처음 들어와서 보면 task1을 가리키고 있는데 재생을 누르면 처음부터
     재생된다"는 후속 제보(스크린샷 2장, 해상도도 다르게 보인다는 관찰 포함) →
     `PlaybackController.play()`가 `QMediaPlayer.setPosition()` 직후 곧바로
     `position()`을 다시 읽어 앵커를 재설정하고 있었는데, `setPosition()`이
     비동기라 아직 반영 전(0)인 상태를 읽어버려 앵커가 0으로 덮어써지는 경쟁
     상태였음. → `play()`에서 `position()` 재조회를 없애고, `seek_to()`가
     이미 설정한 `_audio_anchor_ms`를 그대로 신뢰하도록 수정. **[해결]**
  - "처음 들어오면 task1의 위치에 가있기는 한데 실제로 재생을 누르면 다시
    처음으로 돌아가는데?"라며 위 수정 이후에도 남아있던 증상 재확인 → 원인이
    하나 더 있었음: 크고 이어붙인 wav 파일은 로딩이 끝나기 전에 건
    `setPosition()`이 조용히 무시될 수 있음(`QMediaPlayer`가 아직
    `LoadedMedia` 상태가 아니므로) → `_on_audio_media_status_changed`를
    새로 추가해 `LoadedMedia`/`BufferedMedia` 상태가 되는 시점에 앵커 위치를
    다시 적용하도록 함. **[해결]**
  - 근본적으로 "그럼 애초에 그냥 로드가 다 완료되고 나서 화면을 띄워 주는게
    어때?"라는 사용자의 제안으로, 위 개별 땜질 대신 **오디오가 실제로 재생
    준비된(`is_audio_ready`) 시점까지 화면 전환 자체를 미루는 구조**
    (`audio_ready` 시그널, `MainWindow._show_page_when_audio_ready`)로 최종
    정리. "그럼 모든 센서들의 데이터가 정렬이 완료되고 라벨러가 영상과 오디오를
    들을 준비가 다 되어야 창을 띄워주는 건가 이제?"라는 확인 질문에 그렇다고
    설명. **[해결]**
  - 이 과정에서 "동기, 비동기가 뭐지?"라는 개념 질문에 설명 제공.
- **로딩 중 표시 UI를 넣었다가 사용자가 롤백 요청** — "그럼 로딩되는 동안
  로딩 중 이라고 띄워줄까?" → "예를 들어 distraction라벨링 시작 이라는 버튼을
  누르고 로딩이 될때까지 로딩중 이라고 띄워 주는 거지?"로 구체화 → 실제 구현
  ("distraction 오디오 로딩 중.") 후 "안보이는데? 그리고 문구도 그냥 로딩 중
  이라고만 하면되"라는 피드백으로 문구를 단순화까지 했으나, 최종적으로 "그러면
  그냥 로딩중 띄우지 마 했던거 다시 롤백해"로 텍스트 표시 자체를 철회. 단,
  버튼 잠금(로딩 중 시나리오 선택 버튼 비활성화)은 "그 버튼 잠금은 좋은거
  같은데?"로 유지 확정. **[텍스트 표시는 롤백, 버튼 잠금은 유지 — 해결]**
- **스페이스바가 포커스된 위젯에 따라 재생/정지 토글이 안 먹힐 때가 있음** →
  `LabelingPage._build_ui()` 끝에서 모든 자식 `QWidget`에 `eventFilter`를
  설치해, 어떤 위젯이 포커스를 갖고 있어도 스페이스바는 항상 재생/정지를
  토글하도록 수정. **[해결]**
- **타임라인에 태스크 구간 라벨 텍스트가 겹쳐서 안 보임** (구간이 촘촘할 때) →
  `paintEvent`에서 라벨을 2행으로 교대 배치(`QFontMetrics`로 이전 라벨의 끝
  x좌표를 추적해 겹치면 다음 행으로). **[해결]**
- **타임라인을 확대(zoom)하면 재생헤드가 화면 밖으로 나가버림** →
  `set_playhead()`가 재생헤드가 뷰 경계 근처(`PLAYHEAD_FOLLOW_MARGIN_RATIO=5%`)에
  오면 뷰 범위 자체를(폭은 유지한 채) 이동시켜 자동으로 따라가도록 수정,
  더불어 수동 이동을 위한 "◀ 이동"/"이동 ▶" 버튼과 뷰-스크롤바 양방향 동기화
  (`_on_timeline_view_changed`/`_on_scrollbar_changed`, 재진입 방지 플래그
  포함)를 추가. **[해결]**
- **오디오 마이크 선택 관련 혼란** — 기본 오디오 스트림을 "데이터가 있는 첫
  번째 것"으로 자동 선택하던 방식이 라벨러 입장에서 어떤 마이크가 재생되는지
  알기 어려움 → 마이크 선택 콤보박스(`audio_combo`) 추가, 기본값
  "ecms1_audio", `_on_audio_stream_changed`로 전환 시 재생 중이던 위치를
  유지한 채 소스만 교체(`set_audio()`가 이전 플레이어를 정지 후 교체, 재생
  속도도 재적용). **[해결]**
- **재생 배속 조절 요청** — "배속에 0.25배, 0.5배, 0.75배 도 넣어줘" →
  `speed_combo`에 `["0.25x","0.5x","0.75x","1.0x","1.5x","2.0x"]` 추가,
  `set_playback_rate()`가 위치 점프 없이 배속만 재조정하도록 구현. **[해결]**
- **시나리오 화면에서 재생 중 뒤로 가기를 눌러도 오디오/영상이 계속 재생됨** →
  "지금 하나의 시나리오에서 영상과 음성이 재생중일때 다시 뒤로 돌아가서
  시나리오 선택으로 돌아가도 기존에 재생중이던게 정지되지 않아"라는 제보 →
  `MainWindow._on_back_from_labeling()`이 화면 전환 전에
  `page.playback.pause()`를 호출하도록 수정. **[해결]**
- **"작업 중인 구간들" 목록 클릭 시 재생 동작 설계** — "작업 중인 구간들의
  항목을 누르면 그 구간을 재생하나"라는 질문에서 시작 → "클릭해서 구간으로
  가면 그냥 쭉 재생이 되는게 아니라 그 짤린 구간에 대해서만 재생하게 해줘"로
  요구사항 확정 → `PlaybackController.set_playback_limit(end_ts)` 신규 도입
  (도달 시 자동 정지), `_on_draft_item_clicked`가 구간 시작으로 seek + 타임라인
  확대 + limit 설정을 함께 수행. **[해결]**
  - **한계 해제 케이스 누락 점검** — "그럼 지금 구간 미리보기 한계 해제가
    되는 case는 어떻게 되지?"라는 자체 점검 요청에 응해 전수 조사, 타임라인
    직접 클릭/`_load_task_window`(다른 태스크 전환)에서는 해제되지만 시작/끝점
    지정(`_on_mark_start`/`_on_mark_end`), 가이드라인 리셋
    (`_on_reset_to_guideline`), 프레임 단위 이동(`_step_frame`), 영상 화면
    클릭에서는 해제 로직이 빠져있음을 발견. "그리고 위에서 니가 지적한 놓힌경우도
    반영해" + "그 재생되는 화면쪽을 클릭 했을 때도"라는 요청에 따라 위 모든
    진입점에 `set_playback_limit(None)` 호출을 추가하고, `VideoPanel`에
    `clicked` 시그널을 새로 만들어 영상 패널 클릭도 감지되도록 함. **[해결]**
    - 이 과정에서 사용자가 "다른 항목 클릭 시 그 구간의 끝으로 갱신 갱신 말고
      해제로"라고 정정하려다 스스로 "이거는 무시해 헛소리했어"로 철회한
      해프닝이 있었음(기존 동작 그대로 유지, 실제 반영된 변경 없음).

## 12. 세션 재개(이어가기) 지원 + 동시성/경쟁 상태

- **이전 작업 흔적을 앱 재시작 후에도 확인할 수 있는지에 대한 질문** — "그럼
  지금 이전에 진행되었던 흔적은 확인하나? 한번 라벨링을 진행하다가 나중에
  다시 이어서 하게 되는 경우도 있을 수도 있잖아" → "음 그럼 재시작하면 이전에
  어디를 했는지 기록은 볼 수 있는거지?"로 구체화 → 당시 상태 점검 결과
  `DraftStore`는 JSON으로 영구 저장되지만, `SegmentNamer`의 세그먼트 번호
  카운터는 메모리 전용(`__init__`에서 항상 0부터 시작)이라 재시작 시 이전
  폴더를 덮어쓸 위험이 있음을 확인.
- **"라벨링 policy에 따르면 정합한가?"라는 질문** — 문서화된 규칙(시나리오별
  카운터 독립성)이 재시작-지속성까지 보장하진 않지만, "데이터를 조용히
  잃어버리면 안 된다"는 더 상위 원칙에 따라 수정이 필요하다고 설명 → "이어가기
  수정 진행하자"로 확정. → `SegmentNamer.__init__(session_dir)`이
  `session_dir/{scenario}/`를 스캔해 기존 `{scenario}_segment(\d+)` 폴더 중
  최댓값부터 번호를 잇도록 재작성. **[해결]**
- **"진행하다가 프로그램을 끄고 라벨링 했던 거 중에 삭제를 하고 다시 시작하면
  어떻게 행동하지?"라는 엣지 케이스 질문** → 당시엔 `DraftStore`가 커밋된
  draft를 무조건 "완료"로 취급해서, 폴더를 수동으로 지워도 앱에서는 계속
  완료로 표시되는 문제가 있다고 설명, "응"으로 수정 확정 → `LabelDraft`에
  `segment_dir` 필드 추가(커밋 시 실제 출력 경로 기록), `committed_window_ids()`가
  `segment_dir`이 있는 draft에 한해 `Path(segment_dir).exists()`를 확인해
  진짜 완료 여부를 판정(필드가 없는 과거 기록은 하위호환으로 그대로 신뢰). **[해결]**
- **"작업 중인 구간들" 목록이 최종 저장 후 사라지는 문제** — "한번 최종 커밋을
  누르고 나면 작업중인 구간 목록에서 직전까지 작업 했던게 사라지면 안되 오히려
  이전에 작업 했던것도 볼 수 있었으면 하는데?" → `DraftStore.all_drafts_for_scenario()`
  신규 추가(커밋 여부 무관 전체 반환), `_refresh_draft_list()`가 이걸 사용해
  완료 항목도 `[완료]` 접두사로 표시하되 수정/삭제는 막음(`_on_edit_draft`/
  `_on_delete_draft`가 `draft.committed`면 안내 메시지만 띄우고 차단). **[해결]**
- **동시에 여러 시나리오에서 최종 저장을 눌러도 되는지에 대한 질문** — "그럼
  지금 최종 커밋을 누르고 진행중일 때 다시 시나리오 선택이나 이런 다른 동작을
  해도 되나?" → 검토 결과 `SegmentExporter`가 세션당 하나로 공유되는 구조라,
  서로 다른 시나리오 페이지에서 동시에 최종 저장을 누르면 `_camera_index_cache`/
  `export_warnings`/`SegmentNamer._counters` 같은 공유 가변 상태를 두 스레드가
  동시에 건드리는 실제 경쟁 상태 위험이 있음을 확인.
  - 1차 제안("`is_exporting` 같은 잠금 플래그로 막자")은 사용자가 거부.
  - 2차 제안("최종 커밋을 누를 때마다 `SegmentExporter`를 새로 만들자")에
    사용자가 "2번으로 가면 되고"로 확정, 단 위 문단의 "작업 중인 구간 이력이
    사라지면 안 된다"는 요구를 동시에 재확인.
  - 이후 사용자가 "아니면 최종 커밋 누를 때마다 새로 스레드를 만드는건?"이라고
    대안을 재문의했으나 이미 스레드(`ExportWorker`)는 매번 새로 만들고 있었고,
    문제였던 건 그 안에서 쓰는 `SegmentExporter` 인스턴스 공유였음을 재설명 →
    결국 "최종 저장" 클릭마다 `SegmentExporter(self.trial, self.exporter.session_dir)`를
    새로 생성해서 쓰는 방식으로 구현, `SegmentNamer`가 이미 디스크 스캔 기반
    (위 항목)이라 매번 새 인스턴스를 만들어도 번호가 꼬이지 않음을 근거로
    안전성을 확보. **[해결]**
- **`annotations/` 폴더를 export 도중/이후 수동으로 지운 상태에서 다시 저장
  시도하면 에러** — 실제 스크린샷으로 `[Errno 2] No such file or directory`
  확인, 단 카메라/오디오/IMU/워치/레이더/annotation.json 등 실제 데이터
  파일은 이미 다 정상적으로 써진 뒤였고 마지막 csv 기록 단계에서만 실패하는
  상황이었음 → `export_draft()`가 매 호출마다(최초 1회가 아니라)
  `annotations_dir`와 시나리오별 csv+헤더 존재를 재확인/재생성하도록 수정
  (자가 치유). **[해결]**

## 13. 파일 탐색 다이얼로그 및 앱 종료 문제

- **"처음 runapp을 하고 찾아보기로 세션을 찾을 때 디렉토리가 현제 labeling
  tool 디렉토리를 열게 되어 있나?"라는 질문** → 확인 결과 특별히 지정 안 돼
  있어 OS/Qt 기본값(마지막으로 연 위치 등)을 따르고 있었음.
- **"/home/을 기본으로" 요청** 후 구현했으나 **"왜 home 디렉토리 선택이
  출력되지?"라며 거부(툴 사용 거절)** → 이후 "아니 그냥 디폴트로 찾아보기를
  누르면 home에서부터 시작되게 해줘 그리고 문구도 '디렉토리 선택'으로
  바꿔줘"로 요구사항 재정리 → `StartPage._browse()`가 `/home/`을 시작 경로로
  여는 `QFileDialog`를 열도록 수정, 다이얼로그 제목도 "디렉토리 선택"으로
  변경. **[해결]**
- **파일 다이얼로그가 메인 창 뒤에 숨어서 다시 앞으로 가져올 수 없음 / 다이얼로그가
  열린 채로는 메인 창을 닫을 수 없음** → 원인: 블로킹 정적 호출
  (`QFileDialog.getExistingDirectory`)과 네이티브 OS 다이얼로그의 창 관리
  방식이 얽힌 문제. 1차로 `DontUseNativeDialog` 옵션만 추가했으나 부분적
  해결에 그침 → 사용자가 진짜 원하는 동작을 "메인 창을 닫으면 그냥 전체가
  끝나게 하고 싶은거야"로 명확화 → `_browse()`를 `QFileDialog` 인스턴스를
  직접 만들어 `setFileMode(Directory)` + `ShowDirsOnly`/`DontUseNativeDialog`
  옵션 + `fileSelected` 시그널 연결 + `.open()`(비블로킹) 호출로 재작성
  (다이얼로그 인스턴스를 `self._browse_dialog`에 보관해 GC 방지),
  `MainWindow.closeEvent`에 `QApplication.instance().quit()`을 추가해 다른
  창이 열려 있어도 메인 창을 닫으면 앱 전체가 확실히 종료되도록 함. **[해결]**

## 14. road 카메라 물리적 장착 보정 (180도 회전)

- **"그리고 전에 말했던 road 카메라 영상 저장, 보여줄때 모두 180도
  회전시켜줘"라는 요청** — road 카메라가 실제로 거꾸로 장착돼 있어 화면/저장
  결과 모두 뒤집혀 나오는 문제를 이전에 논의한 적 있었음.
- **"좌우 반전도? 음 카메라가 거꾸로 메달려 있던 건데 어떻게 해야하지?"라는
  반문(툴 사용 거절 포함)** → 180도 회전은 좌우만 뒤집는 거울 반전이 아니라
  상하좌우를 동시에 뒤집는 것이며, 광축을 기준으로 카메라 자체가 뒤집혀
  매달린 상황(장면 전체가 위아래가 뒤집힌 것이지 좌우 대칭이 아님)에는 이게
  수학적으로 정확한 보정임을 설명 → "응"으로 확정.
- **구현 및 검증** — `front/stream_player.py`(화면 표시)와
  `back/segment_exporter.py`(최종 export) 양쪽에
  `cv2.rotate(frame, cv2.ROTATE_180)` 적용(`flip_180=True`를 road position
  스트림에만 지정). 처음엔 `np.array_equal`로 정확도를 검증하려 했으나 mp4v
  손실 압축 때문에 회전 여부와 무관하게 완전 일치가 나오지 않는 문제 발견 →
  평균 픽셀 차이(mean pixel diff) 비교로 검증 방법 교정: 올바르게 회전한
  프레임 vs 기대 프레임 = 평균 차이 약 3.9(압축 노이즈 수준), 회전 안 했다고
  가정한 프레임 vs 기대 프레임 = 평균 차이 약 30.5(실제로 다른 화면) → 회전
  로직이 올바름을 확인. **[해결]**

## 15. DMS_Actions.xlsx 라벨 taxonomy 채우기

- **드롭다운이 비어있는 문제의 근본 원인이 xlsx 파일 부재였음** — "지금
  원래 라벨링 페이지의 드롭박스를 채울 내용들을 비워 뒀는데 여기의 문서에서
  찾아"라며 PDF 스크린샷 여러 장(Table 2.1 Area/Verb/Noun/Abstract Noun 영문
  taxonomy, Table 2.2, Fig 2.1, Table 2.3 실제 한국어 지시문 표현, Table 2.4)
  제공 → 두 표를 교차 참조해서(예: "왼쪽 사이드미러를 조정하세요" → 동사
  "조정하다", 명사 "왼쪽 사이드미러") 6개 Area(좌우 창문/운전석/백미러 및
  선바이저/중앙 콘솔 및 화면/조수석/뒷좌석)별 한국어 동사/명사 목록을 정리,
  `openpyxl`로 `back/label_taxonomy.py`가 기대하는 형식(A열=Area명 첫 행만,
  B열=Verb, C열=Noun, D열=Abstract Noun 참고용)에 맞춰 프로젝트 루트에
  `DMS_Actions.xlsx` 신규 생성. `load_dms_actions()` 호출 및 실제
  `DistractionLabelForm` 드롭다운 채움까지 검증. **[해결]**
- **"그리고 -세부 는 뭐였지?"라는 질문** → 원본 표에서 상위 명사 항목의 하위
  세분류를 나타내는 표기(예: 상위 항목 아래 딸린 세부 옵션)임을 원문 대조로
  확인/설명.

## 16. PyInstaller 빌드 스펙 점검

- **"그럼 이제 이걸 빌드한다고 하면?"이라는 질문** → 이번 세션에서 추가된
  신규 모듈이 `dms_labeling.spec`의 `hiddenimports`에 다 반영됐는지 점검
  필요성 확인 → `find back front -name "*.py"` 결과와 spec의 기존 목록을
  직접 대조해 누락 3건 발견: `back.audio_stitcher`, `front.export_worker`,
  `front.labeling_page_loader` → 모두 추가. **[해결]**
- 부수 확인 사항: `requirements.txt`는 이번 세션에서 새 PyPI 의존성이
  추가되지 않아 변경 불필요, `DMS_Actions.xlsx`(신규 생성)는 spec의 기존
  `datas` 로직(`os.path.exists` 체크 후 자동 포함)이 그대로 프로젝트 루트에
  있으면 자동으로 번들에 포함시켜주므로 별도 수정 불필요, 신규 `QThread`
  서브클래스(`ExportWorker`, `LabelingPageDataLoader`)는 `PySide6.QtCore`가
  이미 hiddenimports에 있어 별도 처리 불필요함을 확인.

## 17. 재생 중 wall-clock 외삽이 실제 오디오 위치보다 앞서나가는 조기 정지 버그

- **재생 중 wall-clock 외삽이 실제 위치보다 앞서나가 조기 정지될 수 있는
  문제**(`/code-review`에서 지적됐던 항목) — `_on_display_tick()`이
  `positionChanged`로 마지막 확인된 `_audio_anchor_ms`에 벽시계 경과 시간을
  더해 오디오 위치를 추정하고, 그 추정치만으로 "구간 미리보기 한계"/"전체
  끝" 도달 여부를 판단해 정지하고 있었음 — 오디오 백엔드가 버퍼링 등으로
  잠깐 멈칫하면 벽시계는 계속 흐르는데 실제 오디오는 못 따라와서, 추정치가
  먼저 한계에 도달해 실제로는 구간이 다 재생되기 전에 정지해버릴 수 있는
  구조적 위험이었음(정상 재생 중에도 `positionChanged` 신호 간격(~64ms)
  자체가 추정 오차 여지를 줌).
  - 수정: `PlaybackController._on_display_tick()`에서, 추정치가 한계에
    도달해도 곧바로 정지하지 않고 마지막으로 확인된 실제 오디오 위치
    (`_audio_anchor_ms` 기준 `real_abs_ts`)가 아직 한계 미만이면 화면만
    한계값으로 고정해두고 정지를 보류, 다음 틱들에서 실제 위치가 진짜
    한계에 도달했을 때만 `pause()`를 호출하도록 변경. **[해결]**

## 18. export 시 센서 간 길이 불일치를 패딩으로 맞춤 (오디오 무음, 비디오 프레임 반복)

- **"비디오와 오디오가 어느정도 겹치면 오디오나 비디오 저장할 때 패딩 처리하자
  길이 맞추게 근데 보통 비디오 기준으로 자를테니까"라는 요청** - 라벨 구간이
  어느 센서의 실제 녹화 공백에 걸쳐서 그 센서만 짧게 잘리는 기존 동작
  (섹션 없음, 대화 중에만 설명됐던 "정직하게 없는 대로 둔다" 원칙)을 바꿔서,
  실제 데이터가 일부라도 있으면 없는 부분을 채워 항상 요청한 구간 길이
  그대로 나오게 해달라는 요청.
- **"거꾸로 비디오가 없을 때도 있나?"라는 자체 점검 질문** → 실제로 확인한
  결과 카메라도 오디오와 똑같이 독립적인 세그먼트 구조라 카메라 쪽에만
  공백이 생기는 것도 이론적으로 가능함을 확인(이번 세션에서 다룬 트라이얼들은
  우연히 항상 카메라가 더 온전했을 뿐). `_missing_sensor_coverage`의 카메라
  체크도 전체 범위만 보고 내부 공백은 못 잡는 한계가 있다는 점도 재확인.
- **패딩 범위를 어디까지 할지 논의** → "일부라도 실제 데이터가 있을 때만
  패딩해서 길이를 맞추고, 완전히 하나도 없으면 지금처럼 파일 자체를 안
  만든다"로 확정(전체 무음/공백 프레임만으로 된 파일을 새로 만들어내는 것까지는
  안 하기로).
- **비디오 패딩 방식 논의** — "그럼 지금 비디오가 짧으면 재생은 어떻게
  되지?"라는 질문에 실제 코드(`CameraTimestampIndex.frame_at_time`)를 확인해
  답변: 재생 중 카메라 공백에 들어가면 `None`(데이터 없음)이 아니라 **공백의
  중간 지점을 기준으로 더 가까운 쪽의 실제 경계 프레임**(공백 앞쪽 절반이면
  직전 프레임, 뒤쪽 절반이면 다음 프레임)으로 클램프된다는 걸 확인 → "지금처럼
  가까운 쪽으로 정지"하는 방식을 export 패딩에도 그대로 적용하기로 확정(재생과
  export의 공백 처리가 일관되게).
- **구현**:
  - `back/timestamp_index.py`의 `CameraTimestampIndex`/`AudioTimestampIndex.
    time_range_to_file_ranges()`가 각 조각의 절대 시작/끝 시각(+ 오디오는
    샘플레이트, 비디오는 fps)도 같이 반환하도록 확장(기존엔 로컬 인덱스만
    반환해서 조각 사이 공백의 실제 길이를 계산할 방법이 없었음). 호출부가
    `back/segment_exporter.py` 안에 딱 한 곳씩(카메라/오디오 각각)뿐이라 안전하게
    변경.
  - `_export_audio`: 조각 사이/앞/뒤 공백을 `np.zeros`(무음)로 채워 이어붙임 -
    재생용 `audio_stitcher.py`가 트라이얼 전체에 하던 것과 같은 방식을 라벨
    구간 하나짜리 범위에 적용.
  - `_export_cameras`/`_write_video_cut`: 공백을 절반으로 나눠 앞쪽은
    `_peek_frame()`으로 읽은 직전 조각의 마지막 프레임을, 뒤쪽은 다음 조각의
    첫 프레임을 반복해서 채움(`write_repeated` 헬퍼). 기존 seek+검증+폴백
    최적화(섹션 10)는 실제 조각을 읽는 부분에 그대로 유지, 패딩은 그 앞뒤에
    별도로 추가된 것이라 서로 안 건드림.
  - 구현 중 실수 발견/수정: `flip_180`(road 카메라) 스트림에서 마지막 프레임을
    기억해뒀다가 나중에 패딩용으로 반복 재사용할 때, 이미 회전된 프레임을
    다시 회전시켜서(`write_repeated`가 내부적으로 또 회전함) 원상복구되는
    버그가 있었음 - 회전 전 원본 프레임을 기억하도록 수정해서 해결.
- **검증**: 실제 트라이얼(`id002_trial1_20260812_145219_jang`, 카메라가 세그먼트
  4개 경계마다 26~148초 진짜 공백이 있음)로 확인.
  - 비디오: seg4→seg5 사이 51.4초 공백을 포함한 65초 구간을 잘라서, 공백
    앞쪽 절반 프레임이 seg4 마지막 프레임과 픽셀 차이 ~1.9(압축 노이즈 수준),
    seg5 첫 프레임과는 차이 ~26(뚜렷이 다름)임을 확인 - 반대쪽도 대칭적으로
    확인. 출력 프레임 수도 요청 길이(65.00s)에 정확히 맞음(1951프레임 ≈
    65.03s).
  - 오디오: 같은 방식으로 자른 wav의 공백 구간 샘플이 정확히 `0.0`, 경계
    바로 안쪽은 실제 신호(0이 아님)임을 확인. 길이도 요청한 65.000s와 정확히
    일치.
  - 공백이 없는 정상 케이스(기존 동작)도 회귀 없이 그대로 요청 길이만큼
    정확히 잘리는 것 확인, `export_draft()` 전체 파이프라인(IMU/워치/레이더/
    annotation.json 포함)도 정상 동작 확인. **[해결]**